/**
 * @module ReportsHubDialogs
 * @description Dialog and modal components for ReportsHub.
 *
 * The current feature set of ReportsHub has no dialogs or modals.
 * This file is a reserved placeholder following the project file-split convention.
 * Add dialog components here when they are introduced (e.g., a "report detail"
 * drawer, a "schedule configuration" modal, or a "confirm unsubscribe" dialog).
 *
 * Example skeleton:
 *
 *   export interface ReportDetailDialogProps {
 *     open: boolean;
 *     onOpenChange: (open: boolean) => void;
 *     reportId: number | null;
 *   }
 *
 *   export function ReportDetailDialog({ open, onOpenChange, reportId }: ReportDetailDialogProps) {
 *     // ...
 *   }
 */

export {};
