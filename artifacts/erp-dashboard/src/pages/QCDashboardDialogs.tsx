/**
 * @module QCDashboardDialogs
 * @description Dialog and modal components for the QC Dashboard.
 * Currently no dialogs are defined; this file is reserved for future additions
 * (e.g. defect detail modal, RCA creation dialog).
 */

// No dialog components exist yet — add them here as the feature grows.
export {};
