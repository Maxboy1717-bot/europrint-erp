/**
 * @module LessonPlayerDialogs
 * @description Dialog / modal components for LessonPlayer.
 * Currently no dialogs are required — this file is reserved for future additions
 * such as a certificate preview dialog or a lesson-notes dialog.
 */

// No dialog components yet. Export a placeholder so the module is always importable.
export {};
