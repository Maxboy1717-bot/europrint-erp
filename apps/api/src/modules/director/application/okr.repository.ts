/**
 * @module okr.repository (back-compat shim)
 * @deprecated Import from `../infrastructure/repositories/okr.repository`
 *   or via the `OKR_REPO` Symbol token.
 */

export {
  OkrRepository,
  type KeyResultsDashboard,
} from '../infrastructure/repositories/okr.repository';
