/**
 * @module zno.repository (back-compat shim)
 * @deprecated Import from `../infrastructure/repositories/zno.repository`
 *   or via the `ZNO_REPO` Symbol token.
 */

export { ZnoRepository } from '../infrastructure/repositories/zno.repository';
