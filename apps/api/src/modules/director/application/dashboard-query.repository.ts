/**
 * @module dashboard-query.repository (back-compat shim)
 * @deprecated Import from `../infrastructure/repositories/dashboard-query.repository`
 *   or via the `DASHBOARD_QUERY_REPO` Symbol token.
 */

export { DashboardQueryRepository } from '../infrastructure/repositories/dashboard-query.repository';
