/**
 * @module coordination.repository (back-compat shim)
 * @deprecated Import from `../infrastructure/repositories/coordination.repository`
 *   or via the `COORDINATION_REPO` Symbol token.
 */

export {
  CoordinationRepository,
  type DoklaStats,
  type RaspStats,
} from '../infrastructure/repositories/coordination.repository';
