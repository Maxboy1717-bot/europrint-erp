/**
 * @module kaizen.repository (back-compat shim)
 * @deprecated Import from `../infrastructure/repositories/kaizen.repository`
 *   or via the `KAIZEN_REPO` Symbol token.
 */

export { KaizenRepository } from '../infrastructure/repositories/kaizen.repository';
