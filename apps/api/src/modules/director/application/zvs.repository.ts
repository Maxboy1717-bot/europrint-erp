/**
 * @module zvs.repository (back-compat shim)
 * @deprecated Import from `../infrastructure/repositories/zvs.repository`
 *   or via the `ZVS_REPO` Symbol token.
 */

export { ZvsRepository } from '../infrastructure/repositories/zvs.repository';
