/**
 * @module hr-assets/hr-assets.service
 * @description Re-export shim. The service was merged into the hr module tree;
 * this barrel keeps legacy import paths working.
 */

export { HrAssetsService } from '../hr/hr-assets/hr-assets.service';
