/**
 * @module finance-ap.repository (back-compat shim)
 * @deprecated Import from `../infrastructure/repositories/finance-ap.repository`
 *   or via the `FINANCE_AP_REPO` Symbol token.
 */

export {
  FinanceApRepository,
  type ApBucket,
} from '../infrastructure/repositories/finance-ap.repository';
