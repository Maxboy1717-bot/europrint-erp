/**
 * @module finance-ar.repository (back-compat shim)
 * @deprecated Import from `../infrastructure/repositories/finance-ar.repository`
 *   or via the `FINANCE_AR_REPO` Symbol token.
 */

export {
  FinanceArRepository,
  type ArBucket,
} from '../infrastructure/repositories/finance-ar.repository';
