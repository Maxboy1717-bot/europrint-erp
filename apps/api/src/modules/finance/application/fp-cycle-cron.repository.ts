/**
 * @module fp-cycle-cron.repository (back-compat shim)
 * @deprecated Import from `../infrastructure/repositories/fp-cycle-cron.repository`
 *   or via the `FP_CYCLE_CRON_REPO` Symbol token.
 */

export { FpCycleCronRepository } from '../infrastructure/repositories/fp-cycle-cron.repository';
