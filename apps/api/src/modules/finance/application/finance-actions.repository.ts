/**
 * @module finance-actions.repository (back-compat shim)
 * @deprecated Import from `../infrastructure/repositories/finance-actions.repository`
 *   or via the `FINANCE_ACTIONS_REPO` Symbol token.
 */

export { FinanceActionsRepository } from '../infrastructure/repositories/finance-actions.repository';
