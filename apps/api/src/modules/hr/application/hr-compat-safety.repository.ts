/**
 * @module hr-compat-safety.repository (back-compat shim)
 * @deprecated Import from `../infrastructure/repositories/hr-compat-safety.repository`
 *   or via the `HR_COMPAT_SAFETY_REPO` Symbol token in the domain layer.
 */

export { HrCompatSafetyRepository } from '../infrastructure/repositories/hr-compat-safety.repository';
