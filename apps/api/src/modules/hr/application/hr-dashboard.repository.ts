/**
 * @module hr-dashboard.repository (back-compat shim)
 * @deprecated Import from `../infrastructure/repositories/hr-dashboard.repository`
 *   or via the `HR_DASHBOARD_REPO` Symbol token in the domain layer.
 */

export {
  HrDashboardRepository,
  type DailyReportsStatsRow,
} from '../infrastructure/repositories/hr-dashboard.repository';
