/**
 * @module hr-compat-a.repository (back-compat shim)
 * @deprecated Import from `../infrastructure/repositories/hr-compat-a.repository`
 *   or via the `HR_COMPAT_A_REPO` Symbol token in the domain layer.
 */

export { HrCompatARepository } from '../infrastructure/repositories/hr-compat-a.repository';
