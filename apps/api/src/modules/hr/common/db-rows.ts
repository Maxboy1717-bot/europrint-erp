/**
 * @module hr/common/db-rows
 * @deprecated PA2-15 — use `@common/db/db-rows` directly. This shim remains so
 * the existing 42 import sites continue to work until they are migrated.
 */

export * from '@common/db/db-rows';
