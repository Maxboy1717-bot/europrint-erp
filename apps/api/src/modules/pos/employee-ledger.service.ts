/**
 * @module pos/employee-ledger.service (barrel re-export)
 * @description Convenience re-export so tests can import from the module root.
 */
export { EmployeeLedgerService } from './application/services/employee-ledger.service';
