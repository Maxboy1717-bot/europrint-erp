/**
 * @module pos/pos-request-ext.repository (barrel re-export)
 * @description Convenience re-export so tests can import from the module root.
 */
export { PosRequestExtRepository } from './infrastructure/repositories/pos-request-ext.repository';
