/**
 * @module raci.repository (back-compat shim)
 * @deprecated Import from `../infrastructure/repositories/raci.repository`
 *   or via the `RACI_REPO` Symbol token.
 */

export { RaciRepository } from '../infrastructure/repositories/raci.repository';
