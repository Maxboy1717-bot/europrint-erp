/**
 * Re-export shim: ApplicationsRepository lives in hr/applications.
 * This barrel keeps the legacy applications/ import path working.
 */
export { ApplicationsRepository } from '../hr/applications/applications.repository';
