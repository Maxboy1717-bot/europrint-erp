/**
 * Re-export shim: ApplicationsService lives in hr/applications.
 * This barrel keeps the legacy applications/ import path working.
 */
export { ApplicationsService } from '../hr/applications/applications.service';
