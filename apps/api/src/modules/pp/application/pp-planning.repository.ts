/**
 * @module pp-planning.repository (back-compat shim)
 * @deprecated Import from `../infrastructure/repositories/pp-planning.repository`
 *   or via the `PP_PLANNING_REPO` Symbol token.
 */

export { PpPlanningRepository } from '../infrastructure/repositories/pp-planning.repository';
