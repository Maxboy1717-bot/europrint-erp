/**
 * @module pp/application/pp-planning.repository (barrel re-export)
 * @description Convenience re-export so tests can import from the application layer.
 */
export { PpPlanningRepository } from '../infrastructure/repositories/pp-planning.repository';
