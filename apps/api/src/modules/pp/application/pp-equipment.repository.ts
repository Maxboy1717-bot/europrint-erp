/**
 * @module pp-equipment.repository (back-compat shim)
 * @deprecated Import from `../infrastructure/repositories/pp-equipment.repository`
 *   or via the `PP_EQUIPMENT_REPO` Symbol token.
 */

export { PpEquipmentRepository } from '../infrastructure/repositories/pp-equipment.repository';
