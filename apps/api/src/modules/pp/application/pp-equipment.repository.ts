/**
 * @module pp/application/pp-equipment.repository (barrel re-export)
 * @description Convenience re-export so tests can import from the application layer.
 */
export { PpEquipmentRepository } from '../infrastructure/repositories/pp-equipment.repository';
