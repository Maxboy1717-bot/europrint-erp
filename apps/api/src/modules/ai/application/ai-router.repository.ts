/**
 * @module ai-router.repository (back-compat shim)
 * @deprecated Import from `../infrastructure/repositories/ai-router.repository`
 *   or via the `AI_ROUTER_REPO` Symbol token.
 */

export { AiRouterRepository } from '../infrastructure/repositories/ai-router.repository';
