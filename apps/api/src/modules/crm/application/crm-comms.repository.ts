/**
 * @module crm-comms.repository (back-compat shim)
 * @deprecated Import from `../infrastructure/repositories/crm-comms.repository`
 *   or via the `CRM_COMMS_REPO` Symbol token.
 */

export { CrmCommsRepository } from '../infrastructure/repositories/crm-comms.repository';
