/**
 * @module crm-followup-compat.repository (back-compat shim)
 * @deprecated Import from `../infrastructure/repositories/crm-followup-compat.repository`
 *   or via the `CRM_FOLLOWUP_COMPAT_REPO` Symbol token.
 */

export { CrmFollowupCompatRepository } from '../infrastructure/repositories/crm-followup-compat.repository';
