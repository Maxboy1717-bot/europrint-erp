/**
 * @deprecated Empty stub — two real implementations exist:
 *   1. apps/api/src/telegram/telegram.service.ts — TelegramService (node-telegram-bot-api,
 *      used by cron jobs, telegram handlers, crm/listeners)
 *   2. apps/api/src/modules/notifications/telegram/telegram.service.ts — TelegramSvc
 *      (notifications module, DB-backed, full delivery tracking)
 *
 * No consumers import from this path. TODO: delete this file.
 */
 
export {};
