/**
 * @module iot-camera.repository (back-compat shim)
 * @deprecated Import from `../infrastructure/repositories/iot-camera.repository`
 *   or via the `IOT_CAMERA_REPO` Symbol token.
 */

export { IotCameraRepository } from '../infrastructure/repositories/iot-camera.repository';
