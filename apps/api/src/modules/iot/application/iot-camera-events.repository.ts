/**
 * @module iot-camera-events.repository (back-compat shim)
 * @deprecated Import from `../infrastructure/repositories/iot-camera-events.repository`
 *   or via the `IOT_CAMERA_EVENTS_REPO` Symbol token.
 */

export { IotCameraEventsRepository } from '../infrastructure/repositories/iot-camera-events.repository';
