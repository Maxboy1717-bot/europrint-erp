/**
 * @module wms-extended.repository (back-compat shim)
 * @deprecated Import from `../infrastructure/repositories/wms-extended.repository`
 *   or via the `WMS_EXTENDED_REPO` Symbol token.
 */

export { WmsExtendedRepository } from '../infrastructure/repositories/wms-extended.repository';
