/**
 * @module inventory-materials.repository (back-compat shim)
 * @deprecated Import from `../infrastructure/repositories/inventory-materials.repository`
 *   or via the `INVENTORY_MATERIALS_REPO` Symbol token.
 */

export { InventoryMaterialsRepository } from '../infrastructure/repositories/inventory-materials.repository';
