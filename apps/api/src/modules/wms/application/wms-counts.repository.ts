/**
 * @module wms-counts.repository (back-compat shim)
 * @deprecated Import from `../infrastructure/repositories/wms-counts.repository`
 *   or via the `WMS_COUNTS_REPO` Symbol token.
 */

export { WmsCountsRepository } from '../infrastructure/repositories/wms-counts.repository';
