/**
 * @module warehouse-rental.repository (back-compat shim)
 * @deprecated Import from `../infrastructure/repositories/warehouse-rental.repository`
 *   or via the `WAREHOUSE_RENTAL_REPO` Symbol token.
 */

export { WarehouseRentalRepository } from '../infrastructure/repositories/warehouse-rental.repository';
