/**
 * @module design-extended.repository (back-compat shim)
 * @deprecated Import from `../infrastructure/repositories/design-extended.repository`
 *   or via the `DESIGN_EXTENDED_REPO` Symbol token.
 */

export { DesignExtendedRepository } from '../infrastructure/repositories/design-extended.repository';
