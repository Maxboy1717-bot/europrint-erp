# LOOP HANDOFF — relay baton (har 20-daqiqa iteratsiya o'qiydi + yangilaydi)

> ⭐ Egasi 2026-06-23: har 20 daqiqada bitta iteratsiya, to'xtamaydi, vizyon to'liq tugaguncha.
> **HAR ITERATSIYA PROTOKOLI (sifatli + tez):**
> 1. **TEKSHIR** — bu faylni + `500K_QURISH_REJA_PROMPT.md` §6 + git holatni o'qi; oldingi ✅ ishni regress-test (jonli DB-proof hali ushlaydimi); in-flight Workflow bormi (TaskList) — bo'lsa uni qayta ishla, yo'q bo'lsa keyingisini boshla.
> 2. **OL** — "KEYINGI BUILDABLE" navbatdan birinchi ✅-emasni ol (yoki yangi disconnected-real/orphan piece scope qil).
> 3. **SCOPE** — Workflow (fan-out scope + adversarial verify, Q-29). ⚠️ Scope agentlari bu loyihada KO'P yashil-yolg'on berdi (uuid╳int schema-manufacturing.ts o'lik-binding'dan; "no-op/broken/two-worlds") — HAR tip/holat da'vosini O'ZING `node _audit/q.cjs` (information_schema) + kod-Read bilan JONLI tasdiqla.
> 4. **QUR** — Result/Zod/Drizzle. Two-worlds'ni bosh-dasturchi sifatida YECH (kanonik integer; data bo'sh→konvergatsiya, data bor→additiv int-link). **FABRIKATSIYA TAQIQ (Q-40):** real DATA/AI yo'q bo'lsa SOXTA yozma — STRUKTURA qur + pastdagi ro'yxatga yoz.
> 5. **TEST + JONLI SINAB** — `tsc` GREEN (mening fayllarimda 0 yangi xato) + END-TO-END real-data DB-proof (rollback-tx: kirit→oqdi→ko'rindi). Plumbing/struktura-only YETARLI EMAS.
> 6. **BOSHQA MUAMMOLAR** — ish davomida topilgan boshqa muammolarni "BOSHQA TOPILGAN MUAMMOLAR" ga yoz (keyingi agentga baton).
> 7. **COMMIT** — faqat o'z fayllarim, push-siz, `--no-verify`, Co-Authored-By. Bu faylni yangila (✅ belgila + holat). Locked skip: crm/ai/ai-agents/integration/org-structure/auth/finance-reports + sd create-invoice.handler.

---

## HOLAT (oxirgi yangilanish: 2026-06-24 iter-59)
- ✅ **Golden-thread yadro 4/5:** SD→PP (`59341adf` real fix) · PP→MES (proven) · MES→QC (proven) · QC→WMS (real persistent: warehouse_stock.75/76). Hammasi jonli DB-proof.
- ✅ **SD→Design sub-flow (Trigger 3):** `4e564804` real fix — stub-listener→`@OnEvent(SO_DESIGN_REQUESTED)` + idempotent `ensureForSalesOrder` (dedupe sales_order_id) + EventBridge `DesignRequired`→canonical map. designFlag → outbox `sd.order.design_requested` → emit → design_orders INSERT. DB-proof (rollback-tx): INSERT qabul, sales_order_id link, idempotent=1, rollback=0; tsc=0. HTTP-firing token kutadi.
- ✅ **WMS→FIN (goods-issue/FG GL):** VERIFIED-BUILT (iter-2) — inventory/COGS `entries` ledger posting real (finance WmsGoodsIssuedListener + WmsFgReceivedGlListener). Bu **customer-invoice'dan FARQLI** hop.
- ⛔ **WMS→FIN(customer-invoice):** GATED — 3 invoice-jadval (invoices uuid / sales_invoices text / fi_invoices) integer sales_orders'ga ulanmaydi + create-invoice.handler LOCKED.

## KEYINGI BUILDABLE (navbat — fabrikatsiyasiz, jonli-isbotli; har biri SCOPE+JONLI-VERIFY shart)
1. [✅ DONE 2026-06-23 `4e564804`] SD→Design sub-flow QURILDI (HOLAT). Naqsh: outbox `@OnEvent(canonical)` + idempotent INSERT + EVENT_NAME_MAP.
2. [⛔ GATED iter-2 2026-06-23] SAMPLE sub-flow. JONLI tasdiq: yagona sample-jadval = `ow_order_samples` (`order_id` **uuid**, NOT NULL) → MAVJUD BO'LMAGAN `ow_orders` jadvaliga ishora (`order-workflow` moduli yo'q jadvalni o'qiydi = **dead order-world**, 0 qator). Integer `sales_orders.id` (create-order `{orderId}`) bu uuid-jadvalga ulanmaydi. Fabrikatsiya/buzuq-uuid-bridge YOZILMADI (Q-40). YECHIM EGASIDA: sample-modelini integer `sales_orders` tomonida belgilash (yangi `sd_samples` integer-jadval APPROVED) YOKI ow_* order-world'ni tiriltirish qarori. Shungача SampleRequired event jim oqadi (zarar yo'q).
3. [⛔ GATED iter-2 2026-06-23] PP routing-execution: `production_order_operations` (exec) + `routing_operations` (master) + `routings` HAMMASI INTEGER (toza, two-worlds YO'Q — eski "routings uuid" da'vosi NOTO'G'RI, jonli `routings.id`=integer). LEKIN `routings=0` + `routing_operations=0` (master BO'SH). Exec'ni instansiyalashga manba yo'q. Bu **EGASI-DATA**: mahsulot bo'yicha operatsiya ketma-ketligi (sex/work_center, planned_duration, sequence). Egasi routing-master bersa (yoki config-forma orqali kiritsa) → "PO release → operatsiyalarni instansiyala + start/complete lifecycle" qurilADI (production_orders=7 real bor).
4. [✅ FIXED+VERIFIED iter-3 2026-06-23 `97391cfd`] MES OEE. JONLI topildi: kanonik `getOee()` (mes-shifts-stats.repo) REAL world-standard A×P×Q'ni production_sessions actual signallaridan hisoblaydi (run/stop sekund, target/actual/defect, downtime fold-in, guarded) — ✅ to'g'ri. Pre-stored `production_sessions.oee/availability` ustunlar QASDAN eskirgan-seed deb e'tiborsiz (README:93). LEKIN `getCurrentShift()`:32 stale `AVG(mps.oee)` o'qirdi (green-lie, mixed-scale seed) → TUZATILDI: real A×P×Q (DB-proof: 77.27 stale → 78.10 real). ⚠️ Performance omili = actual/target proxy (kanonik = idealCycleTime×qty/runTime); ideal-cycle-time ustuni YO'Q = **EGASI-DATA** (routing standart-vaqt). U kelganda performance aniqlashadi.
5. [⛔ GATED-ORPHAN iter-4 2026-06-23 workflow+adversarial-verify] QC rework → routing-graf. `production_order_operations.rework_count/rework_reason` integer-toza + QcFailedEvent real (submit-inspection.handler:54), QC-fail→rework listener QURISA BO'LARDI. LEKIN adversarial-verify REFUTE qildi: `production_order_operations` BUTUN tizimda (BE+FE) **0 reader** — yozilgan rework_count'ni HECH NARSA o'qimaydi (OEE "consumer"=doc-izoh; FE reworkCount=QC-defect, poo emas) + 0 qator. Orphan-write (Q-46 buzadi). YECHIM: avval poo-reader (routing-graf UI / OEE rework-analizi) qurilishi kerak — keyin bu listener mazmunli bo'ladi.
6. [✅ VERIFIED-BUILT iter-2 2026-06-23] WMS FEFO goods-issue → warehouse_stock decrement + GL. Stub EMAS: `goods-issue.handler` tx + FIFO/FEFO batch + guarded warehouse_stock decrement + `wms_goods_issues` audit + WmsGoodsIssuedEvent. GL: `finance/.../wms-goods-issued.listener` @EventsHandler → `entries` ga `Dr COGS 9100/Cr Inventory 1000` (material_cards unit_price baholash, idempotent ref, narx yo'q→skip Q-40) + WmsFgReceivedGlListener `Dr Inventory/Cr AP`. DB-proof: ws#1 (mat 8, ombor 4) 45→40 decrement, over-issue bloklandi (negative guard), rollback→45. ⚠️ GL real-firing: material_cards.unit_price (egasi-DATA) kerak, aks holda skip.
7. Director/LMS/Marketing/POS-CC — workflow+adversarial scope (iter-4 2026-06-23) natijalari:
   - **Director:** ⛔ gated — modul TO'LIQ qurilgan (hamma endpoint real DB); yagona stub `aiInsights:[]` = AI-hudud (LOCKED).
   - **LMS:** [✅ FIXED iter-4 `d8918852`] sertifikat status persist bug — `updateCertificateStatus` textual status'ni boolean'ga collapse qilardi → 'revoked'/'expired' tashlanardi → kunlik expiry-sweep o'sha qatorlarni CHEKSIZ qayta-flag qilardi (CertificateExpiredEvent qayta-publish). Fix: raw SQL (binding 3-ustun stub, `status` yo'q) status+is_active+updated_at yozadi; caller string uzatadi. DB-proof: would_reflag TRUE→FALSE (loop yopildi), 'revoked'/'active' persist.
   - **Marketing A/B:** [✅ TO'LIQ iter-5+6 `99a9b02d`+`1e4ef67e`] BE: `@Post('ab-tests')` (DB-proof count 0→1→rollback 0). FE: AbSection create-form (name+variant A/B) + useMutation(POST)+invalidate; AbTest type DB-shaklga; display real ikki-variant convRate (guarded — eski `t.visitors.toLocaleString()` crash-bug tuzatildi). FE tsc 0. ⚠️ Vizual round-trip auth-token kutadi (login-gated). Modul user-alive (token kelganda ko'rinadi).
   - **POS-CC:** ⛔ gated-orphan — `cc_audit_trail` 'created' entry qo'shsa bo'lardi, lekin jadval BUTUN tizimda **0 reader** (write-only; SELECT yo'q, FE timeline yo'q). "audit timeline consumer"=fabrikatsiya. Avval reader kerak.
> ⭐ FAZA PIVOT (iter-5 2026-06-23): **BE golden-thread buildable yuzasi KONVERGATSIYALANDI** — SD→PP/Design, PP→MES, MES→QC+OEE, QC→WMS, WMS→FIN(goods-issue/FG GL), LMS-cert hammasi qurilgan/tuzatilgan; qolgan BE-uzilishlar (sample, PP-routing-exec, customer-invoice, qc-rework, cc-audit) EGASI-DATA yoki dead-world yoki orphan(reader-yo'q) yoki locked. **Loop endi FE-WIRING + READER-UI fazasiga o'tadi** (FE locked emas): real BE endpoint bor-u FE forma/displayi yo'q joylarni ulash → modullar USER-ALIVE bo'ladi. Nomzodlar: (a) ✅ Marketing A/B create-form (iter-6 DONE), (b) orphan reader-UI'lar (qc rework_count, cc_audit_trail timeline — writer+reader birga), (c) boshqa "BE-real lekin FE-yo'q" formalar. **KEYINGI iteratsiya:** Workflow-scope bilan boshqa modullarda "BE-real endpoint bor (POST/PUT) lekin FE forma/mutation yo'q" bo'shliqlarni top (Marketing A/B kabi) — fan-out scope + adversarial-verify (consumer/endpoint REAL ekanini tasdiqla), eng tozasini FE-ula. ⚠️ FE round-trip auth-token kutadi → tsc + guarded-mantiq + naqsh-moslik = token-siz proof-bar.

## ⚙️ CONFIG-MEXANIZM (egasi-DATA EMAS — qurilADI: mexanizm qur + sample bilan self-test; egasi keyin sozlaydi)
⭐ VIZYON-PRINSIP (egasi 2026-06-23): HAMMA narsa SOZLANADIGAN. Qiymat KUTMA — set/change endpoint+forma qur + sample bilan o'zing test qil (set→change→DB-proof). Egasi real qiymatni tizim orqali sozlaydi/o'zgartiradi. ([[feedback_everything_configurable]])
- ✅ **PP norma/brak/crew BE:** config-mexanizm (`PUT /pp/work-centers/:id/norms`, T4) + 9 sex sample + CHANGE isbotlandi (2026-06-23).
- ✅ **PP norma/brak/crew FE (iter-12 `fb7bee1f`):** `WorkCenterNormsConfig.tsx` — 9 sex dialog-based jadval (norma_m2_per_shift/brak_limit_pct/min+max crew + sabab ≥5). Route `/pp/work-center-norms`. DB-proof: id=5 5500→6000→ROLLBACK CLEAN.
- ✅ **Gofra take-up (marka koeff):** config-WRITE mexanizm qurildi (`PUT /pp/gofra/flute-types/:code` + `updateFluteFactor`, `4b4001d6`). DB-proof: C take-up 1.45→1.60 CHANGE → grammage 454→472 (config formulaga ta'sir). Egasi sozlaydi.
- ✅ **Gofra take-up FE config (iter-10 `41567190`):** `useGofrConversion` hook'ga `updateFluteFactor` mutation qo'shildi (PUT /:code `{takeUpFactor}`). `GofraFluteConfig.tsx` sahifasi yaratildi — 5 flute (A/B/C/E/BC) inline-edit jadval, kalit+Enter saqlaydi. Route `/pp/gofra-config` wired. DB-proof: C 1.45→1.60→ROLLBACK CLEAN. FE tsc 0 yangi xato.
- ✅ **Material layer-stack (gofra grammage manbai):** config-WRITE qurildi (`PUT /technology/materials/:id/layers` + `setMaterialLayers` replace-set tx, `946857a1`). DB-proof: material.1 ga 3 layer SET → grammage 454; flute gsm 120→150 CHANGE → 497.5; rollback 0. Iste'molchi: technology-grammage computeForCard + layer-formula.service. Egasi material layer'larini sozlaydi → grammage real hisoblanadi.
- ✅ **Smena jadvali (iter-11 `9d1d25d3`):** shift_types seeded (MORNING/EVENING/NIGHT) + GET/PATCH `/hr/shifts/types` endpoint (COALESCE safe-update) + ShiftTypesConfig.tsx inline-edit. NUMERIC(3,1) koeff. DB-proof: 1.0→1.5→ROLLBACK CLEAN. Kunlik kech-kelish cron (silently broken) ENDI smena waqtlarini DB'dan o'qiydi.
- ✅ **KPI chegaralari (iter-13 `0fb0c8b3`):** GET+PATCH `/director/dashboard/kpi-definitions` — 6 HR KPI ta'rifi (target/warning/critical) inline-edit. `KpiThresholdConfig.tsx` yaratildi + `/director/kpi-thresholds` route. DB-proof: id=5 82→85→ROLLBACK CLEAN.
- ✅ **Kompaniya holat chegaralari (iter-14 `cab5949c`):** GET+PATCH `/company-state/thresholds` — 25 state_thresholds (5 metrika×5 daraja: min/max + vazn). `CompanyStateThresholdConfig.tsx` + `/director/company-state-config` route. DB-proof: id=17 hr/NORMAL 94→96→ROLLBACK CLEAN.
- ✅ **QC parametrlar config (iter-15 `3d4c85a7`):** `QcParametersConfig.tsx` — grouped inline-edit jadval (min/maqsad/max) + "Standart yuklash" (POST /qc/seed-parameters). GET /api/qc/parameters/grouped + PATCH /api/qc/parameters/:id. 5 domain param seeded (printing/color/material/finishing/cutting). Route `/qc/parameters-config`. DB-proof: id=1 min_value 85→80→ROLLBACK CLEAN. FE tsc 0.
- ✅ **Razryad imtihon % (iter-16 `4775c447`):** `RazryadLevelConfig.tsx` — 6 razryad daraja inline-edit (examPassThreshold/maxRetakes/salaryMin/salaryMax). GET+PATCH `/org-structure/razryad-levels`. Route `/hr/razryad-config`. DB-proof: level=1 NULL→75%/1.5M/2.5M→ROLLBACK CLEAN.
- ✅ **STKP vazn (iter-19 `c0345d61`):** kpi_score_weights (3 qator: attendance 50%/performance 30%/tasks 20%) + GET/PATCH `/director/dashboard/kpi-weights/:code` + `KpiScoreWeightsConfig.tsx` inline-edit (% + jami 100% guard). Route `/director/kpi-weights`. DB-proof: 0.5→0.4→ROLLBACK CLEAN.
- ✅ **gofra chiqindi%/kley (config, iter-20 `e40966ef`):** `gofra_config` jadval (2 qator: waste_pct_default=5%/kley_gsm=120g/m²) + GET/PATCH `/pp/gofra/config/:key` + `GofraWasteConfig.tsx` inline-edit, route `/pp/gofra-waste-config`. DB-proof: waste_pct 5→8.5→ROLLBACK→5 CLEAN.
- ✅ **norma birlik (m²/kg/list, iter-21 `6a43a892`):** `work_centers.unit_preference VARCHAR(10) DEFAULT 'm2'` DDL. DTO+command+repo+controller yangilandi. `WorkCenterNormsConfig.tsx` Select dropdown + jadval ustuni. DB-proof: id=5 m2→kg→ROLLBACK→m2 CLEAN.
- ⛔ **CKP koeff** (org-funksiya config — LOCKED). Har biri: jadval/endpoint + sample self-test.
- 🔴 **Haqiqiy un-fabricatable DATA (faqat egasi):** 22-sex ro'yxatining KENGAYISHI (hozir 9 bor) · 21-material nomlari · manager_id (kim-kimni-boshqaradi) · head_user_id · **routing-master** (mahsulot→operatsiya ketma-ketligi: sex/work_center, planned_duration — PP routing-execution shunsiz instansiyalanmaydi) · **material_cards.unit_price** (WMS→FIN goods-issue/FG GL valuation shunsiz skip qiladi) · **sample-model qarori** (sd_samples integer-jadval kerakmi yoki ow_* tiriltiriladimi). Bularni config-forma bilan KIRITISH mumkin, lekin qiymat egasiniki.

## 🟡 AI-KALIT KUTADI
AISHA dispatcher · AI 7-step planning · karta-AI fit-scorer · OEE-AI · Finance-AI · lead-scoring · AI-kamera VLM.

## ⚙️ ARXITEKTURA-QAROR
kanonik invoice-jadval (invoices uuid / sales_invoices text / fi_invoices) · 2-order-dunyo · LOCKED: create-invoice.handler (egasi ochishi kerak).

## ITER-17+ FE-WIRING TOPILGAN NOMZODLAR (iter-17 scope agenti natijasi)
- **[iter-17 `67a1f387`] ✅ MRO Oshxona yozuv forma:** `CanteenManagementPage.tsx` — GET /mro/canteen/logs list + POST /mro/canteen/logs dialog (meal_name/date/portion/cost/employees). DB-proof: INSERT 45porsiya→ROLLBACK count=0. FE tsc 0.
- **[iter-18 `a26a17ad`] ✅ QC Yetkazuvchi sifat tekshiruv forma:** `SupplierQualityPage.tsx` — POST /qc/supplier-quality → qc_supplier_quality INSERT. supplier_name(required)+sample_size+defects_found+quality_score+notes. Pass-rate auto-preview. DB-proof: id=3 visible→ROLLBACK count=0. FE tsc 0.
- **⛔ DefectManagementPage.tsx** — faqat GET `/qc/defects/extended`. POST `/qc/braks` = QCBraksTab.tsx DA ALLAQACHON ulangan (mutation bor). POST `/qc/defects` = CQRS `inspectionId`/`productionOrderId` FK kerak (tiqilinch). SKIP.
- **⛔ MRO CleaningSchedulePage** — faqat GET, POST endpoint YO'Q. SKIP.
- **⛔ MRO UtilityReadingsPage** — faqat GET, POST endpoint YO'Q. SKIP.
- **⛔ MRO FacilityInventoryPage** — faqat GET `/mro/facilities`, POST endpoint YO'Q. SKIP.
- **[iter-22 `f799dc3b`] ✅ QC Reklamatsiya yaratish forma:** `ReclamationsPage.tsx` — POST /qc/reclamations Dialog (customerName+description min10+severity select). DB-proof: id=5→ROLLBACK CLEAN. FE tsc 0.
- **[iter-23 `a66fe4a6`] ✅ QC Sifat sertifikati yaratish forma:** `QualityCertificatesPage.tsx` — POST /qc/certificates Dialog (certNumber required+productName+issuedDate+status select+issuedBy+notes). DB-proof: INSERT id=12→ROLLBACK→count=0 CLEAN. FE tsc 0.
- **[iter-24 `f6dd06e5`] ✅ HR DailyReportsTab hisobot topshirish forma:** employee-profile/DailyReportsTab.tsx — POST /hr-v2/daily-reports Dialog (report_date+tasks_completed required+tomorrowPlan+blockers+mood select). DB-proof: id=6511 INSERT→ROLLBACK→6510 CLEAN. FE tsc 0.
- **[iter-25 `75459ea5`] ✅ HR eNPS so'rov yaratish forma:** HREnps.tsx — POST /hr-v2/enps CreateEnpsSurveyDialog (title required+description+period select monthly/quarterly/annual+start_date+end_date). enps_surveys: 1 qator bor. DB-proof: id=34 INSERT→ROLLBACK→count=1 CLEAN. FE tsc 0. Invalidates /api/hr/enps/surveys.
- **[iter-26 `50a4d7c8`] ✅ SD Yetkazma yaratish forma:** SDDeliveries.tsx — POST /api/sd/deliveries CreateDeliveryDialog (salesOrderId+driverName+vehicleNumber+plannedDate). deliveries=1 real row. DB-proof: id=4 PICKING→ROLLBACK→count=1 CLEAN. FE tsc 0 yangi xato. TEKSHIRILGAN-GATED: HRGamification(POST yo'q), MentorsPage(0 row+POST yo'q), HRMilestones(jadval yo'q), MonthlyReportTab(read-only), ObligationsTab(wired), FinanceTab advances(wired), WMSMaterials(MaterialDialog wired), EquipmentPage(wired), MESDowntimes(wired), MMVendors(wired), LMSDashboard(wired), SafetyViolationsPage(POST yo'q), QuestionnaireTemplates(POST yo'q).
- **[iter-27 `6aa5b07a`+`77a852b2`] ✅ MM Fleet mashina yaratish forma (Q-40 tuzatish + schema drift fix):** `MMExtended.tsx` — hardcoded `mutate({plateNumber:"NEW-001",...})` Q-40 buzilishini AddVehicleDialog bilan almashtirdi (plate_number req+model+year+type select). BE `MmCreateFleetVehicleSchema`: `registration_number`→`plate_number`, `fuel_type`→`type(own/rent/truck)` drift tuzatildi. `LogisticsDashboard.tsx`: plateNumber→plate_number transform at submit. DB-proof: mm_vehicles INSERT id=1→ROLLBACK→count=0 CLEAN. FE tsc 0. TEKSHIRILGAN-GATED: PP production_orders (boms=0+routings=0 → bomId/routingId form bo'sh dropdown → Q-46), design_orders=0 rows → Q-46.
- **[iter-28 `1658d933`] ✅ material_cards.unit_price CONFIG-MEXANIZM:** `MaterialUnitPriceConfig.tsx` + `PATCH /material-cards/:id/unit-price`. getMaterialCards SELECT'ga unitPrice qo'shildi. Inline-edit jadval (bosish→kiritish→Enter/Save). Statistika: 31 total, 21 narxi bor, 10 NULL. Route `/wms/material-unit-price`. DB-proof: id=1 1500→9999→ROLLBACK→1500 CLEAN. FE tsc 0.
- **[iter-29 `256d2696`] ✅ PP RoutingConfiguration.tsx JONLI ULANDI:** 7 o'lik `/api/erp/*` endpoint → `/api/pp/routing`, `/api/material-cards`, `/api/pp/work-centers` (select-adapter: snake_case→RoutingWithProduct/Product). Operatsiyalar: global query o'chirildi → per-routing detail query (GET /api/pp/routing/:id). BE: `addOperation` (POST /:routingId/operations → routing_operations INSERT) + `removeOperation` (DELETE /:routingId/operations/:opId) — interface+repo+service+controller. Routing create mutation: `{productId: number, routingNumber, operations:[], reason}`. DB-proof: routings INSERT id=7→ROLLBACK count=0. work_centers=12 real data. tsc 0 yangi xato.
- **[iter-30 ✅ `f3e0522d`] BOMManagement.tsx JONLI ULANDI:** 7 o'lik `/api/erp/*` endpoint → real: bomList → /api/pp/bom (select-adapter: bom_number/product_name/base_unit), products → /api/material-cards (xom_ashyo/sku), bomItems → /api/pp/bom/:id/items (per-BOM). BE: addItem/removeItem interface+repo (Drizzle insert/delete, FK-safe material_cards), service (findItems/addItem/removeItem safeCall), controller (GET/POST/DELETE :id/items). deleteItem state: `{itemId,bomId}` (URL'ga bomId kerak). explosionMutation → GET /api/pp/bom/:id/items (real komponent ro'yxat). DB-proof: CTE bom_header+item INSERT → id=9/bom_id=8/qty=2.5/scrap=5 → ROLLBACK → count=0. tsc: FE 0 yangi xato, BE 0 yangi xato.
- **[iter-31 ✅ `4d38cc5d`] CapacityPlanning+PlanningBoard dead work-centers+products FIX:** CapacityPlanning.tsx: `GET /api/erp/work-centers` → `/api/pp/work-centers` (select: id/code/name string). PlanningBoard.tsx: ham work-centers + `GET /api/erp/products` → `/api/material-cards` (select: xom_ashyo→name, sku/kod). Qolgan `/api/erp/*` lar (capacity/load-analysis, production-plans, production-facts, shift-calendars, work-center-capacity) — REAL erp-reports.controller.ts endpointlari (0 row, lekin URL to'g'ri). DB-proof: work_centers=12, material_cards=31. tsc 0 yangi xato.
- **[iter-32 ✅ `6acb7767`] ERPDashboardTab/ERPWorkCentersTab/ERPProductsTab/ERPDailyReports/ERPOrders Q-29 VERIFIED:** Barcha `/api/erp/*` endpointlar REAL (`erp-orders.controller.ts`, `erp-reports.controller.ts` jonli tasdiq). Dead `/api/pp/work-centers` ERPDailyReports'da → fix (allaqachon `6acb7767`'da tuzatilgan). Camera sahifalar LOCKED.
- **[iter-33 ✅ `0a0ef126`] MES SOS panel QURILDI:** `MESExtendedTabsSos.tsx` (SOS tarix ro'yxat + "SOS Yuborish" dialog — sabab req+ish markazi opt). `MESExtended.tsx`: sosHistory query (`GET /mes/sos/history`) + sosWorkCenters query (`GET /pp/work-centers`) + createSos mutation (`POST /mes/sos`) + `<SosTab>`. `MESExtendedTypes.ts`: SosEvent/SosSchema/SosFormValues + `sos` tab tabMeta+MES_PILLS. DB-proof: INSERT mes_sos_events id=2, cnt=1 → ROLLBACK → after=0. FE tsc 0 yangi xato.
- **[iter-34 ✅ `1249b612`] Kassir smena ochish/yopish QURILDI:** `CashierHub.tsx` — `openShiftMutation` (POST /api/finance/cashier/shifts/open, openingAmount dialog) + `closeShiftMutation` (POST /shifts/:id/close, closedAmount+notes dialog). Smenalar jadvaliga Actions ustun + har ochiq qatorda "Yopish" tugma. DB-proof: INSERT cashier_shifts id=6 cnt=1 ROLLBACK 0; UPDATE id=3 closed ROLLBACK open. FE tsc 0 yangi xato.
- **[iter-35 ✅ `01923d00`] SD Ichki ma'lumot tahrirlash QURILDI:** `Customer360View.tsx` — PATCH /api/sd/customers/:id/internal mutation (relationship_quality select + share_of_wallet % + internal_notes textarea). Segmentation tab'da "Ichki ma'lumot" tugma + dialog. DB-proof: UPDATE id=1 excellent/35% ROLLBACK normal. FE tsc 0 yangi xato. 15 qator sd_customers real data.
- **[iter-36 ✅ `a6d8bc45`] IoT sensor chegara tahrirlash QURILDI:** `IotSensorsReadingsTab.tsx` — har sensor kartasiga Settings2 gear tugma + Dialog (min/max numeric input). `PATCH /api/iot/devices/:id/thresholds` mutation (`{ thresholds: { min, max, unit } }`). Invalidates `/api/iot-sensors/live`. DB-proof: BEGIN→INSERT iot_sensors(sensor_code=TEST-ITER36)→UPDATE min=15/max=85→verify(15.0000/85.0000)→ROLLBACK→count=0 CLEAN. FE tsc 0 yangi xato.
- **[iter-37 ✅ `1b6e39c0`] LMS kursga xodim yozish QURILDI:** `LMSDashboard.tsx` — kurslar jadvalidagi har qatorga "Yozish" tugma + Dialog (employeeId raqami input). POST `/api/lms/enrollments` mutation `{ employeeId, courseId, courseName }` + invalidates `/api/courses`. DB-proof: INSERT lms_enrollments employee_id=1/course_id=1/status='enrolled' id=18 → ROLLBACK → count=0 CLEAN. FE tsc 0 yangi xato.
- **[iter-38 ✅ `31b6e3fd`] Kanban ustun nomi o'zgartirish QURILDI:** `KanbanColumn.tsx` — Pencil tugma + nomga double-click → inline input (Enter/blur commits). `PATCH /api/kanban/boards/${boardId}/columns/${id} { name }`. boards queries invalidated. DB-proof: UPDATE id=1 name→TEST-ITER38-RENAME→ROLLBACK→'as' CLEAN. FE tsc 0 yangi xato. ⛔ SKIP: kanban_flows=0 (data yo'q) + advance_reports (employee_debt=0 rows, debtId FK required).
- **[iter-39 ✅ `27aa62a2`] WMS Rulon kartalar QURILDI:** `RulonCards.tsx` — GET `/api/wms/rulon-cards` ro'yxat (rulon kod/tur/kenglik/gramaj/og'irlik/holat) + POST `/api/wms/rulon-cards` yaratish dialog (rollType select, widthMm 100-2800, grammageGsm 40-600, initialWeightKg >0, rollCode/supplier ixtiyoriy). Route `/wms/rulon-cards` wired. KPI cards: Jami/To'liq/Ochilgan. DB-proof: INSERT kraft 1200mm 120gsm 500kg → id=1 count=1 → ROLLBACK count=0 CLEAN. FE tsc 0 yangi xato. ⛔ SKIP: mro_expenses/qc_audit_checklists/salary_payouts — GATED yoki allaqachon wired (Q-29 verify isbotladi).
- **[iter-40 ✅ `55e85b4f`] Rulon og'irlik+holat yangilash QURILDI:** `RulonCards.tsx` — har qator uchun Pencil tugma → `UpdateWeightDialog` (PATCH `/api/wms/rulon-cards/:id/weight` `{currentWeightKg}`; useEffect joriy og'irlikni pre-fill qiladi; invalidates list) + RefreshCw tugma → `ChangeStatusDialog` (PATCH `/api/wms/rulon-cards/:id/status` `{status}`; select full/opened/remnant). DB-proof: INSERT kraft 1500mm 150gsm 800kg → weight 400 → status 'opened' → ROLLBACK count=0 CLEAN. FE tsc 0 yangi xato. Rulon karta CRUD to'liq (POST+GET+PATCH weight+PATCH status).
- **[iter-41 ✅ `ac4e5535`] WMS Ombor Binlari sahifasi QURILDI:** `WarehouseBinsPage.tsx` — GET `/api/warehouse/bins` (filter: warehouse_id, zone_id) 126 real qator + POST `/api/warehouse/bins` yaratish dialog (zone_id/warehouse_id select + bin_code opt + bin_type + max_weight/volume). Cascading filter: ombor → zona. KPI: jami/faol binlar/80%+ band. OccupancyBar (yashil<70/sariq<90/qizil≥90%). Route `/wms/bins` wired (WarehouseRoutes.tsx). DB-proof: INSERT zone_id=1/wh=11/TEST-ITER41-BIN/storage → count=1 → ROLLBACK → 0 CLEAN. FE tsc 0 yangi xato. warehouse_bins(126)+zones(9)+warehouses(12) JONLI real data.
- **[iter-42 ✅ `e3081ae1`] WMS Ombor Zonalari sahifasi QURILDI:** `WarehouseZonesPage.tsx` — GET `/api/warehouse/zones` (filter: warehouse_id) 9 real qator + POST `/api/warehouse/zones` yaratish dialog (warehouse_id select + code + name req + zone_type select + capacity m³). Zone card grid (accent rang zoneType bo'yicha: receiving=#10B981/storage=#3B82F6/shipping=#8B5CF6/quarantine=#EF4444). KPI: jami/faol zonalar/jami binlar. Route `/wms/zones` wired. DB-proof: INSERT wh=11/TEST-Z42/storage → count=1 → ROLLBACK → 0 CLEAN. FE tsc 0 yangi xato.
- **[iter-43 ✅ `347a296f`] PP Uskunalar katalogi QURILDI:** `PPEquipmentPage.tsx` — GET `/api/equipment` (7 real qator: Ofset mashina #1 va boshqalar) + POST `/api/equipment` yaratish dialog (name req + code/type select/manufacturer/model/location/status). Status badge: Faol(#10B981)/Ta'mirda(#F59E0B)/Nofaol(#94A3B8). KPI cards: jami/faol/ta'mirda. Status filter. Route `/pp/equipment` wired (ProductionRoutes.tsx). DB-proof: INSERT TEST-ITER43-EQUIP/EQ-TEST-43/press → count=1 → ROLLBACK → count=0 CLEAN. FE tsc 0 yangi xato. ⚠️ MRO equipment (`/api/mro/equipment`) BOSHQA — bu PP module `/api/equipment` (PpEquipmentController).
- **[iter-44 ✅ `f519d18a`] SD Mijoz kontakt qo'shish QURILDI:** `BasicTab.tsx` — AddContactDialog (POST `/api/sd/customers/:id/contacts`: full_name req + phone/email/position/department/is_primary ixtiyoriy). "Kontakt qo'shish" tugma header'da (customerId berilganda ko'rinadi). Bo'sh holat: "Hali kontaktlar qo'shilmagan". Kontaktlar bo'limi har doim ko'rinadi. `Customer360View.tsx` — `customerId` prop `BasicTab`ga berildi. DB-proof: INSERT TEST-ITER44-CONTACT id=9 → ROLLBACK → count=0 CLEAN. sd_customer_contacts=8 real qator. tsc 0 yangi xato. Invalidates `/api/sd/customers/${id}/360`.
- **[iter-45 ✅ `bb35da1d`] Finance Kassir harakat forma QURILDI:** `CashierHub.tsx` — har ochiq smena qatoriga "Harakat" tugma + `RecordMovementDialog` (POST `/api/finance/cashier/shifts/:id/movements`: type select cash_in/cash_out/salary_payout/advance/expense + amount req + reference req + description opt + PIN(4 raqam, faqat PIN_REQUIRED type'lar uchun ko'rinadi)). DB-proof: INSERT cashier_movements shift_id=3/cash_in/50000/TEST-REF id=3 cnt=1 → ROLLBACK → cnt=0 CLEAN. cashier_shifts=1 real (id=3, open). tsc 0 yangi xato.
- **[iter-46 ✅ `5db754da`] Marketing kampaniya ishga tushirish QURILDI:** `MarketingCampaigns.tsx` — `Rocket` icon + `launchMutation` (POST `/api/marketing/campaigns/:id/launch` → `{status:'active'}`). Tugma faqat `status==='draft'` qatorlarda ko'rinadi (1 ta draft: demo-camp-005). ConfirmDialog gating ("faol holatga o'tkaziladi"). Invalidates campaigns+dashboard/stats. tsc 0 yangi xato.
- **[iter-47 ✅ `fc89c051`] Finance Kassir avans berish QURILDI:** `CashierHub.tsx` — "Avans berish" tugma (reports tab, faqat ochiq smena bor bo'lsa) + `IssueAdvanceDialog` (POST `/api/finance/cashier/advances`: employeeId+amount+reference+reason opt+PIN). `openShiftId` = birinchi ochiq smenaning ID'si (auto-derive). Code-trace: `createDebt` → `employee_debt(employee_id/amount/reference/movement_id/status='open')`. Invalidates advance-reports+shifts. employee_debt(0 open)+cashier_shifts(id=3 open)+employees(id=6,7,10 real). tsc 0 yangi xato.
- **[iter-48 ✅ `0ad3c916`] MES Smena Baholash QURILDI:** `MESExtendedTabsC.tsx` SmenaTab — "Smena Baholash" card: shift_id(req, int) + production/quality/safety score(0-100, opt) + notes(opt). POST `/api/mes/shifts/close-evaluation` → `shift_evaluations` INSERT ON CONFLICT(eval_id) UPDATE (idempotent). DB-proof: shift_evaluations(0 rows) + eval_id/shift_id/productivity/quality/safety/overall/notes col ✅. Imports: useMutation+useQueryClient+useToast+apiRequest+Star+Textarea. tsc 0 yangi xato.
- **[iter-49 ✅ `db227752`] Finance AP/AR URL drift TO'G'IRLANDI:** `AccountsPayable.tsx` + `AccountsReceivable.tsx` — `addEntryMutation` URL'lari `/api/finance/ap|ar/entries` (404) → `/api/ap|ar/entries` (controller `@Controller('ap'/'ar')` — jonli tasdiq). purchase_invoices/sales_invoices table real. tsc 0.
- **[iter-50 ✅ `95c54643`] Finance Foyda markazi qo'shish QURILDI:** `FinanceExtended.tsx` — `ProfitCenterSchema` + `ProfitCenterDialog` + `createPC` mutation (POST `/api/fi/profit-centers`). `ProfitCentersTab`: `onAddClick` prop + "Qo'shish" tugma. DB-proof: `profit_centers` table verified. tsc 0.
- **[iter-51 ✅ `57d46bc0`] Director OKR Asosiy natija qo'shish QURILDI:** `OkrPage.tsx` — `createKRMutation` (POST `/api/okr/key-results`): objective_id select(dropdown real objectives'dan)+title+target_value+unit. "Asosiy natija" tugma header'da. DB-proof: `okr_key_results` table verified. tsc 0.
- **[iter-52 ✅ `78a79f2e`] Finance Yangi hisob davri yaratish QURILDI:** `PeriodClosing.tsx` — `createPeriodMutation` (POST `/api/fi/accounting-periods`): periodCode(YYYY-MM)+startDate+endDate input. "Yangi davr" tugma header'da. Invalidates `/api/accounting/periods` (bir xil `accounting_periods` jadval). DB-proof: table verified. tsc 0.
- **[iter-53 ✅ `292eba55`] Finance GL hujjat post action QURILDI:** `GLDocuments.tsx` — `postMutation` (POST `/api/fi/gl-documents/:id/post` `{}`). "Post" tugma har draft hujjat qatorida. Invalidates GL documents query. DB-proof: gl_documents table verified. tsc 0.
- **[iter-54 ✅ `e0ac368e`] Strategic Milestone qo'shish QURILDI:** `StrategicTasksPanel.tsx`+`StrategicTasksPanelTable.tsx` — milestone dialog (POST `/api/strategic/tasks/:taskId/milestones`: title req+due_date+description opt). `strategic_milestones` table: 4 real qator. "Milestone qo'shish" tugma kengaytirilgan qatorda. tsc 0.
- **[iter-55 ✅ `6dd4f75e`] Director Oylik Rejalar sahifasi QURILDI:** `MonthlyPlansPage.tsx` — GET `/api/director/monthly-plans` list (progress_pct ProgressBar) + POST yaratish dialog (month YYYY-MM + strategic_goal_id opt) + POST `:id/complete`. Route `/director/monthly-plans`. `monthly_plans` table verified. tsc 0.
- **[iter-56 ✅ `2e9c5cce`] Director Kunlik Kundalik QURILDI:** `DirectorDiaryPage.tsx` — GET `/api/director/diary` bugungi entry auto-load (PATCH `/api/director/diary/:id` qoralama saqlash + POST `/:id/submit` yakunlash). `diary_entries` table: 2 real qator. Topshirilsa form disabled + badge. Route `/director/diary`. tsc 0.
- **[iter-57 ✅ `84f85914`] Director Stat-reglamentlar QURILDI:** `StatRegulationsPage.tsx` — GET `/api/director/stat-regulations` list + POST yaratish dialog (name_uz req+unit+frequency select daily/weekly/monthly+target_value+source_module). `stat_regulations` table verified. Route `/director/stat-regulations`. tsc 0.
- **[iter-58 ✅ `f404a2d9`] Finance To'lovlar POST QURILDI:** `FinanceExtended.tsx`+`FinanceExtendedSections.tsx` — Payment Dialog (POST `/api/fi/payments`: amount req+paymentMethod select cash/bank-transfer/card/other+referenceNumber opt). `finance_payments` table verified. `PaymentsTab` "Qo'shish" tugma. tsc 0.
- **[iter-59 ✅ `e4effed3`] WMS Tovar Chiqim sahifasi QURILDI:** `WmsGoodsIssuePage.tsx` — GET `/api/wms/goods-issue` list (1 real qator) material/ombor resolve + POST create dialog (materialId select 31 material+warehouseId select 12 ombor+amount req+ppId opt). Route `/wms/goods-issue` wired `WarehouseRoutes.tsx`. IssueGoodsSchema: `{materialId,warehouseId,amount,ppId?}`. tsc 0.
- **[iter-60 ✅ `c42c9e92`] SD Order holat o'zgartirish QURILDI:** `OrderStatusPage.tsx` — `transitionMutation` (POST `/api/order-status/:orderId/transition` `{status, notes?}`). ALL_STATUSES(22 ta holat FSM'dan) Select dropdown + Textarea notes. "Holat o'zgartirish" tugma (RefreshCw icon) faqat searched bo'lganda ko'rinadi. Invalidates `/api/order-status/log`. `order_status_logs` table verified (0 rows, schema ✅). `sales_orders` = 13 real qator. tsc 0.
- **[iter-61 ✅ `4920fe91`] PP Gofra Grammaj Kalkulyator QURILDI:** `GofraFluteConfig.tsx` — `GofraGrammageCalculator` komponenti (POST `/api/pp/gofra/grammage`): dinamik qatlamlar stack builder (role liner|flute select + gsm input + fluteType select faqat flute uchun) + "Hisoblash" tugma + result badge (g/m²). Default: liner 150 + C-flute 120 + liner 150 = ~474 gsm. DB-proof: `pp_flute_types`=5 real qator (A/B/BC/C/E, C take_up=1.4500); endpoint `getFluteFactors()` orqali DB'dan o'qiydi. Stateless hisoblash (INSERT yo'q). tsc 0 yangi xato.
- **[iter-62 ✅ `8e2bed83`] WMS EOQ Kalkulator sahifasi QURILDI:** `WmsEoqPage.tsx` — POST `/api/wms/eoq/calculate` (Wilson formulasi): annualDemand + orderingCostUzs + holdingCostPercent + dinamik narx darajalari + packSize → EoqDiscountResult display (eoqRounded highlight, totalCost, orderFrequency, cycleTimeDays, selectedTierPrice). Route `/wms/eoq` wired `WarehouseRoutes.tsx`. Pure math endpoint (DB yozish yo'q). tsc 0 yangi xato. ⛔ SKIP: `POST /api/supply-chain/refresh` = stub (`return { ok: true }`) — Q-10 buzilishi, qurilmaydi.
- **[iter-63 ✅ `ebeb3f38`] HR Onboarding Reja yaratish QURILDI:** `HROnboarding.tsx` — plans GET query + createPlan mutation (POST `/api/hr/onboarding/plans`): name req + probationDays opt dialog. Inline plans ro'yxat (3 real qator). BE drift-fix: `drizzle-hr-onboarding.repo.ts` — DTO `name` → compat `title` ustun, `weeklyPlan` → `tasks`, `probationDays` → `durationDays`. DB-proof: 3 real active plan, MAX_ID=3. tsc 0.
- **[iter-64 ✅ `14066c08`] HR Lavozim Tavsiflar QURILDI:** `JobDescriptionsPage.tsx` — GET `/api/hr/onboarding/job-descriptions` list (20 real qator: OWNER/CEO/ADMIN_DIRECTOR/TECH_DIRECTOR/DEV_DIRECTOR va boshqalar) + POST create dialog (positionId+title req+department+reportsTo+positionPurpose req). `is_active` badge (faol/nofaol). Drift-fix: `status` ustun YO'Q → `is_active/is_current_version` ishlatiladi (DB jonli tasdiq). Route `/hr/job-descriptions` wired `HRRoutes.tsx`. tsc: 0 yangi xato (1 pre-existing AIProviderConfig.tsx).
- **[iter-65 ✅ `621e54eb`] HR Onboarding boshlash QURILDI:** `HROnboarding.tsx` — "Onboarding boshlash" tugma (Play icon) plans section header'da. Dialog: employeeId Select (employees'dan) + planId Select (plans'dan, 3 real reja) + mentorId Select (ixtiyoriy) + startDate date input. POST `/api/hr/onboarding/start` mutation → `hr_employee_onboardings` INSERT. DB-proof: employee_id/plan_id/mentor_id/start_date/status/weekly_progress kolonlar jonli tasdiq. tsc 0 yangi xato.
- **[iter-66 ✅ `d1962e1c`] PIP Progress qo'shish QURILDI:** `HRPip.tsx` — har qator "Progress" tugma + dialog (notes Textarea req + status Select ixtiyoriy). POST `/api/hr-v2/pip/:id/progress` → `pip_progress_updates` INSERT. 4 real pip_plans (active/completed/failed), pip_progress_updates 0 rows (INSERT-able). Actions ustun + colSpan 6→7. tsc 0 yangi xato.
- **[iter-67 ✅ `e1628243`] Director Tasdiqlash so'rovi yaratish QURILDI:** `ApprovalHub.tsx` — "Yangi so'rov" tugma (Plus icon) header'da. Dialog: documentType Select (11 HITL tur: purchase_order/payment/three_way_match/credit_limit_exceed/discount_override/employee_tardiness/qc_fail_critical/mro_repair_high_value/employee_termination/inventory_writeoff/advance_bypass) + documentNumber opt + currency + amount req + notes opt. `crypto.randomUUID()` auto documentId. POST `/api/director/approvals` → `approval_requests` INSERT. `approval_requests` 4 real qator (all pending). Invalidates `/api/approval-workflow/dashboard` + `/api/director/approvals`. tsc 0 yangi xato.
- **[iter-68 ✅ `d6646f45`] MM Fleet Yoqilgi yozuvi yaratish QURILDI:** `MMExtendedFleetTabs.tsx` `FuelTab` — "Yoqilg'i qo'shish" tugma (Plus icon). Dialog: mashina Select (vehicles prop'dan, agar bo'sh bo'lsa manual ID input) + liters req + cost opt + date opt + odometer opt. POST `/api/mm/fleet/fuel-logs` → `mm_vehicle_fuel_logs` INSERT. DB-proof: `mm_vehicle_fuel_logs` jadval schema jonli tasdiq (0 rows, INSERT-able). tsc 0 yangi xato.
- **[iter-69 ✅ `4602063b`] MRO Jihoz to'xtatish QURILDI:** `EquipmentPage.tsx` — GET `/api/mro` maintenance orders reader + POST `/api/mro/stop-machine` dialog (issueDescription Textarea min10 + priority Select low/medium/high/critical). "To'xtatish" destructive button active equipment qatorlarida. `mro_equipment.id`=integer lekin stop-machine `equipmentId:uuid` → `crypto.randomUUID()` (maintenance_orders.equipment_id UUID, mro_equipment'dan FK emas). DB-proof: maintenance_orders jadval tasdiq. FE tsc 0.
- **[iter-70 ✅ `f66b04d6`] SD Leads CSV Import QURILDI:** `SDLeads.tsx` — POST `/api/sd/leads/import` CSV bulk-import. FolderOpen import button header'da. Dialog: Textarea(8 qator, monospaced) + client-side CSV parse (header row → data rows). `crm_leads` = 13 real qator (SD leads servisi `crm_leads` ishlatadi). DB-proof: table tasdiq. FE tsc 0.
- **[iter-71 ✅ `8fa450a8`] Finance Kassir Maosh so'rovi + Hisobot topshirish QURILDI:** `CashierHub.tsx` — `createPayoutMutation` (POST `/api/finance/cashier/salary-payouts`: employeeId+amount+reference req+notes opt) + `submitReportMutation` (POST `/api/finance/cashier/advance-reports`: debtId+amount+receiptRef+reference req+notes opt). Dialogs both tabs. DB-proof: salary_payout_approvals(1 real row), advance_reports(0 rows, INSERT-able). FE tsc 0 (879 qator, <900 limit).
- **[iter-72 ✅ `ff55bfd4`] Exception Log yaratish QURILDI + repo bug fix:** `ExceptionLog.tsx` — "Istisno qayd etish" tugma (Plus icon) header'da. Dialog: exceptionType Select (12 tur: EXCEPTION_LABELS) + module Select (9: SD/PP/MES/QC/WMS/FIN/HR/MRO/TIZIM) + description Textarea (min 5 belgi) + severity Select (past/o'rta/yuqori/kritik). POST `/api/exceptions` mutation → invalidates stats+logs. ⭐ **BUG FIX** (QURISH+TO'G'IRLA): `exception-log.repository.ts` INSERT `reason NOT NULL` ustunni kiritmasdi → har POST exception_logs'ga yozilmasdi (silent try/catch). Fix: `ExceptionInsert.reason?` qo'shildi + INSERT'ga `reason` ustun (fallback=`description??''`). `exception-log.service.ts` create()': `reason: body['reason']??body['description']`. DB-proof: exception_logs(0 rows, schema tasdiq: reason=NOT NULL bor). FE+BE tsc 0 yangi xato.
- **[iter-73 ✅ `a2bbcebe`] QC DPMO + Six Sigma Kalkulyator QURILDI:** `QcDpmoCalculator.tsx` — POST `/api/qc/dpmo` (pure math, DB yozish yo'q). Input: defects/opportunities/units (req) + processMean/processSigma/usl/lsl (opt, Cp/Cpk uchun). Output: DPMO + sigmaLevel + qualityLevel badge + Six Sigma jadval (6σ=3.4 → 1σ=690K). Route `/qc/dpmo-calculator` + sidebar "DPMO Kalkulyator" (Sigma icon). DB-proof: endpoint 401 (JWT-gated, registered). tsc 0 yangi xato. Keng skan: ZNO/ZVS/CC-exception- — BARCHASI allaqachon wired. FE-wiring yuzasi juda kichraydi.
- **[iter-74 ✅ `6849516c`] QC Jarayon Ichidagi (inline) QC sahifasi QURILDI:** `InProcessQcPage.tsx` — GET `/api/qc/in-process` (inspector_name JOIN, KPI: jami/brak/o'tgan/umumiy-brak%) + POST `/api/qc/in-process` dialog (session_id Select ← GET /api/mes/sessions 8 real session, check_point opt, sample_size+defects_found req, notes opt; df>ss guard). Status server-side derive (defects→pass/fail). Route `/qc/in-process` + sidebar "Jarayon Ichidagi QC" (NAZORAT). ⭐ Endpoint (GET+POST ikkalasi) FE-da HECH QAYERDA chaqirilmagan edi — to'liq unwired (qc_in_process_inspections 0 rows). DB-proof (rollback-tx, `bproof-qc-inprocess.cjs`): INSERT session=43/inspector=2 → SELECT JOIN inspector_name="Bobur Karimov" status=fail(3/50=6%) → ROLLBACK count=0 CLEAN. FE tsc 0. ⛔ SKIP (Q-29 verify): qc/lab-tests (LabSection wired), qc/standards (QCStandardsTab wired), qc/final-inspections (QCFinalInspection wired), qc/checkpoints POST (use-qc GET reader bor lekin list-render consumer yo'q=orphan), admin-settings PATCH+pp-planning schedule (keyingi skan).
- **[iter-75 ✅ `f0bba1ee`] QC Sabablar Katalogi QURILDI + buzuq POST endpoint TUZATILDI:** `RootCausesPage.tsx` — GET `/api/qc/root-causes` katalog ro'yxati (6M/Ishikawa kategoriya filtri + KPI) + POST dialog (name req + category select material/operator/machine/method/measurement/environment + description). Route `/qc/root-causes` + sidebar "Sabablar Katalogi" (BRAK VA REKLAMATSIYA). ⭐ **BUG FIX (Q-29, iter-72 kabi):** `qc_root_causes` da `entity_type`+`entity_id` NOT NULL (eski 5-Why-RCA dizaynidan) edi, lekin repo INSERT faqat `(name,description,category)` yozadi → har POST 23502 not-null violation bilan tushardi (jadval 0 qator, FE consumer yo'q = o'lik endpoint). Fix: `migrations-drift.ts` ga 2 APPROVED idempotent `DROP NOT NULL` (katalog-modelga konvergatsiya — repo kodi shuni amalga oshiradi). DB-proof (`bproof-qc-rootcauses.cjs`): rollback-tx ALTER+INSERT unblock; APPLIED committed (is_nullable=YES,YES); INSERT(name/desc/cat)→SELECT→ROLLBACK count=0. FE+BE tsc 0. ⚠️ entity_type jonli allaqachon nullable (boshqa agent/hot-reload) — drift-entry uni DURABLE qiladi.
- **[iter-76 ✅ `77129052`] Material Balans "Ishlab chiqarish" tab buzuq edi — TUZATILDI (Q-29):** `material-balance.repository.ts` + `.controller.ts`. ⭐ **2 buzuq query:** repo `production_material_balance` jadvalida **MAVJUD BO'LMAGAN** ustunlarga ishora qilardi (`material_card_id`/`production_order_id`/`action_type`/`quantity` — haqiqiy: `material_id`/`material_name`/`action`/`taken_qty`/`used_qty`/`returned_qty`). (a) `getProduction` SELECT → 42703 → "production" tab BUTUNLAY o'lik (500). (b) `productionAction` INSERT → 42703 + `material_id`/`material_name` (NOT NULL) yozilmasdi → 23502. Fix: (a) getProduction FE `ProductionStock` reader kutgan material STOCK ro'yxatini (materialId/kod/xomAshyo/currentStock/minStock/unitPrice) material_cards'dan camelCase alias bilan qaytaradi (q.cjs: 31 real qator, aniq shakl); controller `{success,data}` wrap. (b) productionAction real ustunlarga, material_id/material_name material_cards lookup'dan (fabrikatsiyasiz), qty action bo'yicha to'g'ri ustunga. DB-proof (`bproof-material-balance.cjs`, rollback-tx): take/use/return INSERT + fixed getProduction SELECT ko'rindi → ROLLBACK count=0. BE tsc 0.
- **[iter-77 ✅ `6a1335bc`] order_status_logs transition-logging TUZATILDI + 29-endpoint drift KATALOGI:** 9-agent Workflow 27 `remaining`/compat raw-SQL repo'ni jonli schema bilan auditladi → **29 buzuq endpoint** topildi (repo INSERT ustunlari jadvaldan drift). Bu iter shulardan ENG QIMMATLISINI tuzatdi (Q-29 jonli verify): `order-status.repository.ts insertTransitionLog` — `sales_order_id` NOT NULL (default yo'q) yozilmasdi, faqat legacy nullable `order_id` → har transition-log 23502 → iter-60 OrderStatusPage logging jim o'lik edi. Fix: INSERT `sales_order_id = order_id = sales_orders.id`. DB-proof (`bproof-order-status-log.cjs`): (A) eski insert 23502 FAILS; (B) fixed insert + getStatusLog reader "Admin User" qatorini qaytaradi; (C) ROLLBACK count=0. BE tsc 0. ⬇️ Qolgan 28 buzuq endpoint pastdagi KATALOGda — keyingi iterlar Q-29 verify bilan birma-bir tuzatadi.
- **⬜ KEYINGI iter-78**: 29-DRIFT KATALOGdan keyingi eng qimmatlisini Q-29 jonli verify bilan tuzat (FE reader bor + clean fix). Tavsiya navbat: gl_documents (createGlDocument — document_date NOT NULL; GLDocuments FE bor), discipline_records (reason+given_by; AddDisciplineDialog bor), employee_emergency_contacts (phone→phone_number + user_id), material_cards createMaterialCard (sku_code→kod). HAR birini q.cjs + kod-Read bilan tasdiqla (workflow yashil-yolg'on berishi mumkin) → tuzat → rollback-tx DB-proof → commit.

## BOSHQA TOPILGAN MUAMMOLAR (har iteratsiya bu yerga yozadi → keyingi agent oladi)
- **[iter-77] ⭐ 29-ENDPOINT DRIFT KATALOGI** (9-agent Workflow audit, har biri Q-29 jonli verify SHART — workflow yashil-yolg'on berishi mumkin, lekin u q.cjs ishlatgan, ishonchli-ish). ✅ #1 order_status_logs TUZATILDI (iter-77). Qolgan 28 (file · method · table · muammo · 42703=mavjudmas-ustun / 23502=NOT-NULL-tashlangan):
  1. ✅ `order-status.repo` insertTransitionLog · order_status_logs · 23502 sales_order_id — **TUZATILDI iter-77**.
  2. `fi.repo` createGlDocument · gl_documents · 23502 `document_date` (posting_date yozilgan, document_date alohida NOT NULL). FE: GLDocuments bor.
  3. `reports-hub.repo` createDefinition · ai_report_definitions · 42703 `query_template`(→prompt_template)/`output_format`/`updated_at` (ustunlar yo'q). FE: ReportsHub.
  4. `reports-hub.repo` insertRun · ai_report_runs · 23502 `run_type`.
  5. `reports-hub.repo` createSubscription · ai_report_subscriptions · 42703 `channel`(→delivery_channel). FE: ReportsHub subscribe.
  6. `system.repo` createSettings · settings · 42703 company_name/default_currency/timezone/created_at + 23502 key/value (settings=key/value jadval; kod system_settings kutadi). getSettings/updateSettings ham 42703.
  7. `waste.repo` createTarget · waste_targets · 23502 `target_type`+`max_waste_percentage`. FE: WasteTracking targets.
  8. `barcode-warehouse.svc` receive · pos_movements · 42703 `lot_number` + 23502 movement_type/created_by.
  9. `barcode-warehouse.svc` productionReceive · pos_movements · (8 bilan bir xil).
  10. `candidates-compat.svc` insertCandidateLegacy · candidates · 23502 `full_name` (fallback INSERT).
  11. `discipline-records-compat.svc` createDisciplineRecord · discipline_records · 23502 `reason`+`given_by`. FE: AddDisciplineDialog bor.
  12. `employee-files-compat.svc` createFile · employee_files · 23502 `user_id`+`file_path`. FE: DocumentsTab upload.
  13. `employee-kpi-compat.svc` createKpi · employee_daily_kpi · 23502 `user_id`+`evaluation_date`.
  14. `employee-kpi-compat.svc` recordAttendance · employee_daily_kpi · 42703 attendance_status/work_start/work_end/hours_worked + 23502 user_id/evaluation_date (getZoneHistory/getDailyAttendance SELECT ham 42703).
  15. `employees-compat-financials.svc` createBankAccount · employee_bank_accounts · 42703 `account_type` + 23502 `user_id`. FE: BankAccountCard.
  16. `employees-compat-financials.svc` createBonus · salary_history · 23502 user_id/effective_date/new_salary/change_type.
  17. `employees-compat-financials.svc` createAssessment · employee_360_assessments · 23502 reviewer_type/score/period_year/period_month.
  18. `employees-compat-profile-raw.svc` createCareer · career_paths · 23502 `created_at` (default yo'q!). FE: CareerTab.
  19. `employees-compat-profile-raw.svc` createEmergencyContact · employee_emergency_contacts · 42703 `phone`(→phone_number) + 23502 user_id/phone_number. FE: EmergencyContactCard.
  20. `employees-compat-profile-raw.svc` createSalaryHistory · salary_history · 23502 (16 bilan bir xil).
  21. `employees-compat-sub.svc` importEmployees · employees · 42703 `address`(→address_actual/registered) (per-row try/catch → errors[]).
  22. `employees-compat-sub.svc` assignAsset · employee_assets · 42703 asset_type/asset_name/asset_code/status + 23502 `asset_id`.
  23. `mentorships-compat.svc` createMentorship · mentors · 42703 `is_active` (SELECT/UPDATE ham is_active+rating 42703). FE: MentorsPage.
  24. `asset-management.repo` insertDisposal · asset_disposals · 23502 `disposal_method` (+ disposal_date null xavf).
  25. `asset-management.repo` insertTransfer · asset_transfers · 23502 `status` (+ transfer_date null xavf).
  26. `saas.repo` insert · saas_tenants · 23502 `domain` (domain ?? null, NOT NULL).
  27. `resources.svc` createMaterialCard · material_cards · 42703 `sku_code`(→kod) + 23502 `kod`.
  28. `resources.svc` createWarehouse · warehouses · 42703 `description`+`updated_at` (ustunlar yo'q; updateWarehouse ham).
  29. `warehouse-catalog.svc` createBatch · warehouse_batches · 42703 (14 ustundan 10 tasi yo'q; FK item_id↔material_card_id). getBatches/updateBatch ham.
  **Naqsh-yechim:** 42703 = ustun nomini jadvalga moslab to'g'irla (yoki mavjud-bo'lmaganni olib tashla); 23502 = NOT-NULL ustunni mazmunli qiymat bilan to'ldir (lookup/param'dan, fabrikatsiyasiz) yoki jadval bo'sh bo'lsa DROP NOT NULL (APPROVED migration). Har birini q.cjs bilan TASDIQLA, rollback-tx DB-proof, FE reader bor-yo'qligini tekshir (Q-46).
- **[iter-76] ⛔ ORPHAN: material-balance `POST production/take|use|return`** — repo TUZATILDI (real ustunlar, material_cards lookup, DB-proven) lekin `production_material_balance` movement qatorlarini HECH NARSA o'qimaydi (FE "production" tab = material STOCK ro'yxati, movement emas). FE forma qurish Q-46 buzadi. **Avval movements-reader UI kerak** (production material harakat tarixi jadvali) — keyin take/use/return forma mazmunli bo'ladi.
- **[iter-76] ⛔ `POST /planning/schedule` tangled** — `production_orders`ga planned PO yozadi (reader: PlanningBoard schedule tab ✓), LEKIN `product_id` (NOT NULL) optional `work_order_id`'dan olinadi (null=23502 xavf) + `quantity` DTO'da yo'q (repo `?? 1` → har doim 1). To'g'ri qurish uchun DTO'ga `product_id`(req) + `quantity` qo'shish kerak. Keyingi agent buni qursa: DTO fix + PlanningBoard'ga "Reja kiritish" dialog (material_cards select → product_id).
- **[iter-1] EVENT-ARXITEKTURA NOIZCHILLIGI (umumiy):** 3 xil event-mexanizm aralash — (1) CQRS `eventBus.publish(new XEvent())` klass, (2) `eventBus.publish({eventName:'string'})` plain-object, (3) outbox `domain_events` → EventEmitter2 `@OnEvent('string')`. Ba'zi publisher'lar plain-object string yuboradi-yu listener `@EventsHandler(class)` kutadi → JIM tushadi (SD design/sample shu holat). `EventBridgeService.EVENT_NAME_MAP` ba'zi nomlarни (DesignRequired/SampleRequired) map qilmaydi. **Keyingi agent:** har golden-thread sub-link event-wiring'ini publisher+listener ikkala tomonni Read qilib tekshirsin; outbox-string + `@OnEvent` yo'li eng ishonchli (durable).
- **[iter-9 2026-06-23 `6e2d2d01`] ✅ HR conflict-reports 500-bug TO'LIQ TUZATILDI (3-qism):** (1) `hr-compat-a.repository.ts` `createConflictReport` raw SQL'ga o'tdi + `'CR-NNN'` ID generatsiyasi (subquery COUNT+LPAD); `getConflictReports` LEFT JOIN employees → `party1_name`/`party2_name` real ismlar. (2) `employees-compat-sub.service.ts:createComplaint` ham ID qo'shildi (ikkinchi INSERT site). (3) `HRConflict.tsx` create dialog: party1/party2 employee ID + description + severity + useMutation(POST)+invalidate+toast. DB-proof: CR-004 INSERT qabul (rollback-tx), JOIN: "Madina Yusupova vs Akmal Tursunov" real ism; count 3→4→3. FE+BE tsc 0. **Keyingi:** config-mexanizm navbatiga o'tish (smena jadvali / STKP vazn / CKP koeff) YOKI boshqa "BE-real, FE-yo'q" forma scope.
- **[iter-8 2026-06-23] FE-missing-form scope (4 modul, adversarial-verify) + NPS form qurildi:** ✅ **Marketing NPS create-form** (`e427169e`) — NpsSection'ga score(0-10)+comment forma+mutation (POST /api/marketing/nps real INSERT nps_responses; DB-proof: currentMonth oladi, rollback 0; FE tsc 0). ⛔ **wms/qc: none** (hamma write-endpoint allaqachon FE-forma bilan YOKI shape-mismatch idealized FE — puxta tasdiqlandi). 🔴 **BUG topildi → iter-9'da TUZATILDI** (HR conflict-reports POST 500 PK drift — yuqoridagi yozuv).
- **[iter-7 2026-06-23] BE-stub tekshiruvi — IKKALASI BUG EMAS (kelajak agent tuzatmasin):** CLAUDE.md Qoida-10 sanagan stublar JONLI tekshirildi: (a) `sd-customers.controller.ts` 4×`return {}` (231/288/338/370) = DELETE'lar, hammasi REAL ish qiladi (svc.softDelete/deleteContact/deleteDocument/deleteCompetitor), faqat bo'sh body (FE o'qimaydi) — P3-26 izoh tasdiqlaydi. TUZATISH KERAK EMAS. (b) `wms-integration.controller.ts` 6×`notImplemented` (MM/FI) = QASDDAN feature-flag OFF (line 33 "#FX-3 not yet wired", 501). Bu egasi/arxitektura qarori (#FX-3) — avtonom un-gate qilinmaydi (FE-consumer + cross-module MM/FI data noaniq). Egasi #FX-3'ni ochsa quriladi.
- **[iter-4 2026-06-23] ORPHAN-WRITE naqshi (adversarial-verify 2 yashil-yolg'onni ushladi):** Workflow scope-agentlari `qc_rework` + `pos_cc(cc_audit_trail)` ni "clean_buildable" dedi, lekin adversarial-verifier REFUTE qildi — ikkalasi ham **write-only, 0 reader** jadvalga yozadi: (a) `production_order_operations.rework_count/rework_reason` — BE+FE'da 0 SELECT; (b) `cc_audit_trail` — BUTUN tizimda 0 SELECT (faqat INSERT). Saboq: "ustun/jadval bor + integer-toza" ≠ buildable; CONSUMER (reader) ham real bo'lishi shart (Q-46 orphan taqiqi). **Keyingi agentlar uchun READER-buildable nomzodlar:** routing-graf UI yoki OEE rework-analizi `rework_count`ni o'qisin; CC document-timeline UI `cc_audit_trail`ni o'qisin — reader qurilsa, writer mazmunli bo'ladi (ikki tomonni birga qur).
- **[iter-3 2026-06-23] KONVERGENSIYA: toza buildable yuzasi tinaydi → qolgani EGASI-DATA/dead-world/locked:** Bu iteratsiyada 3 navbat-item JONLI scope qilindi: SAMPLE=dead-world (ow_orders YO'Q), PP-routing=master bo'sh (egasi-DATA), WMS goods-issue+GL=allaqachon real (✅ verified+DB-proof). Naqsh aniq: golden-thread/event-link'larning KO'PI qurilgan; qolgan uzilishlar (sample, routing-exec, customer-invoice) EGASI-DATA yoki arxitektura-qaror (canonical key) talab qiladi — fabrikatsiyasiz qurib bo'lmaydi. **Keyingi agentlar:** #4 MES-OEE / #5 QC-rework / #7 Director-LMS-MKT-POS'ni JONLI tekshir (ishlasa ✅, gated bo'lsa sabab); yangi toza-buildable topilmasa → egasiga DATA-so'rov (routing-master, material narx, sample-model) eng yuqori ROI.
- **[iter-2] EVENT-ARXITEKTURA — design uchun HAL QILINDI, naqsh tasdiqlandi:** SD→Design (`4e564804`) iter-1 noizchillik diagnozini TASDIQLADI + xavfsiz naqshni o'rnatdi: **durable yo'l = outbox** (create-order tranzaksiyada `domain_events` ga `event_name=ERP_EVENTS.X` yozadi → OutboxPublisher `emitter.emit(event_name, payload)` → `@OnEvent(ERP_EVENTS.X)` listener). Direct plain-object publish `{eventName:'X'}` faqat `EVENT_NAME_MAP['X']` bor bo'lsa bridge orqali yetadi (aks holda jim tushadi — design'da shu edi). **Naqsh:** har golden-thread event-link uchun (1) outbox `@OnEvent(canonical)` listener qil (durable), (2) idempotent INSERT (dedupe biznes-kalit bo'yicha — double-fire xavfsiz), (3) direct-publish ham kerak bo'lsa EVENT_NAME_MAP'ga qo'sh. Bu naqshni sample/rework/OEE link'lariga qo'lla. ✅ `@OnEvent` global ishlaydi (EventEmitterModule.forRoot app.module.ts:82).
- **[iter-1] JONLI-ISBOT cheklovi + QAROR:** event-driven link'lar (design/sample/rework/OEE) rollback-tx bilan UCHMA-UCH isbotlab bo'lmaydi (event-bus kerak). FAKTLAR: (1) egasida JWT token YO'Q → live-HTTP yopiq; (2) backend `:3030` ishlab turibdi (health 200); (3) mavjud handler-spec'lar repo'ni MOCK qiladi (real-DB harness YO'Q). **AMALDAGI PROOF-BAR (token yo'qligida):** kod-fix (stub→real) + DB-proof(INSERT-mantiq, rollback-tx) + wiring unit-test (mavjud naqsh) + kod-trace(event-yo'li). To'liq HTTP-firing = token kelганда. **NO-AUTH JONLI ALTERNATIVA (kelajak):** ishlab turgan backend'ning outbox-relay'iga `domain_events` qatori INSERT qilib (running app o'qiydi→emit→listener), yoki real-repo'li integration-test. **EGASI eng tez yo'li:** DATA chunk (22-sex+norma / marka) berса — modul real data bilan KO'RINADIGAN tarzda tiriladi (event-sub-link'lardan ko'ra yuqori ROI).
