# SECTION 3 — 22 VIZYON MOSLIK (jonli kod + DB tasdiqlangan)

> Tahlilchi roli (QAT'IY read-only). Sana: 2026-06-06 (qayta tasdiqlangan).
> Har bir baho jonli kod (fayl:satr / endpoint / FE sahifa) yoki DB SELECT natijasi bilan tasdiqlangan.
> DB read-only helper: `node _audit/q.cjs "SELECT ..."` (BEGIN READ ONLY).
> Tasnif: REAL (service/repo + DB yozadi/o'qiydi) · 501-stub · YASHIL-YOLG'ON (200 qaytaradi, DB ga yozmaydi) · DUBLIKAT.

> ⚠️ **VERIFY-DON'T-TRUST tuzatish:** bu faylning oldingi versiyasi (vizyon 7) "Qatlam formula HECH QAYERDA YO'Q" degandi — **SOXTA-NEGATIV**. Jonli kodda formula TOPILDI va wired: `apps/api/src/modules/mm/application/layer-formula.service.ts` + endpoint `GET /api/mm/materials/:id/sheet-conversion`. Quyida tuzatildi.

---

## Asosiy xulosa (oldindan)

Texnik jihatdan tizim **juda keng** qurilgan: deyarli har vizyon uchun jadval + service/repo + controller + FE sahifa **mavjud va REAL** (kod sifatida). Lekin:

1. **Jonli data deyarli YO'Q** — eng kritik biznes jadvallari BO'SH (jonli SELECT bilan tasdiqlangan): `work_centers`=0, `pp_routing_operations`=0, `routing_operations`=0, `material_layer_config`=0, `cash_registers`/`cash_sessions`/`cash_transactions`=0, `entries`(GL)=0, `pos_gl_posting_log`=0, `sd_order_departments`=0, barcha `ow_*`=0 (`ow_molds`/`ow_tech_cards`=0), `mes_telemetry`=0, `material_consumption`=0, `payroll`/`payroll_calculations`/`payroll_advances`=0, `blog_posts`/`leads`/`campaigns`/`marketing_budget_items`=0.
2. **Vizyon yadrosi (formula, oqim) qurilgan, lekin data yo'q** — ⭐ Qatlam formula (kg→list) REAL kod, lekin `material_cards` da `format_a`=0/21, `material_kind`=0/21, `grammage`=5/21 → formula ishlay olmaydi (kirish data yo'q). Oltin ip end-to-end faqat test'da, jonli ishlatilmagan.
3. **Modullar "orol"** — kod ulangan, lekin haqiqiy biznes oqimi (buyurtma→ishlab chiqarish→pul) bir marta ham to'liq aylanmagan; `pos_movements`=2, `kanban_cards`=2, `sales_orders`=12 — pilot darajadagi izlar.

Shu sabab egasi **30-35%** his qiladi (pastda batafsil izoh).

---

## Vizyon jadvali

| # | Vizyon | Ball | Nima bor / nima yetishmaydi (dalil) |
|---|--------|------|--------------------------------------|
| **1** | POS Monitor (DRAFT→KARANTIN→QC→OMBOR→AI_GL) | 🟡 | **REAL state-machine.** `pos-movement-status.constants.ts:13` VALID_TRANSITIONS: draft→karantin→qc_pending→qc_approved→approved(OMBOR)→ai_processing(AI_GL)→completed; `quarantine-workflow.service.ts:79` `qcDecision()` QC qarorida `warehouse_stock` ni QC-HOLD↔RM-MAIN real ko'chiradi; `statusToConfirmStep` (`:32`) QC/OMBOR/AI_GL/FINANCE bosqichlarini `movement_confirmations` ga yozadi. **YETISHMAYDI:** (a) jonli ishlatilmagan — `pos_movements`=**2**; (b) ⭐ AI_GL bosqichi UZUQ: `pos-gl-auto.listener.ts:44` `handle()` faqat `pos_gl_posting_log` (AWAITING_REVIEW staging) ga yozadi, kanonik `entries` defteriga HECH QACHON o'tmaydi; va listener o'zining izohida (`:18`) "no emit site exists today" deydi — event hech kim emit qilmaydi (dead-letter). `pos_gl_posting_log`=**0**. AI_GL = qoida-asoslangan, haqiqiy "AI" emas. |
| **2** | Kassir-hub (hamma pul bitta kassirdan) | 🔴 | **Markaziy kassir YO'Q.** `cash_registers`=0, `cash_sessions`=0, `cash_transactions`=0 (jonli SELECT). `CashRegister.tsx` + `useCashRegister.ts` = **chakana POS kassasi** (`/api/pos/products`, `/api/pos/transactions`, scan, refund — REAL mutationlar), lekin bu "hamma pul bitta kassirdan" markaziy hub EMAS. Finance modulida cashflow/advance controllerlar bor, lekin oylik+xarajat+sotuv hammasi bitta kassir-darvozadan o'tadigan yagona oqim qurilmagan. Pul oqimi tarqoq. |
| **3** | 9-13 ombor turi (barkod) | ✅ | **REAL — eng to'liq vizyon.** `warehouses`=**12** to'g'ri tur bilan (jonli): FG-MAIN(tayyor), RM-MAIN(xom ashyo), RM-ROLLS(rulon), MRO-MAIN(xo'jalik), MRO-STORE, QC-HOLD(karantin), SCRAP-MAIN(brak), TOOL-MAIN(asbob), WH-MAIN, WH-PROD-FLEXO, +offset. Barkod: `wms-barcode.controller.ts` REAL; `warehouse_stock`=**25** qator (jonli data bor!). FE: POS Monitor + Ombor Dashboard Tabs. |
| **4** | 22 ofset sex (sexma-sex marshrut) | 🔴 | **Infratuzilma bor, data 0.** `work_centers`=**0** (22 sex kiritilmagan, jonli SELECT), `pp_routing_operations`=0, `routing_operations`=0. `pp/work-centers/`, `pp/routings/` modul-papkalari + `pp.module.ts` REAL CRUD skeleti. Ofset ombori bor. **YETISHMAYDI:** birorta sex/marshrut konfiguratsiya qilinmagan; "sexma-sex" oqim hech qachon ishlamagan. |
| **5** | Flekso bo'lim | 🔴 | Ombor bor (WH-PROD-FLEXO "Flekso sex ombori"). Alohida Flekso work-center/routing YO'Q — umumiy `pp` ostida bo'lishi kerak edi, `work_centers`=0. Faqat ombor nomi darajasida. |
| **6** | Gofra 2-qatlam | 🔴 | **Formula bor, ishlatilmagan.** ⭐ `layer-formula.service.ts:44` `corrugatedTotalGsm()` REAL — gofra ko'p qatlam: Σ(liner gsm) + Σ(flute gsm × take_up_factor), `material_layer_config` jadvalidan o'qiydi. **AMMO** `material_layer_config`=**0** (birorta gofra qatlam konfiguratsiya qilinmagan), na gofra work-center, na modul. "2-qatlam" matematikasi bor lekin jonli ishlatilmagan. (Oldingi "boshlanmagan" baho — soxta-negativ, formula bor.) |
| **7** | ⭐Qatlam formula (kg→m2→list, AI-reja uchun kritik) | 🟡 | **REAL va WIRED (oldingi "YO'Q" baho noto'g'ri edi).** `apps/api/src/modules/mm/application/layer-formula.service.ts` to'liq: `sheetAreaM2()`, `plainSheetCount()` (kg→list), `plainKg()` (list→kg), `corrugatedTotalGsm()` (gofra). `convert()` (`:53`) `material_cards` (grammage/format_a/format_b/material_kind) + `material_layer_config` dan o'qiydi — **hech narsa hardcode emas**. Endpoint REAL: `mm-materials.controller.ts:56` `GET :id/sheet-conversion?kg=&sheets=`. **YETISHMAYDI (shuning uchun 🟡 emas ✅):** kirish data BO'SH — `material_cards`(21): `format_a`=**0**, `material_kind`=**0**, `grammage`=**5/21**, `material_layer_config`=**0** → formula har materialda VALIDATION xato qaytaradi (o'lcham/gsm belgilanmagan). Aql bor, oziq yo'q. AI-rejaga (pp) ulanish ko'rinmadi — faqat alohida endpoint. |
| **8** | Kassirovka (avto/qo'l) | ⬜ | **Boshlanmagan.** Kassirovka (kesish/tahrir bosqichi) uchun alohida operatsiya/jadval topilmadi. `pp_routing_operations` da bo'lishi mumkin edi — u BO'SH (=0). QC da `imposition.service.ts` (sheet imposition/cutting-stock FFDH) bor, lekin bu kassirovka-operatsiya oqimi emas. |
| **9** | Tigel (QC/brak manbasi) | 🔴 | **QC modul REAL, tigel-operatsiya YO'Q.** `modules/qc/` REAL kod (`spc.service.ts` SPC, `imposition.service.ts`, `qc-parameters.repository.ts` cutting parametrlar). Brak ombori SCRAP-MAIN mavjud. **YETISHMAYDI:** "Tigel" mashinasi alohida work-center/operatsiya sifatida konfiguratsiya qilinmagan (`work_centers`=0); brak oqimi (tigel→SCRAP-MAIN) jadval darajasida tayyor, jonli ishlatilmagan. |
| **10** | Har buyurtmaga material sarfi (IoT+skan, yarim-tayyor, brak/qoldiq/kamomad) | 🔴 | **Skelet bor, data 0.** `material_consumption`=**0**, `mes_telemetry`=**0** (IoT). `ow_material_requirements` fan-out REAL kod (`advance-approved-fanout.listener.ts` warehouse→createMaterialRequirementJob) lekin natija jadvali BO'SH. IoT modul (`iot/sensors`, `iot/oee`) skeleti bor. Yarim-tayyor ko'chirish, brak/qoldiq/kamomad hisobi jonli yo'q. (Memory: IoT anomaly handler no-op stub.) |
| **11** | Oylik (moliya→direktor tasdiq→kassir; avans PIN orqali) | 🟡 | **Payroll service REAL, oqim qisman, PIN YO'Q.** `finance-extended-payroll.controller.ts` calculate/approve/runPayroll + `hr-payroll.controller.ts:120` approvePayroll REAL (memory: compute INPS8/JSHD12). `payroll`/`payroll_calculations`/`payroll_advances` jadval bor lekin =**0** (jonli ishlatilmagan). **YETISHMAYDI:** (a) "avans PIN orqali" — grep `pin.*advance` HECH NARSA topmadi (PIN-darvoza yo'q); (b) moliya→direktor→kassir to'liq zanjiri (kassir oxiri = vizyon 2 ga bog'liq, u 🔴); jonli oylik bir marta ham yopilmagan. |
| **12** | Oshpaz xo'jalik-ombordan skanerlaydi | 🔴 | **Ombor bor, modul YO'Q.** MRO-MAIN "Xo'jalik Ombori" + barkod skan infratuzilmasi REAL. Lekin "oshpaz"/oshxona/kitchen moduli, menyu, oshpaz-skan oqimi HECH QAYERDA YO'Q (grep `oshpaz|kitchen|chef` = 0 natija). Faqat ombor turi darajasida. |
| **13** | CRM aloqa (SMS+AI-qo'ng'iroq+email) | 🔴 | **YASHIL-YOLG'ON — hech narsa yuborilmaydi.** `crm/application/crm-comms.service.ts:16-39` `sendEmail`/`sendSms`/`sendWhatsapp` faqat DB ga LOG yozadi (`logEmail`/`logSms`/`logWhatsapp`) va `{sent:true, sent_at:...}` qaytaradi — haqiqiy provider (Eskiz/SMTP/Twilio) chaqiruvi YO'Q. `sendSms` subject = `"SMS sent to ${phone}"` (echo). **AI-qo'ng'iroq (voice call) umuman YO'Q** (`crm-ai-extended.service.ts:79` `analyzeVoiceCall` faqat tahlil qiladi, qo'ng'iroq qilmaydi). 200 qaytaradi, real aloqa yo'q. |
| **14** | Menejer buyurtma-paneli (real-time, bosqich, kechikkan-belgili) | 🟡 | **REAL panel + saga.** `order-workflow.controller.ts:52-123` create order / status / payment-plan / saga-status (REAL CQRS handlerlar: `get-order-saga.handler.ts`, `create-payment-plan.handler.ts`). FE order-workflow saga-status o'qiydi. **YETISHMAYDI:** real-time WebSocket emas (GET polling), bosqich fan-out `sd_order_departments`=**0** (har buyurtma bosqichi jonli yo'q), jonli buyurtma 12 ta. "Kechikkan-belgili" UI darajasida bo'lishi mumkin lekin data yo'q. |
| **15** | Web-sayt (katalog+lid+CMS) | 🟡 | **REAL backend + FE.** `ecommerce/website/website.controller.ts` (pages/settings CRUD), `ecommerce-public.controller.ts` (@Public public/categories, public/orders, public/products/:slug, public/contact→CRM lead). FE: `MarketingWebsiteCMS.tsx`. Lid: `crm_leads` source='website' (jonli 1 ta). **YETISHMAYDI:** to'liq public katalog UI alohida sayt sifatida yo'q (faqat admin CMS); `blog_posts`=**0**, `leads`=**0** — kontent kiritilmagan. |
| **16** | Marketing xarajat (kassa→xarajat, reja-vs-fakt, Moliyaga bog') | 🔴 | **DB CRUD bor, Moliyaga bog' YO'Q.** Marketing budget endpointlar REAL (memory: budget/calendar real DB), `MarketingBudget.tsx` reja-vs-fakt UI. **AMMO** `marketing/` modulida Finance/GL ga event listener yoki expense-posting HECH QAYERDA YO'Q (grep `OnEvent|fi-expenses|finance` = 0 marketing repo'larida) — kassa→xarajat→Moliya zanjiri ulanmagan. `marketing_budget_items`=**0**, `campaigns`=**0**. `entries`(GL)=0. |
| **17** | SMM + AI nazorat (AI kuzatadi, YOZMAYDI) | 🔴 | **Vizyonga ZID + FE-BE drift.** `MarketingSocialInbox.tsx` `social/accounts` (REAL), lekin `inbox/conversations/.../messages`, `inbox/.../reply`, `inbox/ai-reply/:id` backendda 501-stub (S2 §16: `marketing-analytics-stubs.controller.ts`). Mavjud `aiReplyMutation` AI **javob YOZADI** — vizyon "AI faqat kuzatadi, yozmaydi" deydi, **qarama-qarshi**. Sof SMM-monitor/sentiment AI yo'q. |
| **18** | Lid-gen 4 kanal (Web/Telegram/LinkedIn/Instagram) | 🔴 | **2/4 kanal.** Web (`ecommerce-public public/contact`→lead, REAL) ✅; Telegram (`crm-auto-lead.service.ts:45` `ingestTelegramLead`→`crm_leads`, REAL) ✅. **LinkedIn + Instagram ingestion YO'Q** (grep faqat HR/marketing-dto kontekstida, lid-kanal emas). `crm_leads`(5): source faqat `website`/`CALL`. 4 dan 2 kanal. |
| **19** | ⭐⭐ Butun ERP org-strukturaga bog' (master data) | 🟡 | **Eng kuchli zona — REAL master data.** `org_departments`=**142** (parent_id tree, head_user_id ustun), `org_functions`=97, `users`=31, `employees`=30. **34 modul fayli** `org_departments` ga murojaat (HR/CC/procurement/chat/export). CC org-resolver (`cc-org-resolver.service.ts:36`) tasdiqlovchini orgsxemadan topadi (CEO root, MANAGER_OF_SENDER + org-tree walk-up fallback `:65`, DEPT_HEAD). **YETISHMAYDI:** (a) `head_user_id` faqat **18/142** to'ldirilgan (124 NULL); (b) `employees.manager_id`=**0/30** (hammasi NULL, jonli SELECT) → MANAGER_OF_SENDER to'g'ri yo'li buzuq, fallback'ga tayanadi; (c) "butun ERP" emas — asosan HR/CC bog'langan, ishlab chiqarish/moliya org-bog'i zaif. `employees.user_id`=30/30 (backfill bo'lgan ✅). |
| **20** | Moslashuvchan org-konstruktor (head_user_id master) | 🟡 | **REAL konstruktor.** `org-structure.service.ts` getHierarchy/create/update/move (cycle-guard), `org-structure.controller.ts`. `head_user_id` master ustun mavjud (jonli). `node-portret.service.ts` Portret wizard (memory: REAL upsert). **YETISHMAYDI:** getNodeHistory 501 (memory), `org_node_portret` jonli ishlatilmagan, "kerakli jihozlar" modeli yo'q, head_user_id 18/142. |
| **21** | ⭐"Oltin ip" MVP (buyurtma→ishlab chiqarish→ombor→moliya end-to-end) | 🔴 | **Kod bor, jonli oqim YO'Q.** Phase-4 fan-out spine REAL: `sd/infrastructure/event-handlers/advance-approved-fanout.listener.ts` (70% avans→`sd_order_departments`→har bo'limga job: mold/design/cliche/logistics/warehouse — 5/6 wired, production deferred), `pp/.../advance-approved.listener.ts`, saga + `order-workflow.controller.ts` REAL. **AMMO jonli oqim UZILGAN:** `sd_order_departments`=**0**, `ow_molds`/`ow_tech_cards`=**0**, `domain_events`=**0** (memory: outbox ishlaydi lekin persist 0), `work_centers`=0, `entries`(GL)=0 → end-to-end **jonli bir marta ham aylanmagan** (faqat test, memory). "Ishlab chiqarish" bo'g'ini (vizyon 4-10) bo'sh → ip uzilgan. |
| **22** | Integratsiya oqimi (CC→Kanban→Kassir, jonli stok; "orollar") | 🔴 | **Qisman ulangan, ko'pi orol.** Spine REAL: CC (`cc-workflow`, `cc-org-resolver`, cc.gateway WebSocket, `cc-event.listener.ts`), kanban (`kanban_cards`=**2** jonli), POS↔WMS sync (memory: tuzatilgan). **AMMO** CC→Kanban→Kassir to'liq zanjiri bir marta ham aylanmagan; `domain_events`=**0** (event bus ishlaydi, persist 0); 13+ zero-listener event (memory: §transmission-map); pos-gl→log lekin ledger emas (vizyon 1). Egasi aytgan "orollar" holati jonli kuzatiladi. |

---

## Yakuniy hisob

| Holat | Soni | Vizyonlar |
|-------|------|-----------|
| ✅ done | **1** | 3 (9-13 ombor turi + barkod — yagona to'liq, jonli data ham bor) |
| 🟡 qisman | **7** | 1 (POS Monitor), 7 (qatlam formula — aql bor, data yo'q), 11 (oylik), 14 (menejer panel), 15 (web-sayt), 19 (org-bog'), 20 (org-konstruktor) |
| 🔴 zo'rg'a | **12** | 2 (kassir-hub), 4 (ofset sex), 5 (flekso), 6 (gofra), 9 (tigel), 10 (material sarf), 12 (oshpaz), 13 (CRM aloqa), 16 (marketing xarajat), 17 (SMM AI), 18 (lid-gen), 21 (oltin ip), 22 (integratsiya) |
| ⬜ boshlanmagan | **1** | 8 (kassirovka) |

> Aniq sanoq (jonli tasdiqlangan): ✅=**1** (vizyon 3) · 🟡=**7** (1,7,11,14,15,19,20) · 🔴=**13** (2,4,5,6,9,10,12,13,16,17,18,21,22) · ⬜=**1** (8). Jami = 22.

---

## ⭐ NEGA texnik ~78% lekin egasi ~30-35% his qiladi?

**Qurilgan (78%) = kod skeleti.** Deyarli har vizyon uchun jadval + Drizzle repo + Result-pattern service + controller + FE sahifa **mavjud va sintaktik REAL** (DB ga yozadigan kod). Reviewer'lar yashil (tsc 0, guard PASS). Bu "78%" raqamining manbai. Hatto eng kritik aqliy qism — qatlam formula (vizyon 7) — to'liq yozilgan va endpoint'ga ulangan.

**His qilingan (30-35%) = vizyon haqiqatan talab qilgan narsa — UCH bo'shliq:**

1. **Jonli biznes-data BO'SH (eng katta sabab).** Vizyon yadrosi bo'lgan jadvallar 0 qator (jonli SELECT bilan tasdiqlangan): `work_centers`=0 (22 ofset sex yo'q), `material_layer_config`=0 (formula oziqlanmaydi), `cash_*`=0, `entries`(GL)=0, `pos_gl_posting_log`=0, `sd_order_departments`=0, `ow_*`=0, `mes_telemetry`=0, `payroll`=0, `blog_posts`/`campaigns`/`marketing_budget_items`=0. Egasi ekranni ochganda — bo'sh ro'yxat ko'radi. Hatto vizyon 7 formulasi REAL bo'lsa ham, `material_cards` da `format_a`=0/21, `material_kind`=0/21 → har chaqiruvda VALIDATION xato. "Kod bor" ≠ "ishlaydigan tizim".

2. **Yashil-yolg'on va uzuq bo'g'inlar (eng aldamchi sabab).** Ba'zi qismlar 200 qaytaradi, lekin vizyon talabini bajarmaydi: (a) vizyon 13 — CRM `sendSms/sendEmail` faqat log yozadi, hech narsa **yubormaydi** (`{sent:true}` echo); (b) vizyon 1 — POS GL faqat `pos_gl_posting_log` staging'ga, kanonik `entries` defteriga **hech qachon** o'tmaydi, va eventni hech kim emit qilmaydi (dead-letter); (c) vizyon 16 — marketing xarajat Finance'ga ulanmagan; (d) vizyon 17 — AI "yozadi" lekin vizyon "faqat kuzatsin" deydi (teskari). Egasi "ishladi" deb ko'radi, lekin natija ko'rinmaydi.

3. **Oqim uzuq — "orol"lar (vizyon 21, 22).** Oltin ip (buyurtma→i.ch→ombor→moliya) faqat **test'da** aylangan, jonli emas — `sd_order_departments`=0, `domain_events`=0 (event bus ishlaydi, hech narsa persist bo'lmaydi), 13+ zero-listener event. Markaziy kassir-hub (vizyon 2) qurilmagan, shuning uchun pul oqimi tarqoq. "Ishlab chiqarish" bloki (vizyon 4-10) deyarli bo'sh, shuning uchun zanjirning o'rta bo'g'ini yo'q. Natijada egasi har modulni alohida orol sifatida ko'radi.

**Qisqasi:** 78% = "har xona qurilgan, devorlar bor, hatto formula-miya o'rnatilgan" (skelet). 30-35% = "lekin elektr ulanmagan, suvni hech kim ochmagan, formula-miyaga ma'lumot kirmagan, hech kim yashamaydi" (jonli oqim + data + uzuq bo'g'in). **Eng katta leverage (tartib bilan):** (1) vizyon 7 formulasini oziqlantirish — `material_cards.format_a/material_kind/grammage` + `material_layer_config` seed (kod tayyor); (2) `work_centers` + `pp_orders` seed va jonli oltin-ip aylanishi (vizyon 21 — spine tayyor); (3) POS GL→`entries` ledger ulash + emit-site (vizyon 1) va CRM SMS adapterini real Eskiz'ga ulash (vizyon 13 — `pos/sms.service.ts` da real Eskiz adapteri bor, faqat CRM uni chaqirmaydi); (4) kassir-hub markazlashtirish (vizyon 2).
