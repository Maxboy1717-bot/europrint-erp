# UI-2 — OMBOR (WAREHOUSE) MODULI FRONTEND TAHLILI (2026-06-02)

> **ROL: 🔵 Tahlilchi (QAT'IY READ-ONLY).** Hech narsa o'zgartirilmadi — faqat shu hisobot.
> **Qatlam: FAQAT KOD** (brauzersiz). Har topilma fayl:satr + route + endpoint dalili bilan.
> **FE root:** `artifacts/erp-dashboard/src`
> **Vizyon:** `docs/ombor-pos-master-plan.md` (§0–§17).
> **Oldingi:** `docs/_ui-codescan-2-ombor.md` + `docs/agent6-ombor-moduli-2026-06-02.md` — o'qib KOD darajasida qayta tasdiqlandi/aniqlashtirildi.

---

## 0. QISQA HUKM

Ombor moduli FE'da **3 avlod UI yonma-yon** turibdi:
1. **YANGI kanonik `/pages/Warehouse*` + `/pages/PosMonitorPage` + `warehouse.api.ts`** — toza (EP/ui + `tLabel` + semantic token, 0 inline xom rang). **Sidebar (tz08) shularni ko'rsatadi.**
2. **O'RTA avlod `/pages/{WMS,MM,Material,Stock,Inventory}*`** — toza dizayn, lekin **funksional dublikat** + boshqa-boshqa BE endpoint.
3. **ESKI POS SPA `src/pos-monitor/pages/*`** (25 sahifa) — raw-kalit i18n + xom hex + dual i18n; **sidebar'da YO'Q**, lekin `PosMonitorApp.tsx` route bilan deep-link ochiladi.

**Asosiy faktlar (kod):**
- Sidebar tz08 = **9 navigatsiya bandi** (`constants.ts:308-319`) → **6 distinct sahifa**.
- `WarehouseRoutes.tsx` = **49 route entry → ~32 distinct komponent**.
- `PosMonitorApp.tsx` = **29 route** (1 kanonik + ~25 eski POS SPA + catch-all).
- Vizyon yadrosi (config-driven turlar, POS Monitor kirim/chiqim, dashboard drill-down, Material 360, P2P) **wired va real**; rulon parent-child, bron (haqiqiy `stock_reservations`), faktura PDF, AI kamera **yo'q/qisman**.

---

## 1. SAHIFA INVENTARI (wired / stub)

### 1.1 KANONIK — toza, wired, sidebar'da ko'rinadi ✅

| Sahifa | Route | Fayl | Endpoint(lar) | Holat |
|---|---|---|---|---|
| **Moliya dashboard** | `/wms/overview` | `WarehouseDashboardPage.tsx` | `GET /api/pos/warehouse-config/dashboard` (`warehouse.api.ts:146`) | ✅ REAL wired. KPI (jami qiymat/omborlar/stok qatorlari), kam-qoldiq paneli, ombor jadvali (`:155-167`) har qator drill-down `→/wms/warehouse-stock/:id` (`:158`), so'nggi harakatlar. Faqat `useEffect`+fetch (mutation yo'q — read dashboard). |
| **Omborlar (config-driven)** | `/wms/warehouses` + `/:type` | `WarehousesPage.tsx` + `WarehouseTypePage.tsx` | `GET .../warehouse-config/types` (`warehouse.api.ts:136`) | ✅ REAL. Har ombor turi = kartochka (`:49-69`); `warehouseCount` badge; karantin/QC/unitBasis badge'lari. Yangi tur qo'shilsa UI o'zi ko'rsatadi (§3.0). |
| **Ombor qoldig'i (Excel)** | `/wms/warehouse-stock/:id` | `WarehouseStockPage.tsx` | `.../warehouses/:id/stock` + `.../materials/:id/movements` (`warehouse.api.ts:141,143`) | ✅ REAL. Qoldiq jadval + material harakat tarixi drill-in. |
| **POS Monitor (kirim/chiqim/skaner)** | `/pos-monitor` (+ `/wms/pos-monitor`) | `pages/PosMonitorPage.tsx` (893 satr) | `posOperationsApi` → `POST .../warehouses/:id/issue`,`/receive`, `GET .../stock`, `.../p2p/pending`, `POST .../p2p/:id/receive` | ✅ REAL + boy. Tablar=ombor turlari, dropdown=ombor, Excel qoldiq jadval, [Kirim]/[Chiqim] dialog (real mutation), minus-saldo bloki (`:393 disabled`), barcode qidirish (USB wedge `useHardwareScanner` + kamera `PosBarcodeScanner`), P2P qabul bo'limi (chek dialog). |
| **Xarid so'rovi (P2P)** | `/wms/procurement` | `ProcurementPage.tsx` | `procurementApi` → approval-chain, `POST/GET requests`, `decide`, worklist | ✅ wired + CRUD (create/approve/reject), ⚠️ UX xom (pastda §3). |
| **Material 360 / ro'yxat** | `/inventory/materials` + `/:id` | `WMSMaterials.tsx` + `components/Material360Card` | `GET /api/inventory/materials` + `.../360-card` | ✅ REAL CRUD. KPI, qidiruv (debounce), kategoriya/holat filtri, jadval, `MaterialDialog` (qo'shish/tahrir), ABC/status badge. ⚠️ "0 material" = bo'sh `europrint` DB, kod nuqsoni emas. |

### 1.2 IKKINCHI DARAJALI — wired, lekin sidebar'da YO'Q yoki dublikat ⚠️

| Sahifa | Route | Fayl | Endpoint | Holat |
|---|---|---|---|---|
| Inventarizatsiya | `/wms/inventory`, `/warehouse/inventory-count` | `InventoryCount.tsx` | `/api/pos/inventory-counts*` | ✅ wired (sidebar'da bor `:315`) |
| Qabul akti (GRN) | `/wms/grn`, `/warehouse/goods-receiving` | `GoodsReceiving.tsx` | `/api/pos/.../grn` | ✅ wired (sidebar `:316`) |
| **Reservation Panel** | `/wms/reservation`, `/warehouse/reservations` | `StockReservation.tsx` | **`GET/POST /api/ai-reservation/{dashboard,batches,requests,optimize}`** | ⚠️ wired+CRUD, LEKIN bu **AI-reservation** moduli (batch/FEFO optimizatsiya), vizyon §-dagi `stock_reservations` (bron/reserved_quantity) EMAS. Sidebar `:317`. ❌ uz/ru **qo'lda translations** (tLabel emas, `:18,30-31`). |
| Material 360 (yagona) | `/wms/material/360/:id` | `WarehouseMaterial360.tsx` | `components/wms/material360/*` | ✅ wired (boy tablar) — `/inventory/materials/:id` bilan dublikat |
| WMS Dashboard | `/wms/dashboard` | `WMSDashboard.tsx` | `GET /api/warehouse/dashboard/{kpis,movement-summary,alerts,top-materials}` (`:50-64`) | ⚠️ kodi toza+wired (alohida BE endpoint), LEKIN **3-chi dashboard dublikat**; bo'sh DB'da `EPErrorState`. |
| Kirim Wizard | `/wms/kirim-new` | `WarehouseKirimWizard.tsx` | `GET /api/pos/wms/warehouses`,`/materials`; `POST /api/pos/movements` + `/inventory-passport` (`:53,66,126,128`) | ⚠️ wired (5-bosqich EXTERNAL_IN), PosMonitorPage [To'liq Kirim] tugmasi bilan ham bog'liq |
| KPI Hub / Reports / Reports-All / Rental / Quarantine / QC-Review / Passport / Barcode-Queue / Employee-Inventory / Audit-Log / Material-Balance / Barcode | `/wms/*`, `/warehouse/*` | tegishli `Warehouse*`/`Material*` | har xil `/api/*` | ⚠️ aksariyati wired, lekin **sidebar'da YO'Q** — faqat URL deep-link. Ko'pi dublikat klasterga kiradi (§4). |
| MM (vendors/PO/dashboard/extended) | `/mm/*` | `MMVendors/MMPurchaseOrders/MMDashboard/MMExtended` | `/api/mm/*` | ✅ wired (tz09 Ta'minot). `MMExtended` 3 URL→1 sahifa (Chek-Bot tab "Tez orada" stub). |

### 1.3 ESKI POS SPA — sidebar'da YO'Q, faqat deep-link ⚠️❌

`PosMonitorApp.tsx:13-39` — 25 eski sahifa (`PosDashboard`, `PosKpiDashboard`, `PosWarehouses`, `PosWarehouseDetail`, `PosMaterials`, `PosMaterial360`, `PosMovementKirim/Chiqim`, `PosLedger`, `PosMyInventory`, `PosReservations`, `PosQuarantine`, `PosQCReview`, `PosReports`, `PosGoodsReceipts`, `PosInventory`, `PosRequests`, `RequisitionDetail`, ...). Catch-all (`:347-349`) noma'lum yo'lni `/pos-monitor`ga qaytaradi.
- `/pos-monitor` = **kanonik `PosMonitorPage`** (`:14,118-124`) ✅.
- Qolgan ~25 = deep-link bilan tirik. PosMonitorPage'dagi [To'liq Kirim]/[To'liq Chiqim]/[Karantin/QC]/[Harakatlar]/[Hisobotlar] tugmalari (`PosMonitorPage.tsx:657-671`) **aynan shu eski SPA sahifalariga** olib boradi (`/pos-monitor/movements/new/kirim` → `PosMovementKirim`). Ya'ni eski SPA hali kanonik oqimning bir qismi.
- ❌ Bu sahifalar: raw camelCase i18n kalit (263/39 fayl), xom hex (473/30 fayl), inline style (523/43 fayl), dual i18n — vizyon §1.13/§16.4 buzilishi (oldingi skanda batafsil).

---

## 2. VIZYON XUSUSIYAT → UI MAVJUDLIGI

| Vizyon xususiyat | UI holati | Dalil | Hukm |
|---|---|---|---|
| **13 ombor turi** (config-driven) | `WarehousesPage` kartochka + `PosMonitorPage` tablar serverdan (`warehouseApi.types()`) | `WarehousesPage.tsx:49`, `PosMonitorPage.tsx:534` (`warehouseCount>0` filter) | ✅ bor (config 9 tur jonli; UI 13 ham ko'rsata oladi) |
| **Barcode SKAN** (USB + kamera) | PosMonitorPage `useHardwareScanner` + `PosBarcodeScanner` (BarcodeDetector/ZXing) | `PosMonitorPage.tsx:611,770-772,877-889` | ✅ bor |
| **Barcode ota-bola (parent-child, rulon split)** | UI YO'Q (DB `parent_barcode_id` bor, FE/BE kod 0) | grep: hech bir sahifa parent barcode ko'rsatmaydi | ❌ yo'q |
| **Per-rulon kg / qisman sarf** | UI YO'Q (qoldiq jadval umumiy qty/unit; rulon-kg, split maydonlari yo'q) | `PosMonitorPage StockTable` faqat name/qty/available/unit | ❌ yo'q |
| **Inventory 360** | `Material360Card` + `WarehouseMaterial360` + `components/wms/material360/*` (Stock/Movements/Finance/Suppliers/Quality/Forecast tab) | `WMSMaterials.tsx:7,136`; `WarehouseMaterial360.tsx` | ✅ bor (boy) |
| **Invoice / akt PDF** | UI'da to'g'ridan PDF tugma KAM; harakat akti PDF BE'da bor (`/pos/movements/:id/pdf`), faktura alohida YO'Q | `WarehouseKirimWizard` passport yozadi; FE'da faktura yuklash tugmasi topilmadi | ⚠️ qisman (akt bor, faktura yo'q) |
| **Reservation (bron)** | `StockReservation` sahifa bor, LEKIN `/api/ai-reservation/*` (AI batch optimizatsiya) — haqiqiy `stock_reservations`/reserved_quantity bron UI'si emas | `StockReservation.tsx:64-73` | ⚠️ qisman (boshqa funksiya; haqiqiy bron tugmasi yo'q) |
| **KPP / Goods-Receiving (qabul)** | `GoodsReceiving` (`/wms/grn`) + PosMonitor P2P qabul + Kirim Wizard | `WarehouseRoutes.tsx:53,57`; `PosMonitorPage P2PSection` | ✅ bor |
| **AI kamera (avto-aniqlash)** | Faqat barcode kamerasi (BarcodeDetector/ZXing) bor; "AI kamera"(object/material foto-aniqlash) sahifa = `StubPage` (`/ai-camera`, CLAUDE.md F4) | `PosBarcodeScanner` = barcode; `/ai-camera` stub | ⚠️ qisman (barcode-kamera ✅, AI-foto ❌) |
| **Karantin / QC** | `WarehouseQuarantine` + `WarehouseQCReview` (+ POS SPA dublikat) | `WarehouseRoutes.tsx:66,69` | ✅ bor (lekin 2-4 dublikat sahifa) |
| **Moliya drill-down dashboard** | `WarehouseDashboardPage` ombor→stok drill-down + low-stock | `WarehouseDashboardPage.tsx:114,158` | ✅ bor |

**Jami:** ✅ 6 · ⚠️ 3 · ❌ 2 (asosiy xususiyatlar bo'yicha).

---

## 3. UX SIFATI (jadval / filtr / forma / touch)

**Kuchli (kanonik tomon):**
- **Jadvallar:** PosMonitorPage qoldiq jadvali responsive (`hidden sm:table-cell`/`md:table-cell` ustun yashirish, `PosMonitorPage.tsx:331,340`), tabular-nums, rang-kodli mavjudlik (qizil/orange/normal `:360-366`). WMSMaterials EP `Table` + ABC/status badge.
- **Filtrlar:** WMSMaterials — debounce qidiruv (350ms `:91`) + kategoriya Select + stok-holat Select (`:186-205`). PosMonitor — jadval ichi qidiruv (5+ qatorda ko'rinadi `:791`) + barcode/nom qidiruv.
- **Formalar:** Dialog asosli (Kirim/Chiqim `:119`, P2P `:237`, MaterialDialog), `autoFocus`, miqdor `type=number` + `max={available}` (minus blok `:154`), validatsiya + toast.
- **Touch / katta tugma (ombor xodimi):** PosMonitorPage tab'lar `rounded-full` katta target (`:693`), sticky header sana/vaqt (`:639`), kamera tugma. Lekin ⚠️ amaliy [Kirim]/[Chiqim] tugmalari `h-7 px-2 text-xs` (`:382,391`) — **planshet/qo'lqop uchun KICHIK** (vizyon §1.3 planshet talabiga to'liq mos emas).
- **Loading:** hamma kanonik sahifa `Skeleton` ishlatadi (F1 qoidasi PASS).

**Zaif:**
- **ProcurementPage** — eng xom UX: xodim/rahbar **raw user ID** so'raydi (`placeholder="masalan 5"` `:152`, `"masalan 35"` `:208`, `:250,284`). Qidiruv/dropdown yo'q; foydalanuvchi ID yodlamaydi (vizyon §7.2-§7.3 buziladi).
- **StockReservation** — uz/ru tilni qo'lda toggle qiladi (`:30, 170-185`), `tLabel` emas — ERP i18n bilan nomuvofiq.
- **POS SPA (25 sahifa)** — inline-styled, raw-kalit, dual i18n (UX tutqichsiz, RU rejimda buzuq matn — oldingi skan §2.2).
- "Yangi material" tugmasi PosMonitorPage'da YO'Q (faqat mavjud qatorlarga kirim/chiqim) — yangi material faqat `/inventory/materials` MaterialDialog'da, u esa boshqa jadvalga yozadi (agent6 §5 material bo'linishi).

---

## 4. DUBLIKAT / O'LIK SAHIFALAR

> Memory: oldin ko'p orphan `WarehouseHub12`/`WMSExtended`/`SupplyChainDashboard`/`BarcodeWarehouse` **o'chirilgan** (`WarehouseRoutes.tsx:4-5` izoh). Hozir qolganlari:

### 4.1 Funksional dublikat klasterlar

| Funksiya | Kanonik (sidebar) | Dublikat (faqat URL/SPA) | Dalil |
|---|---|---|---|
| **Dashboard** ×4 | `WarehouseDashboardPage` (`/wms/overview`) | `WMSDashboard` (`/wms/dashboard`) · `WarehouseKpiHub` (`/wms/kpi-hub`) · `MMDashboard` (`/mm/dashboard`) · POS SPA `PosKpiDashboard`/`PosDashboard` | `WarehouseRoutes.tsx:45,61,62,81` + `PosMonitorApp.tsx:13,21` |
| **Material ro'yxat / 360** | `WMSMaterials` (`/inventory/materials`) | `MaterialBalance` · `MaterialCardsPage` · `MaterialsAccounting` · `WarehouseMaterial360` (`/wms/material/360/:id`) · POS `PosMaterials`/`PosMaterial360`/`PosMaterialBalance` | Glob + `WarehouseRoutes.tsx:65,74` |
| **Kirim/Chiqim** | `PosMonitorPage` inline | `WarehouseKirimWizard` (`/wms/kirim-new`) · POS `PosMovementKirim/Chiqim/New` | `WarehouseRoutes.tsx:71` + `PosMonitorApp.tsx:28,29` |
| **Karantin / QC** | `WarehouseQuarantine`/`WarehouseQCReview` | POS `PosQuarantine`/`PosQCReview` | `WarehouseRoutes.tsx:66,69` + `PosMonitorApp.tsx:38,39` |
| **Reservation** | `StockReservation` (`/wms/reservation`) | POS `PosReservations` | `WarehouseRoutes.tsx:58` + `PosMonitorApp.tsx:24` |
| **Hisobot** ×4 | bittasi | `WarehouseReports` (`/warehouse/reports`) · `WarehouseReportsAll` (`/wms/reports`+`/reports-all`) · POS `PosReports` · `components/pos/PosReports` (eski kassa) | `WarehouseRoutes.tsx:55,63,64` |
| **Omborlar / qoldiq** | `WarehousesPage`/`WarehouseStockPage` | POS `PosWarehouses`/`PosWarehouseDetail` | `PosMonitorApp.tsx:15,16` |

### 4.2 Dublikat ROUTE (1 sahifa → ko'p URL)

- `WarehouseReportsAll` → 2 URL (`:63,64`); `GoodsReceiving` → 2 (`:53,57`); `StockReservation` → 2 (`:54,58`); `InventoryCount` → 2 (`:52,59`); `WMSMaterials` → 2 (`:76,77` — list+detail, LEGITIM).
- `MMExtended` → 3 URL (`:82-84`); `LogisticsDashboard` → 7 URL (`:86-92`).

### 4.3 @deprecated marker

❌ YO'Q — dublikatni faqat route/funksiya xulqi bilan aniqlash mumkin, marker bilan emas.

### 4.4 Sidebar gigiyena ✅

tz08 (`constants.ts:301-320`) = **9 band, toza**: yagona "POS Monitor" (`:313`), yagona "Omborlar" config-driven (`:310`), 9-ombor-turi klasteri YO'Q, eski `/pos/*` klasteri YO'Q (Qoida 22 hurmat qilinadi). ⚠️ "Material 360°" (`:318`) → `/inventory/materials` (= ro'yxat sahifa) — yorliq/sahifa nom-mos emas (mayda IA).

---

## 5. DATA BINDING (sahifa → endpoint)

| Sahifa | Asosiy endpoint(lar) | Client |
|---|---|---|
| WarehouseDashboardPage | `GET /api/pos/warehouse-config/dashboard` | `warehouse.api.ts` |
| WarehousesPage / WarehouseTypePage | `GET /api/pos/warehouse-config/types`, `/warehouses?type=` | `warehouse.api.ts` |
| WarehouseStockPage | `GET .../warehouses/:id/stock`, `.../materials/:id/movements` | `warehouse.api.ts` |
| PosMonitorPage | `POST .../warehouses/:id/issue` & `/receive`; `GET .../stock`; `.../p2p/pending`; `POST .../p2p/:id/receive` | `pos-operations.api.ts` + `warehouse.api.ts` |
| ProcurementPage | `/api/pos/procurement` (approval-chain, requests, decide, list) | `procurement.api.ts` |
| WMSMaterials | `GET /api/inventory/materials` + `.../360-card` | `queryClient.apiRequest` |
| StockReservation | `GET/POST /api/ai-reservation/{dashboard,batches,requests,optimize}` | `useReservationMutations` |
| WMSDashboard | `GET /api/warehouse/dashboard/{kpis,movement-summary,alerts,top-materials}` | `wmsApi` + `apiRequest` |
| WarehouseKirimWizard | `GET /api/pos/wms/warehouses`,`/materials`; `POST /api/pos/movements`,`/inventory-passport` | `apiRequest` |

⚠️ **Endpoint bo'linishi:** ombor FE'da kamida **4 ta turli BE prefiks** ishlatiladi — `/api/pos/warehouse-config/*` (kanonik), `/api/pos/wms/*` (Kirim Wizard), `/api/warehouse/dashboard/*` (WMSDashboard), `/api/inventory/materials` (WMSMaterials), `/api/ai-reservation/*` (StockReservation), `/api/mm/*` (Ta'minot). Bu — 3 avlod backend ham bo'lingani aksi.

---

## 6. TAVSIYALAR (faqat tahlil — ruxsat EMAS, Qoida 23)

1. **POS Monitor amaliy tugmalarini kattalashtirish** — [Kirim]/[Chiqim] `h-7 text-xs` → planshet/qo'lqop uchun katta touch-target (vizyon §1.3).
2. **ProcurementPage UX** — raw user ID o'rniga xodim/rahbar **qidiruv-dropdown** (BE approval-chain tayyor).
3. **Dashboard konsolidatsiya** — `WMSDashboard`+`WarehouseKpiHub`+`MMDashboard` kerakli widgetlarini `WarehouseDashboardPage`ga yig'ib, 4→1.
4. **Eski POS SPA → redirect/o'chirish** (vizyon §16.5) — lekin avval PosMonitorPage [To'liq Kirim/Chiqim] tugmalari hozir `PosMovementKirim/Chiqim`ga bog'liq (`PosMonitorPage.tsx:657-671`); shu 5-bosqich oqimni kanonik EP-uslubga ko'chirish kerak, keyin SPA o'chiriladi.
5. **StockReservation aniqlik** — bu AI-batch optimizatsiya; agar vizyon "bron" (reserved_quantity) bo'lsa, alohida haqiqiy bron UI kerak; tilni `tLabel`ga ko'chirish.
6. **Rulon parent-child + per-kg UI** (§3.2/§6.2) — hozir UI 0; PosMonitor qoldiq jadvaliga rulon-kg/split kengaytirish kerak.
7. **AI-kamera (foto-aniqlash)** stub (`/ai-camera`) — real qilish yoki vizyondan chiqarish.
8. **"Yangi material" tugma** PosMonitorPage'ga — material bo'linishi (`material_cards` vs `materials`) hal qilingach.

> ⚠️ **DIQQAT (Qoida 23):** Yuqoridagi hammasi TAVSIYA. Hech narsa o'chirilmasin/o'zgartirilmasin — faqat egasi aniq "bajar" deganda.

---

## 7. RAQAMLAR (yakuniy, kod)

- Sidebar tz08: **9 band → 6 distinct sahifa** (toza).
- `WarehouseRoutes.tsx`: **49 route → ~32 komponent**; `PosMonitorApp.tsx`: **29 route** (1 kanonik + 25 eski + 3 boshqaruv).
- Kanonik wired sahifa: **6** (overview/warehouses/stock/pos-monitor/procurement/materials-360) — hammasi REAL endpoint, 0 inline xom rang.
- Eski POS SPA: **25 sahifa** (raw-kalit + xom hex), sidebar'da YO'Q, deep-link tirik.
- Vizyon xususiyat: ✅ 6 · ⚠️ 3 · ❌ 2.
- BE endpoint prefiks bo'linishi: **6 turli oila**.

---

*Tahlil 2026-06-02 — FAQAT KOD (brauzersiz). 🔵 Read-only. Hech narsa o'zgartirilmadi (faqat shu hisobot). Oldingi 2 skan KOD darajasida qayta tasdiqlandi; StockReservation `/api/ai-reservation/*` (bron emas) va PosMonitorPage [To'liq Kirim] → eski SPA bog'lanishi aniqlashtirildi.*
