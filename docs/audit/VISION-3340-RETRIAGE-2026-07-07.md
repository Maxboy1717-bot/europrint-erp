# VISION-3340 Re-Triage — 20-Area Vision-vs-Code Re-Verification (2026-07-07)

**Purpose:** `VISION-3340-RECONCILIATION-2026-07-04.md` (835 findings, 385 STILL-OPEN + 223 PARTIALLY-RESOLVED) is 3 days old and this codebase has had a large volume of fix work land since. This pass re-verifies every STILL-OPEN/PARTIALLY-RESOLVED finding against TODAY's live code, deduplicates near-identical root causes (many findings restate the same gap across the original 4 prompt-variant expansions), and classifies each surviving distinct root cause into one of 4 buckets. Source: `wf_008e68d3-fb6/journal.jsonl` (20 area-level triage agents, read-only, live DB access).

**Status as of this writing:** the 65 `fixable-now` items below (minus 6 flagged for explicit owner/schema approval — see the caveats inline: #21 GL cost_center_id, #23 new pp_reason_codes/pp_shift_plans tables, #31 new crm_loss_reasons table, #32 crm_deals.sales_order_id type change, #34 new crm_stage_history table, #38 IoT idempotency columns) were dispatched as 43 clustered work-units to a second workflow (`wf_1eee15f8-be1`, "vision-3340-fix-batch1"). Each cluster's outcome (implemented / partially implemented / found-already-resolved / blocked) will be recorded in `MASTER-STATUS-BOARD-2026-07-06.md` once reviewed, tested, and committed by the main agent — this document is the frozen triage snapshot, not a live status tracker.

**2026-07-08 update:** fix-batch1 closed out at 15/43 units confirmed complete (see `MASTER-STATUS-BOARD-2026-07-06.md`'s "VISION-3340 batch1 fix-workflow — CLOSED OUT" section for the full commit list — items #1-8,10,11,14,18,19,22,25,27 done). The remaining 43 fixable-now items (65 − 6 excluded − 16 done) were re-dispatched as `wf_91c2396c-5db` ("vision-3340-fix-batch2", 39 clustered work-units) — outcome to be recorded on the board once reviewed/tested/committed.

**2026-07-08 — SIX OWNER-APPROVED SCHEMA ITEMS EXECUTED (5 done, 1 blocked).** The owner explicitly approved all six schema-approval items. Executed sequentially, single-writer, one commit each:
- **#38** IoT `tablet_id`+`local_seq_no` idempotency (downtime_events/inline_qc_checks/material_movements) → `58bfcdc9` ✅
- **#21** GL `entries.cost_center_id` (canonical posting table verified = `entries`) + journal-line threading → `e594c987` ✅
- **#23** PP `pp_reason_codes` + `pp_shift_plans` (2 new tables) + real AI shift-assign → `83bb47d7` ✅
- **#31** CRM `crm_loss_reasons` new table + `crm_deals.lost_reason_id` FK + loss-reason rollup → `ace8852` ✅
- **#34** CRM `crm_stage_history` new table + audit write in deal/lead stage handlers → `be68dd24` ✅
- **#32** `crm_deals.sales_order_id` varchar→integer + FK → **BLOCKED-OWNER-DECISION.** Fresh live re-verify: column still 0/5 non-empty (safe on that axis), BUT `crm_deals` is a **VIEW** over base table `deals` (`pg_class.relkind='v'`), so `ALTER COLUMN TYPE`/`ADD CONSTRAINT` is physically impossible (dry-run BEGIN/ROLLBACK confirmed 3 distinct errors). The only path is DROP VIEW → ALTER base `deals` → RECREATE 50-column view → ADD FK — a structural two-worlds change beyond the approved "in-place empty-column type change." No workaround substituted (per owner rule). Note: the reverse FK **`fk_sales_orders_deal_id`** (`sales_orders.deal_id uuid → deals.id uuid`) already exists, so the CRM↔SD link is already FK-enforced in that direction; `deals.sales_order_id` is a redundant varchar back-pointer. Owner options: (a) approve the DROP/RECREATE-view structural task, (b) treat #32 as satisfied by the existing reverse FK and drop the redundant back-pointer, or (c) leave as-is.

Also this session: the card-gate precheck was aligned to the authoritative login-gate predicate (`857938bd`), `CARD_LOGIN_GATE_ENABLED` turned ON (`00002f20`, 0/32 blocked), and the card-attribute owner-data template written (`726e5987`).

Source: `wf_008e68d3-fb6/journal.jsonl`, 20 area-level `result` records, 365 total distinct root causes.

---

## Fixable-Now Items (full detail)

**1. PP order-cancel never notifies SD** (Area: Golden-thread / event / oqim, SB-ids: SB0294) — Cancelling a production order (via aggregate `.cancel()` or the live `PATCH /planning/operations/:id`) never publishes an event, so `sales_orders` is never updated/notified. **Fix:** Add `apps/api/src/modules/pp/domain/events/pp-cancelled.event.ts` (`PpCancelledEvent(poId, salesOrderId)`). Inject `EventBus` into `pp-planning.service.ts`; after a successful `updateOperation` with `status==='cancelled'`, publish the event using `sales_order_id` already returned by the repo's `RETURNING *`. Add `apps/api/src/modules/sd/infrastructure/event-handlers/pp-cancelled-sd.listener.ts` (`@EventsHandler(PpCancelledEvent)`) that runs a guarded idempotent `UPDATE sales_orders SET status='on_hold' WHERE id=... AND status NOT IN ('cancelled','delivered','closed')` with try/catch+Logger. Register listener in `sd.module.ts`. Additive-only, no schema change. **Files:** apps/api/src/modules/pp/domain/aggregates/production-order.aggregate.ts, apps/api/src/modules/pp/application/pp-planning.service.ts, apps/api/src/modules/pp/infrastructure/repositories/pp-planning.repository.ts, apps/api/src/modules/pp/dto/pp.dto.ts

**2. Karta Portret has no PDF export/email** (Area: AI (per-karta + planning), SB-ids: SB0499) — `NodePortretService` only supports get/save JSON; no PDF generation or email distribution exists. **Fix:** Add `NodePortretService.generatePortretPdf(nodeId)` using the existing `getPortret()` data, rendered via the same builder pattern as `hr-pdf-generator.service.ts`/`qc-certificate-pdf.service.ts` (position, razryad, ЦКП target, salary range, requirements). Expose `GET /org-structure/nodes/:nodeId/portret/pdf` returning a PDF buffer (mirror `sd-invoice-pdf.service.ts`'s response pattern). Add `POST .../portret/email` sending the same PDF via the existing `SmtpEmailAdapter` to HR managers/the card's employee, mirroring `pos/application/services/email.service.ts`. **Files:** apps/api/src/modules/org-structure/node-portret.service.ts, node-portret.repository.ts, org-structure.controller.ts, apps/api/src/common/pdf/hr-pdf-generator.service.ts, apps/api/src/modules/notifications/infrastructure/external/smtp-email.adapter.ts

**3. AI invoice-classify listener is a dead-letter** (Area: AI (per-karta + planning), SB-ids: SB0521) — `AiInvoiceClassifyHandler` is fully built but nothing ever publishes `FinanceInvoiceCreatedEvent`. **Fix:** In `finance-invoices.controller.ts`, inject `EventBus` (CommandBus/QueryBus already imported there); after `result.ok` in both `createInvoiceRoot()` (~line 96-101) and `createInvoice()` (~line 125-130), publish `new FinanceInvoiceCreatedEvent({invoiceId, description, amount, vendor, userId})`. Wires the already-built handler that writes AI category/subcategory/confidence to `gl_documents`. **Files:** apps/api/src/modules/finance/presentation/finance-invoices.controller.ts, apps/api/src/modules/ai/domain/events/finance-invoice-created.event.ts, apps/api/src/modules/ai/services/ai-automation-events.service.ts

**4. Mentor rating + qualification-check missing** (Area: LMS / Darslik, SB-ids: SB0122, SB0139) — Mentor assignment/approval CRUD is real, but no rating column or qualification-verification exists (fabrication of a value is explicitly prohibited). **Fix:** Additive migration: `ALTER TABLE lms_card_mentors ADD COLUMN rating NUMERIC(2,1) CHECK(0-5), ADD COLUMN qualification_verified_at TIMESTAMP, ADD COLUMN qualification_verified_by INTEGER REFERENCES users(id)`. Add `RateCardMentorSchema` in `courses.dto.ts`. Add `rateCardMentor()` to `drizzle-lms-misc.repo.ts` and `lms-misc.service.ts` mirroring `updateCardMentor`. Add `PATCH cards/:id/rating` in `LmsMentorsController` with the same `@Roles('HR_MANAGER','TRAINING_OFFICER','DIRECTOR','SUPER_ADMIN')`. **Files:** migrations dir, apps/api/src/modules/lms/presentation/dto/courses.dto.ts, apps/api/src/modules/lms/infrastructure/repositories/drizzle-lms-misc.repo.ts, apps/api/src/modules/lms/application/services/lms-misc.service.ts, apps/api/src/modules/lms/presentation/lms-misc.controller.ts

**5. JWT TTL doc is stale (says 24h, code is 15m)** (Area: Auth / RBAC / login, SB-ids: SB0202, SB0230) — All live code now consistently defaults to 15m; only `ARCHITECTURE.md` still documents 24h. **Fix:** Update `ARCHITECTURE.md:128` to state TTL=15 minutes via `JWT_ACCESS_TOKEN_TTL` (with `JWT_EXPIRES_IN` legacy fallback), and fix the `:140` example to `JWT_ACCESS_TOKEN_TTL=15m`. Doc-only. **Files:** apps/api/src/modules/auth/ARCHITECTURE.md

**6. ЦКП per-product target has no write path** (Area: CKP / maqsad / KPI, SB-ids: SB0005, SB0017, SB0025, SB0035) — Multi-slot fact-entry UI and per-product norma resolution exist, but `ckp_card_products` has no CREATE/UPDATE — table is 0 rows. **Fix:** Add `listCardProducts`, `upsertCardProduct`, `deactivateCardProduct` to `ckp-fact.repository.ts` (parametrized SQL matching the existing `cardProductTarget` read method). Add GET/POST/DELETE routes under `org-structure/ckp/card-products` in `ckp.controller.ts` with a Zod schema (cardId, productId, targetValue, formulaType, measurementUnit, isActive) and same @Roles/@UseGuards pattern. Add a small FE management dialog next to `CkpTab.tsx` reusing its already-fetched `products` query. **Files:** apps/api/src/modules/org-structure/ckp-fact.repository.ts, apps/api/src/modules/org-structure/ckp.controller.ts, artifacts/erp-dashboard/src/components/hr/orgnode/CkpTab.tsx

**7. ЦКП per-employee personal-target override has no write path** (Area: CKP / maqsad / KPI, SB-ids: SB0018) — `ckp_personal_targets` schema + read-resolution exist; no controller route to create/update an override — 0 rows. **Fix:** Add `listPersonalTargets`, `upsertPersonalTarget`, deactivate/delete to `ckp-fact.repository.ts`. Add GET/POST/DELETE `org-structure/ckp/personal-targets` routes with Zod schema (employeeId, cardId, productId?, period? 'YYYY-MM', targetValue, formulaType). Add a small FE form (employee profile or a CkpTab dialog) for HR/managers. **Files:** apps/api/src/modules/org-structure/ckp-fact.repository.ts, apps/api/src/modules/org-structure/ckp.controller.ts

**8. Stat-reglament has no approval/sign-off route** (Area: Hisobot / dashboard, SB-ids: SB0372, SB0387) — `stat_regulations` exists with real versioning+CRUD, but no approve/imzo endpoint. **Fix:** Additive migration adding `approved_by_card_id INTEGER REFERENCES org_functions(id)`, `approved_at TIMESTAMPTZ` to `stat_regulations`. Add `approve(id, approverCardId)` to the repo interface/impl. Add `POST director/stat-regulation/:id/approve` (director/super_admin only) mirroring the existing PATCH handler. **Files:** apps/api/src/shared/db/migrations, apps/api/src/modules/director/domain/repositories/i-stat-regulation.repo.ts, apps/api/src/modules/director/.../stat-regulation.repository.ts, stat-regulation.controller.ts

**9. No root-cause/delay taxonomy surfaced on dashboard** (Area: Hisobot / dashboard, SB-ids: SB0373, SB0381, SB0398, SB0406) — `qc_root_causes` CRUD exists but is scoped only to QC/CAPA, never surfaced as a director-level delay drill-down; 0 rows. **Fix:** Seed `qc_root_causes` with material/transport/operator/qolip categories. Add a nullable `root_cause_id` FK column to `qc_defects`/`production_sessions`. Add `GET director/root-cause-summary` (new repo method GROUP BY root_cause_id COUNT) and surface as a widget on `DirectorDashboard.tsx`. **Files:** apps/api/src/modules/qc/infrastructure/repositories/qc-extended-root-causes.repository.ts, apps/api/src/modules/director/presentation/director-root.controller.ts, artifacts/erp-dashboard/.../DirectorDashboard.tsx

**10. AI daily strategic summary never reaches owner/Telegram** (Area: Hisobot / dashboard, SB-ids: SB0374, SB0383, SB0395) — A real LLM-backed `generateExecutiveSummary()` runs daily at 08:00 but output only goes to server logs; separately, the "ai-summary" endpoint is fake (`aiGenerated: false` hardcoded, no LLM call). **Fix:** In `ai-automation-daily.service.ts`, inject `TelegramService` (already used identically in `owner-summary-daily.cron.ts`) and push `summary.headline`+metrics to `OWNER_TELEGRAM_CHAT_ID`, config-gated like `OwnerSummaryService.trySend()`. Optionally persist to a new `ai_executive_summaries` table for `DirectorDashboard.tsx` display. **Files:** apps/api/src/modules/ai/services/ai-automation-daily.service.ts

**11. IoT/MES telemetry not wired into director dashboard auto-fill** (Area: Hisobot / dashboard, SB-ids: SB0384) — Real telemetry data exists (`mes_telemetry`=684 rows, `downtime_events`=2, `mes_sessions`=6) but no director-level query consumes it. **Fix:** Add a query method to `company-state.repository.ts` (or new director analytics repo) reading `mes_telemetry`/`production_sessions` and expose downtime/energy alongside existing production_plan%, following the `getRawMetrics()` pattern. **Files:** apps/api/src/modules/remaining/company-state.repository.ts

**12. ЦКП deadline-gate compliance rate not on dashboard** (Area: Hisobot / dashboard, SB-ids: SB0397) — `ckp-gate.ts`'s real gate is used only in payroll, never surfaced as a KPI. **Fix:** Add `getCkpDeadlineComplianceRate(date)` aggregation (pass/fail counts over `ckp_fact_values`) and expose on the existing `director/summary` response. **Files:** apps/api/src/modules/director/presentation/director-root.controller.ts

**13. razryad_levels.min_months=0 disables the 3-month interval guard** (Area: Razryad / malaka, SB-ids: SB0763, SB0767, SB0785, SB0793) — UI/BE to set it is now built, but value is unset on all 6 rows; EP-ORG-011 is owner-approved at ">=3 months". **Fix:** Additive migration `UPDATE razryad_levels SET min_months=3 WHERE min_months=0` (safe — EP-ORG-011 is JAVOBLANGAN, unlike the exam-threshold case which was reverted for being unapproved). Verify all 6 rows=3; this activates the existing no-op guard in `razryad-history.service.ts:90-103`. **Files:** apps/api/src/shared/db/migrations, apps/api/src/modules/org-structure/razryad-history.service.ts, artifacts/erp-dashboard/src/pages/RazryadLevelConfig.tsx

**14. No question-bank CRUD for exam content** (Area: Razryad / malaka, SB-ids: SB0770) — `hr_question_bank` is read-only (0 rows); no controller lets HR populate it. **Fix:** Add small CRUD controller/service/repo mirroring `razryad.controller.ts` (list/create/update/soft-delete) covering org_function_id, category, question_uz/ru, expected_keywords, difficulty, lang, razryad_level_id, is_active. Expose under `org-structure/question-bank`; add matching FE admin page mirroring `RazryadLevelConfig.tsx`. **Files:** apps/api/src/modules/ai/infrastructure/repositories/drizzle-ai-exam.repo.ts, ai-exam.service.ts, apps/api/src/modules/org-structure/razryad.controller.ts

**15. Razryad promotion certificate has no PDF** (Area: Razryad / malaka, SB-ids: SB0789) — Certificate is a plain string, unlike QC's working PDF pattern. **Fix:** Mirror `qc-certificate-pdf.service.ts` with a `RazryadCertificatePdfService` rendering card/employee/old-new razryad/cert number/approver. Add `GET :id/certificate.pdf` on `razryad-history.controller.ts` and a download button in `RazryadTab.tsx`. **Files:** apps/api/src/modules/org-structure/razryad-history.service.ts/.repository.ts, apps/api/src/modules/qc/application/qc-certificate-pdf.service.ts, artifacts/erp-dashboard/.../RazryadTab.tsx

**16. No batch/LOT FK from MES material consumption** (Area: IoT / telemetriya, SB-ids: SB0308) — `material_kit_items` has no `batch_id`; `material_batches` (28 cols, 0 rows) is never referenced. **Fix:** Add nullable `batch_id INTEGER REFERENCES material_batches(id)` (IF-NOT-EXISTS migration, same pattern as T7-05 `operator_card_id`). Extend `MaterialKitScanSchema` with optional `batchId`; update scan endpoints in `iot-tablet.controller.ts` to persist batch_id and decrement `material_batches.remaining_quantity`. **Files:** apps/api/src/modules/iot/presentation/iot-tablet.controller.ts, iot-tablet.schemas.ts, apps/api/src/shared/db/invariants/migrations-schema.ts

**17. production_sessions.current_stage never written (100% NULL)** (Area: IoT / telemetriya, SB-ids: SB0306, SB0321) — Schema (setup/main/teardown_seconds + current_stage/stage_started_at) fully provisioned, but no write-path exists. **Fix:** Add stage-transition writes to `iot-tablet.controller.ts` session endpoints and/or `DrizzleMesRepository.saveSession` setting `current_stage IN ('setup','running','teardown')` and `stage_started_at=NOW()`, mirroring the existing COALESCE-write pattern for `operator_card_id`. **Files:** apps/api/src/modules/iot/presentation/iot-tablet.controller.ts, apps/api/src/modules/mes/infrastructure/repositories/drizzle-mes.repo.ts

**18. OEE calculation uses a sensor-threshold proxy, not the real formula** (Area: IoT / telemetriya, SB-ids: SB0322, SB0334) — `findOee()` still counts sensor readings >80 instead of using real timing columns. **Fix:** Rewrite `DrizzleIotOeeRepo.findOee()` to compute `availability = (running_time_seconds - stopped_time_seconds - setup_seconds) / NULLIF(running_time_seconds - stopped_time_seconds, 0)` using `production_sessions` columns now available, grouped by work-center. **Files:** apps/api/src/modules/iot/infrastructure/repositories/drizzle-iot-oee.repo.ts

**19. SOS escalation has no Telegram delivery leg** (Area: IoT / telemetriya, SB-ids: SB0333) — `mes-sos-escalation.service.ts` escalates but never sends Telegram. **Fix:** Inject a Telegram notifier (mirror `pos-telegram.service.ts`) into `mes-sos-escalation.service.ts`, call it at the highest escalation tier for CRITICAL SOS alerts. **Files:** apps/api/src/modules/mes/application/mes-sos-escalation.service.ts, apps/api/src/modules/pos/application/services/pos-telegram.service.ts

**20. Anomaly detection never pauses MES on CRITICAL severity** (Area: IoT / telemetriya, SB-ids: SB0344) — Handler hardcodes severity='high', only writes to `iot_alerts`, no MES pause. **Fix:** In `anomaly-detected.handler.ts`, branch on severity/threshold ratio; on CRITICAL, dispatch a CommandBus command (mirror `IotTabletController.reportProductionDefect`) to set the related `production_sessions` row to 'paused', in addition to the existing `iot_alerts` insert. **Files:** apps/api/src/modules/iot/infrastructure/event-handlers/anomaly-detected.handler.ts

**21. GL `entries` table has no cost_center_id column** (Area: Moliya / GL / kassa, SB-ids: SB0835) — Cost centers are full CRUD master data, but no GL posting can ever be tagged to one. **Fix:** Additive nullable `cost_center_id integer references cost_centers(id)` on `entries` (mirrored in `fi-gl.ts`). Add optional `costCenterId?: number` to `JournalLine`, thread through `createJournalEntry`/`postJournal` into the insert in `drizzle-gl-posting.repo.ts`. All existing callers keep working since it's optional. **CAVEAT:** GL is an owner-protected area per project convention — get explicit sign-off before implementing despite being purely additive. **Files:** lib/db/src/schema/fi-gl.ts, apps/api/src/modules/finance/domain/services/gl-posting.service.ts, drizzle-gl-posting.repo.ts, i-gl-posting.repo.ts

**22. FE Orders tab only shows 4 of 9 backend PO statuses** (Area: PP / ishlab-chiqarish reja, SB-ids: SB0262) — Backend has a full 9-status lifecycle; `ERPOrdersTab.tsx` type union/Select/Badge logic only knows 4 legacy statuses. **Fix:** Widen the status union at `ERPOrdersTab.tsx:89` to match `PoStatus` enum (planned/confirmed/released_to_production/in_progress/in_qc/completed/closed/cancelled/paused). Add matching `<SelectItem>` entries + i18n keys (uz/ru). Extend the Badge variant/label logic to all 9, ideally via a shared status→{variant,label} map driven by `PO_STATUS_TRANSITIONS` so the UI only offers legally-reachable transitions. **Files:** artifacts/erp-dashboard/src/pages/erp/ERPOrdersTab.tsx, apps/api/src/modules/pp/domain/aggregates/production-order.aggregate.ts

**23. pp_reason_codes / pp_shift_plans tables don't exist** (Area: PP / ishlab-chiqarish reja, SB-ids: SB0245, SB0246) — Order-level reasons are free-text; AI shift-assign step is a hardcoded stub with no DB read/write. **Fix:** Add `pp_reason_codes` lookup table (mirror `downtime_reason_codes`: code/name/name_ru/category/color/is_active/sort_order) with Drizzle schema in `pp-enhanced.ts`; extend `FlagsDtoSchema` with optional `reasonCodeId`. Add `pp_shift_plans` table (work_center_id FK, shift_date, shift_type, target_quantity, assigned employees, created_by) + schema; rewrite `runStep6ShiftAssign` in `pp-ai-planning.service.ts` to query real work-center capacity/`shift_assignments` and persist a real shift-plan row instead of a static string. **Files:** lib/db/src/schema/pp/pp-enhanced.ts, apps/api/src/modules/pp/presentation/pp-orders.controller.ts, apps/api/src/modules/pp/application/services/pp-ai-planning.service.ts

**24. Sensitive karta-field edits don't require a reason** (Area: Org-struktura / KARTA-markazlilik, SB-ids: SB0156, SB0188) — Audit interceptor opportunistically captures `reason` but nothing rejects a request that omits it for razryad/salary/state changes. **Fix:** Extend `OrgNodeSchema` in `org-structure.controller.ts` with a `.superRefine()` rejecting (400) any PATCH touching razryadLevelId/salaryType/min/maxSalary/otdeleniyeNo/currentState/headUserId when reason/justification/comment is empty (mirror `AuditInterceptor`'s REASON_KEYS). Thread the caller-supplied reason through `updateFromDto`→`recordManualRazryadChange` so `razryad_history` stores the real reason instead of a hardcoded literal. Apply the same to `CardFreezeSchema` (`card.controller.ts:256-262`, make reason `z.string().min(3)`). **Files:** apps/api/src/modules/org-structure/org-structure.controller.ts, org-structure/org-mutations.repo.ts, apps/api/src/common/interceptors/audit.interceptor.ts, card.controller.ts

**25. mentors.card_id write path missing + no 2-per-card cap** (Area: HR / Xodim, SB-ids: SB0071) — `card_id` column exists and is read-only surfaced; `createMentorship`/`updateMentorship` never accept/write it. **Fix:** In `mentorships-compat.service.ts`, extend `createMentorship` to read `card_id`, run `SELECT COUNT(*) FROM mentors WHERE card_id=... AND deleted_at IS NULL`, throw `BadRequestException` at count>=2, include `card_id` in the INSERT. Apply same to `updateMentorship` via COALESCE. No DTO change needed (`CompatBodyDto` is passthrough). **Files:** apps/api/src/modules/compatibility/mentorships-compat.service.ts

**26. Karta template import/export has no FE UI** (Area: HR / Xodim, SB-ids: SB0069, SB0108) — Two real backend mechanisms (card-template CRUD+apply, org-structure bulk import/export) have zero frontend consumers. **Fix:** Add an "Import" button to the org-structure page backed by a file input parsing uploaded `.xlsx` client-side (add SheetJS/`xlsx` dependency), POST to `/api/org-structure/nodes/import`, render the `{imported,failed,total,created,errors[]}` result in a summary dialog. Separately surface card-template CRUD+apply-template in the card/karta detail view. **Files:** apps/api/src/modules/org-structure/org-structure.controller.ts, card-template.controller.ts, artifacts/erp-dashboard/package.json

**27. LMS training-gate not exposed on FE card-gate banner** (Area: HR / Xodim, SB-ids: SB0093) — `lmsGate.isCardTrainingComplete` is wired into payroll but `resolveGate()` never joins it; no FE surface. **Fix:** In `card.repository.ts` `resolveGate()` (line 709-722), add LMS mandatory-course-completion lookup (reuse `lmsGate` logic) returning `lmsGateBlocked: boolean`. In `EmployeeCardsSummary.tsx`, extend `CardGate` interface and add a fourth `EPStatusPill` ("Darslik-gate") next to the existing `oylikHuquqi` pill. **Files:** apps/api/src/modules/org-structure/card.repository.ts, artifacts/erp-dashboard/src/components/employee/EmployeeCardsSummary.tsx

**28. CRM Kanban stage-keys don't match real DB values** (Area: CRM, SB-ids: SB0638, SB0648) — `DEAL_STAGES`/`LEAD_STAGES` use stale Bitrix-style keys (`C0:NEW`, `IN_PROGRESS`) that don't intersect with real `stage_id`/`status_id` values (QUALIFICATION/PROPOSAL/NEGOTIATION/WON; NEW/IN_PROCESS/CONVERTED/JUNK) — real rows fall into no Kanban column at all. **Fix:** In `crm-types.ts`, replace `DEAL_STAGES` with entries keyed on `deal-status.vo.ts`'s real values (qualification/proposal/negotiation/won/lost), giving Uzbek labels. Fix `LEAD_STAGES` so `IN_PROGRESS` matches real `IN_PROCESS` (rename or accept both) and add a `JUNK` stage. After renaming, grep for other hardcoded `C0:`/`IN_PROGRESS` literal comparisons (e.g. `mark-deal-won.handler.ts`, `deal.aggregate.ts`) to ensure nothing else depends on old keys. **Files:** artifacts/erp-dashboard/src/pages/crm/crm-types.ts, .../useCRMWorkspace.ts, apps/api/src/modules/crm/domain/value-objects/deal-status.vo.ts, drizzle-crm-deals.repo.ts

**29. Customer 360 missing financial/risk fields** (Area: CRM, SB-ids: SB0639, SB0656, SB0677) — `DetailSheetCustomer360.tsx` renders stats/activity/contacts but omits `open_debt`/`credit_limit`/`is_blocked`/`block_reason` which already exist on `crm_companies`. **Fix:** Add a "Financial status" block in the `entityType==='companies'` section (after ~line 132-158) surfacing these fields — red banner when `is_blocked`, debt/limit ratio otherwise. No backend change needed (already returned by companies GET). **Files:** artifacts/erp-dashboard/src/pages/crm/DetailSheetCustomer360.tsx, crm-types.ts

**30. No overdue-task visual warning in CRM** (Area: CRM, SB-ids: SB0644) — `due_date`/`status` exist and one NBA endpoint computes `overdue_count`, but no page shows an overdue badge. **Fix:** In `CRMActivitiesSections.tsx` and `DetailSheetTabs.tsx`, compute `isOverdue = act.dueDate && new Date(act.dueDate) < new Date() && !act.isDone` and render a red badge/icon (reuse existing `text-[var(--ep-red)]` token). No backend change needed. **Files:** artifacts/erp-dashboard/src/pages/CRMActivitiesSections.tsx, crm/DetailSheetTabs.tsx

**31. crm_deals.lost_reason has no taxonomy** (Area: CRM, SB-ids: SB0655, SB0663, SB0675) — Free-text column only, no rollup/analytics possible. **Fix:** Add seed lookup table `crm_loss_reasons` (id, code, label_uz/ru, is_active) via owner-approved migration + additive nullable `lost_reason_id` FK on `crm_deals` (keep free-text field as supplementary note). Update `mark-deal-lost.handler.ts` to accept optional `lost_reason_id`. Add `GET /api/crm/analytics/loss-reasons` rollup endpoint. **Files:** apps/api/src/modules/crm/application/commands/mark-deal-lost.handler.ts, presentation/crm-analytics.controller.ts, analytics/repositories/drizzle-crm-analytics.repo.ts

**32. crm_deals.sales_order_id is varchar, no FK** (Area: CRM, SB-ids: SB0667) — Type mismatch with `sales_orders.id` (integer) blocks a real FK; column is 100% unpopulated so conversion is safe today. **Fix:** Owner-approved migration: `ALTER TABLE crm_deals ALTER COLUMN sales_order_id TYPE integer USING sales_order_id::integer`, then add FK to `sales_orders(id) ON DELETE SET NULL`. Update `deal-won.listener.ts:136` to bind `orderId` directly (integer, not `String(orderId)`); update the Drizzle schema definition to integer. **Files:** apps/api/src/modules/sd/infrastructure/event-handlers/deal-won.listener.ts, crm_deals schema definition

**33. No lead-aging reassignment cron** (Area: CRM, SB-ids: SB0668) — `last_activity_at`/`next_activity_at` populated but no job reassigns leads cold 60+ days. **Fix:** Add `@Cron` job `lead-aging-reassign.cron.ts` (registered in `crm.module.ts`) running daily, selecting leads where `COALESCE(last_activity_at, created_at) < NOW() - INTERVAL '60 days'` and status not terminal, reassigning via a generalized `assignManagerIfMissing` (allow overwriting, not just filling nulls) from `website-lead.repository.ts`, logging as a `crm_activities` note. **Files:** apps/api/src/modules/crm/listeners/website-lead.repository.ts, crm.module.ts

**34. No CRM stage-transition history table** (Area: CRM, SB-ids: SB0669) — Handlers overwrite `stage_id` in place with no audit trail. **Fix:** Owner-approved additive migration creating `crm_stage_history` (entity_type, entity_id, from_stage, to_stage, changed_by, changed_at). In `update-deal-stage.handler.ts` and `update-lead-stage.handler.ts`, insert a history row using the already-loaded "before" state, same-transaction. **Files:** apps/api/src/modules/crm/application/commands/update-deal-stage.handler.ts, update-lead-stage.handler.ts

**35. CRM leads/deals have no row-level manager scoping** (Area: CRM, SB-ids: SB0679) — Role guards exist but any CRM-read role sees every lead/deal regardless of `manager_id`. **Fix:** Thread authenticated user's role/employeeId into list endpoints in `crm-leads.controller.ts`/`crm-deals.controller.ts`; for scoped roles (e.g. `sales_manager`, not director/crm_manager/super_admin), pass employeeId as a `manager_id` filter. Update `findAll()` in `drizzle-crm-leads.repo.ts`/`drizzle-crm-deals.repo.ts` to accept optional `scopedManagerId` and add `WHERE manager_id=...` when present. **Files:** apps/api/src/modules/crm/presentation/crm-leads.controller.ts, crm-deals.controller.ts, leads/drizzle-crm-leads.repo.ts, deals/drizzle-crm-deals.repo.ts

**36. QC certificate PDF not trilingual, fake QR** (Area: QC / sifat, SB-ids: SB0460, SB0470, SB0494) — `drawQrPlaceholder()` draws a fake rectangle despite `qrcode` package already used elsewhere; only uz/en labels, no ru. **Fix:** Import `qrcode`, replace fake rectangle with `await QRCode.toBuffer(...)` embedded via `pdf.embedPng`+`drawImage`, mirroring `cc-pdf.service.ts`. Add Russian labels alongside uz/en in `buildPdf()`/`drawTestTable()`/`drawSignatureArea()`. Leave `qc_certificate_templates` (unread, 0 rows) as future work. **Files:** apps/api/src/modules/qc/application/qc-certificate-pdf.service.ts

**37. No razryad-based sign-off gate for final QC** (Area: QC / sifat, SB-ids: SB0443, SB0446) — Card linkage on inspections is real, but no minimum-razryad check exists for final sign-off. **Fix:** In `qc-inspections.controller.ts` `POST :id/submit` (or `submit-inspection.handler.ts`), when the inspection is 'final', join `inspector_card_id`→org_departments/razryad_levels and require a minimum razryad; return `BUSINESS_RULE_VIOLATION` otherwise. Reuse the join pattern from `create-inspection.handler.ts:49-54`. **Files:** apps/api/src/modules/qc/application/commands/create-inspection.handler.ts, presentation/qc-inspections.controller.ts

**38. IoT tablet endpoints lack idempotency keys** (Area: QC / sifat, SB-ids: SB0459) — Offline defect/inline-QC/session start-stop mutations have no `tablet_id`+`local_seq_no` dedup, risking double-insert on retry. **Fix:** Add nullable `tablet_id TEXT`+`local_seq_no BIGINT` columns (additive migration, owner sign-off per Q-35) with unique index, to tables backing defect/inline-qc/start/stop/material-return endpoints in `iot-tablet.controller.ts`; accept as optional DTO fields, check-existing-before-insert (or ON CONFLICT DO NOTHING) when present. **Files:** apps/api/src/modules/iot/presentation/iot-tablet.controller.ts, iot-tablet.schemas.ts

**39. AI vision QC confidence threshold is hardcoded** (Area: QC / sifat, SB-ids: SB0461) — `AI_AUTO_CONFIDENCE_THRESHOLD=0.85` is a constant, not configurable. **Fix:** In `vision-qc.service.ts`, replace the direct constant import with a runtime read from the `settings` key/value table (`AI_VISION_QC_AUTO_CONFIDENCE`), falling back to 0.85, mirroring the exact pattern in `delivery-completed.listener.ts`'s `readCostRatio()`/`readVatRate()`. **Files:** apps/api/src/modules/ai-agents/qc/vision-qc.service.ts, ai-agents.constants.ts

**40. No multi-hop material→QC→delivery traceability query** (Area: QC / sifat, SB-ids: SB0465) — No dedicated JOIN-based traceability service exists despite all the FK pieces being present. **Fix:** Add `GET /qc/traceability/:productionOrderId` joining `mm_goods_receipts`→`production_orders`→`qc_inspections`→the `QC-<id>`/`MES-<id>` batch numbers already written into `warehouse_stock`→`sales_orders`/deliveries. Purely additive reporting code, no schema change. **Files:** apps/api/src/modules/wms/infrastructure/event-handlers/qc-passed.listener.ts, mes-completed-fg.listener.ts

**41. Quarantine gate has a TOCTOU race condition** (Area: QC / sifat, SB-ids: SB0466) — `sendToQuarantine`/`applyQcDecision`/`releaseToMain` each SELECT then separately UPDATE with no transaction/lock/optimistic guard. **Fix:** In `wms-quarantine.repository.ts` `updateReceiptStatus()`, add `AND status = ${expectedStatus}` to the UPDATE's WHERE; treat a 0-row `RETURNING` result as `Err('CONFLICT')` in `wms-quarantine-gate.service.ts` rather than assuming success. **Files:** apps/api/src/modules/wms/infrastructure/repositories/wms-quarantine.repository.ts, apps/api/src/modules/wms/application/wms-quarantine-gate.service.ts

**42. Defect stage enum inconsistent between two write paths** (Area: QC / sifat, SB-ids: SB0473, SB0493) — A real `stage`-based stats aggregation exists, but `qc-defects-extended.controller.ts` accepts free-text `stage` defaulting to 'production' (not in the enum used by `qc-new.controller.ts`). **Fix:** Validate incoming `stage` in `qc-defects-extended.controller.ts` against the same Zod enum (`['incoming','in_process','final','dispatch']`) defined in `qc-new.controller.ts:29`. **Files:** apps/api/src/modules/qc/presentation/qc-defects-extended.controller.ts, qc-new.controller.ts

**43. FMEA critical-RPN stop-production flag is computed but never enforced** (Area: QC / sifat, SB-ids: SB0496) — `requiresStopProduction` (RPN>200) is calculated and discarded; `FmeaService` is registered but never injected anywhere. **Fix:** Wire `FmeaService.analyze()` output into `report-defect.handler.ts` or QC inspection submit flow: when `requiresStopProduction=true`, update `production_orders.status` to a blocking state ('qc_hold') using the same safe idempotent UPDATE pattern already in `qc-rework.listener.ts`, and notify. **Files:** apps/api/src/modules/qc/domain/services/fmea.service.ts, application/commands/report-defect.handler.ts, apps/api/src/modules/pp/infrastructure/event-handlers/qc-rework.listener.ts

**44. IoT tablet downtime endpoint bypasses reason_code_id FK** (Area: MES / sex / ish-sessiya, SB-ids: SB0417, SB0424, SB0441) — The BE maintenance-controller path writes `reason_code_id` correctly, but the actual shop-floor tablet path (used by `MESDowntimes.tsx`) writes free-text only from a hardcoded 8-item dictionary. **Fix:** Add optional `reasonId` to `DowntimeEventSchema`; in `iot-tablet.controller.ts` `reportDowntimeEvent`, look up code/name from `mes_downtime_reasons` when supplied and populate `reason_code_id`, falling back to free text. On FE, replace hardcoded `REASON_LABELS` in `MESDowntimes.tsx` with a `useQuery(['/api/mes/downtime-reasons'])` fetch (already used in `MESExtended.tsx`) so operators pick from the live 16-row list. **Files:** apps/api/src/modules/iot/presentation/iot-tablet.controller.ts, iot-tablet.schemas.ts, artifacts/erp-dashboard/src/pages/MESDowntimes.tsx, apps/api/src/modules/mes/infrastructure/repositories/mes-maintenance.repo.ts

**45. Session-start checklist gate has no way to be satisfied** (Area: MES / sex / ish-sessiya, SB-ids: SB0414, SB0420) — A real fail-closed checklist gate blocks session start, but no CRUD exists to create a checklist or mark items complete — permanently blocks every session. **Fix:** (1) Auto-create a `setup_checklists` row + fixed `checklist_items` when a `production_sessions` row is created (or a `POST /api/mes/sessions/:id/checklist` that seeds one). (2) Add a PATCH endpoint (same `@Public()`+`TabletTokenGuard` pattern as other tablet routes) letting the operator toggle `checklist_items.is_completed`/`completed_by`/`completed_at` before `POST production-sessions/:id/start`. **Files:** apps/api/src/modules/mes/application/commands/start-session.handler.ts, apps/api/src/modules/iot/presentation/iot-tablet.controller.ts, apps/api/src/modules/mes/infrastructure/repositories/drizzle-mes.repo.ts

**46. OEE has no shift/brigade/shop grouping levels** (Area: MES / sex / ish-sessiya, SB-ids: SB0415, SB0440) — Only machine-level aggregation exists despite the vision's 4-level OEE cascade. **Fix:** Extend `get-oee.handler.ts`'s `execute()` to accept a `groupBy` filter (machine|shift|brigade|shop) and build parallel accumulator maps keyed by `production_sessions.shift_id` (shift), crew grouping (brigade), `work_centers.org_department_id` (shop), reusing the existing accumulator/ratio helpers. Note shift/brigade levels return empty until shift_id/crew data are populated (separate owner-data item). **Files:** apps/api/src/modules/mes/application/queries/get-oee.handler.ts, get-oee.query.ts

**47. sales_orders/sd_order_departments have no card/dept FK** (Area: SD / sotuv-buyurtma, SB-ids: SB0579, SB0581, SB0588, SB0598, SB0599, SB0621) — Department is free-text varchar; `assigned_to`/`created_by` are bare UUIDs with no card linkage — no card-scoped RBAC possible in SD. **Fix:** Follow the precedent already shipped for `created_by_user_id` (T8-01, `sd-orders.ts:190-194`): add a nullable `org_department_id` FK to `sales_orders` and `sd_order_departments` (alongside/replacing the varchar `department`). Wire `create-order.handler.ts`/`execSdSalesOrderInsert` to persist the creating card's dept; update `drizzle-sd-order-departments.repo.ts`'s fan-out inserts. Add a card-scoped filter in `sd-orders.controller.ts`/`drizzle-sales-order.repo.ts` so SALES_MANAGER only sees their own department's orders. **Files:** lib/db/src/schema/sd-orders.ts, apps/api/src/common/database/queries-sd.ts, apps/api/src/modules/sd/application/commands/create-order.handler.ts, orders/drizzle-sd-order-departments.repo.ts

**48. Advance-percent default inconsistent (30% vs 70%)** (Area: SD / sotuv-buyurtma, SB-ids: SB0582, SB0586, SB0601, SB0622) — The avans gate is real (insert-time population + hard block), but `sd-quotations.repository.ts:213` hardcodes a `?? 30` fallback vs 70% everywhere else; live columns are all nullable/unmigrated defaults with all 13 legacy rows NULL. **Fix:** Replace the `?? 30` fallback with a shared `DEFAULT_ADVANCE_PERCENT=70` constant in `business.constants.ts`. Add `advance_percent: advanceRequired` to the INSERT in `queries-sd.ts:70-86`. Optionally backfill the 13 legacy rows (`advance_required=70, advance_status='no_advance' WHERE NULL`). **Files:** apps/api/src/modules/sd/infrastructure/repositories/sd-quotations.repository.ts, apps/api/src/common/database/queries-sd.ts, apps/api/src/common/constants/business.constants.ts

**49. Quotation→order conversion bypasses the CQRS/outbox pipeline** (Area: SD / sotuv-buyurtma, SB-ids: SB0585, SB0590, SB0597, SB0602, SB0627) — `convertQuotationToOrder` does a hand-rolled raw-SQL INSERT, never dispatching `CreateOrderCommand`, so no `OrderCreatedEvent`/outbox row fires for this path; also no PDF/versioning/14-day-deadline enforcement for quotations exist. **Fix:** Refactor `DrizzleSdQuotationsRepository.convertQuotationToOrder` to dispatch `CreateOrderCommand` via `CommandBus` instead of hand-rolling the INSERT (mirror how `deal-won.listener.ts` (CRM) already does this) — this also fixes the 30%/70% advance-default bug via `execSdSalesOrderInsert`'s correct default. Add `GET /sd/quotations/:id/pdf` reusing `sd-invoice-pdf.service.ts`'s pattern; add a scheduled/on-read check flagging quotations past their 14-day `valid_until` window. **Files:** apps/api/src/modules/sd/infrastructure/repositories/sd-quotations.repository.ts, apps/api/src/modules/sd/infrastructure/event-handlers/deal-won.listener.ts, sd/invoices/sd-invoice-pdf.service.ts

**50. No auto-invoice listener on billable-status transition** (Area: SD / sotuv-buyurtma, SB-ids: SB0593) — `INVOICEABLE_STATUSES` is defined but `CreateInvoiceCommand` is only dispatched manually from the controller. **Fix:** Add `@EventsHandler(OrderStatusChangedEvent)` listener `auto-invoice.listener.ts` (mirror `order-material-waiting.listener.ts`'s shape): when newStatus is in `INVOICEABLE_STATUSES` and no invoice exists yet, dispatch `CreateInvoiceCommand` with the order's line items. Register in `sd.module.ts`'s listeners array. **Files:** apps/api/src/modules/sd/application/commands/create-invoice.handler.ts, new apps/api/src/modules/sd/infrastructure/event-handlers/auto-invoice.listener.ts, sd.module.ts

**51. SD delivery not linked to WMS stock / no partial-qty field** (Area: SD / sotuv-buyurtma, SB-ids: SB0594, SB0625) — `DeliveriesService` publishes no domain event and calls no `EXTERNAL_OUT` movement; no quantity field means partial deliveries can't be represented. **Fix:** Publish `DeliveryCreatedEvent`/`DeliveryStatusChangedEvent` from `DeliveriesService.create()`/`updateStatus()`, mirroring the outbox-backed `OrderStatusChangedEvent` pattern. Add a WMS-side listener (shape of `OrderMaterialWaitingListener`) recording an `EXTERNAL_OUT` movement on delivery creation. Add a `quantity`/`delivered_quantity` column to the delivery-items table and update `sales_orders.delivery_status` to PARTIALLY/FULLY accordingly. **Files:** apps/api/src/modules/sd/deliveries/deliveries.service.ts, i-sd-deliveries.repo.ts / drizzle-sd-deliveries.repo.ts

**52. FG-warehouse storage-charge formula never computed** (Area: SD / sotuv-buyurtma, SB-ids: SB0605) — Columns (`storage_freedays`, `storage_tariff_per_m2`, `storage_accrued_amount` etc.) are fully modeled but zero application code computes them. **Fix:** Add a calculation (`accrued = max(0, daysSince(fgWarehouseEntryAt) - storageFreedays) * storageTariffPerM2 * storageTotalM2`) either as a daily cron (pattern: `financial-reports-monthly.cron.ts`) or on-read in the SD dashboard, persisting to `storage_accrued_amount`. Set `fg_warehouse_entry_at` from the WMS listener proposed for #51 once built. **Files:** lib/db/src/schema/sd-orders.ts

**53. RepeatOrderDialog (clone order) doesn't exist** (Area: SD / sotuv-buyurtma, SB-ids: SB0610) — No component under any name in the FE. **Fix:** Add a "Takrorlash" button next to the cancel button in `SDSalesOrders.tsx` (~lines 425-432) opening a dialog pre-filled with the selected order's customer/items (via existing `get-order-by-id.handler.ts`), calling `POST /sd/orders` with copied items — same "backend exists, wire the FE" pattern used for `SDLostOrders.tsx`. **Files:** artifacts/erp-dashboard/src/pages/SDSalesOrders.tsx

**54. SDOrderDetail standalone page doesn't exist** (Area: SD / sotuv-buyurtma, SB-ids: SB0607, SB0614, SB0615, SB0616) — Only the side-panel inside `SDSalesOrders.tsx` exists; every sub-finding about tabs/history/maket-gate is moot until this page is built. **Fix:** Build `artifacts/erp-dashboard/src/pages/SDOrderDetail.tsx` with 4 tabs, all backed by existing data: (1) order fields via `get-order-by-id.handler.ts`; (2) Tarix via the existing `audit_logs` table filtered by entity/entity_id; (3) Maket/tech-checkpoint gate reusing `approve-tech-checkpoint.handler.ts` + existing `techBomApproved`/`techRoutingApproved`/`techCardApproved` fields; (4) line items via `sales_order_items`. Route at `/sd/orders/:id`, link from each row in `SDSalesOrders.tsx`. **Files:** new artifacts/erp-dashboard/src/pages/SDOrderDetail.tsx, apps/api/src/modules/sd/application/queries/get-order-by-id.handler.ts

**55. PII sub-endpoints guarded too broadly** (Area: Xavfsizlik / multi-tenancy, SB-ids: SB0721) — FE `RoleGate` masks salary/passport/bank data, but the backend endpoints are guarded by a class-level `@Roles(...HR_ROLES)` that includes plain 'MANAGER' — wider than the FE's `PII_VIEWER_ROLES` — so any manager can bypass the FE mask via direct API call. **Fix:** In `employees-compat-sub.controller.ts`, add narrower method-level `@Roles('HR_MANAGER','HR_SPECIALIST','SUPER_ADMIN','DIRECTOR')` decorators directly above `getPassport`/`createPassport`/`getBankAccounts`/`createBankAccounts`/`getSalaryHistory`/`createSalaryHistory`, matching `RoleGate.tsx`'s `PII_VIEWER_ROLES`. `RolesGuard` reads handler-level metadata before class-level, so this cleanly narrows just these routes. Optionally add an ownership bypass (compare `@CurrentUser().employeeId` to the `:id` param) so employees can still see their own record. **Files:** apps/api/src/modules/compatibility/employees-compat-sub.controller.ts, artifacts/erp-dashboard/src/components/RoleGate.tsx

**56. Warehouse-rental fee calculation never computed** (Area: WMS / Ombor + POS Monitor, SB-ids: SB0533, SB0554) — CRUD/schema exist but `total_amount` is omitted on create and untouched on close. **Fix:** In `warehouse-rental.repository.ts`'s `closeRecord`, before UPDATE, SELECT `area_m2`/`daily_rate_per_m2`/`free_days`/`start_date`, compute `billableDays = GREATEST(0, (CURRENT_DATE - start_date) - free_days)` and `total_amount = billableDays * daily_rate_per_m2 * area_m2` in SQL, include in the same UPDATE. Add the same running-total expression to `getRecords()`/`getSummary()` for active records using `CURRENT_DATE`. **Files:** apps/api/src/modules/wms/infrastructure/repositories/warehouse-rental.repository.ts, apps/api/src/modules/wms/application/warehouse-rental.service.ts

**57. POS stock decrement floors at 0, no error on overdraw** (Area: WMS / Ombor + POS Monitor, SB-ids: SB0543) — `execCurrentStockDecrement` unconditionally floors via `GREATEST(0, quantity - qty)` with no pre-check, unlike WMS's own guarded issue path. **Fix:** Change `execCurrentStockDecrement` to a guarded conditional UPDATE (mirroring `execIssueFromWarehouseStock`): `... WHERE ... AND available_quantity >= qty RETURNING id`, returning the row or null. In `pos-movement-status.repository.ts`'s `decrementStock`, return `Err(AppErr('INSUFFICIENT_STOCK', ...))` when no row returned. In `pos-movement-status.service.ts`'s `_processCompletedMovement`, check the Result and abort completing the movement on failure instead of discarding it. **Files:** apps/api/src/common/database/queries-remaining-a.ts, apps/api/src/modules/pos/infrastructure/repositories/pos-movement-status.repository.ts, apps/api/src/modules/pos/application/services/pos-movement-status.service.ts

**58. Failed production QC doesn't trigger WMS quarantine/scrap action** (Area: WMS / Ombor + POS Monitor, SB-ids: SB0548, SB0556) — A full goods-receipt quarantine gate exists and works; but on production/FG QC failure, only a notification fires — no stock action. **Fix:** Add a new `QcFailedFgListener` in `wms/infrastructure/event-handlers/`, structured like `qc-passed.listener.ts` but for `QcFailedEvent`, writing a scrap/quarantine `wms_transactions` row (or a warehouse_stock entry in a quarantine warehouse) for the failed quantity. Register in `wms.module.ts` next to `QcPassedListener`/`MesCompletedFgListener`. **Files:** apps/api/src/modules/wms/infrastructure/event-handlers/qc-passed.listener.ts, apps/api/src/modules/qc/domain/events/index.ts, apps/api/src/modules/wms/wms.module.ts

**59. "Pres-kirim" fast-path quick-entry screen missing** (Area: WMS / Ombor + POS Monitor, SB-ids: SB0570) — Every building block (Web-Serial scale reading, barcode gen/print, movement create) exists separately, but not composed into a single kg→barcode→INTERNAL_IN flow. **Fix:** Add a "Pres-kirim" quick-entry mode reusing `PosQtyKeypad`'s Web-Serial weight capture (already in `PosMovementKirimBarcode.tsx`), chained into the existing `POST /api/pos/barcode/generate`+`print` calls and `CreateMovementSchema`/`POST /api/pos/movements`. Pure UI composition, no new backend plumbing. **Files:** artifacts/erp-dashboard/src/pos-monitor/pages/PosMovementKirimBarcode.tsx, apps/api/src/modules/pos/application/services/pos-movement.service.ts

**60. Movement photo-evidence column not wired into movement flow** (Area: WMS / Ombor + POS Monitor, SB-ids: SB0571) — `pos_movements.photo_evidence_url` exists live but nothing writes it for movements (only used on the unrelated `pos_shift_handovers`). **Fix:** Add optional `photoEvidenceUrl` field to `CreateMovementSchema` (mirroring `shift-handover.dto.ts`), thread through `pos-movement.service.ts`'s `createMovement` into the INSERT, add an upload/URL input to `PosMovementKirim.tsx`/`PosMovementChiqim.tsx` reusing the shift-handover upload pattern. **Files:** apps/api/src/modules/pos/dto/movement.dto.ts, apps/api/src/modules/pos/application/services/pos-movement.service.ts, apps/api/src/modules/pos/infrastructure/repositories/pos-movement.repository.ts

**61. Org-card color-token CSS regression** (Area: Frontend / dizayn-tizim / UX, SB-ids: SB0692) — A tokenization fix left `${baseColor}f0`-style hex-alpha string concatenation, which now produces invalid CSS since `baseColor` is a `var()` reference — breaking every org card's background/border/shadow. **Fix:** In `TreeNodeCard.tsx` lines 57-59, replace alpha-hex-suffix concatenation with CSS `color-mix()`, e.g. `color-mix(in srgb, ${baseColor} 94%, transparent)` (94/73/27% approximate the original f0/bb/44 alpha values). In `OrgStructureHierarchy.tsx` lines 211-215, replace 5 raw hex literals passed to `KpiCard`'s `color=` prop with matching tokens (`#1d4ed8→var(--ep-org-l1)` etc.). In `OrgNodeDetail.tsx:67`, change the `|| "#1d4ed8"` fallback to `|| "var(--ep-org-l1)"`. **Files:** artifacts/erp-dashboard/src/components/hr/org/TreeNodeCard.tsx, pages/OrgStructureHierarchy.tsx, pages/OrgNodeDetail.tsx

**62. Org pages don't use canonical EPPageHeader** (Area: Frontend / dizayn-tizim / UX, SB-ids: SB0685) — Org pages use the legacy `ModulePage` wrapper or hand-rolled header markup instead of the design-system `EPPageHeader`. **Fix:** Replace the bespoke header blocks in `OrgStructureHierarchy.tsx` and `OrgNodeDetail.tsx` with `<EPPageHeader title=... subtitle=... actions={...}/>`, following the same pattern already applied to `TechCards.tsx`/`DailyKPIDashboard.tsx` (SB0702). Swap `ModulePage` for `EPPageHeader` in `OrgDepartmentsPage.tsx` the same way. **Files:** artifacts/erp-dashboard/src/pages/OrgDepartmentsPage.tsx, OrgNodeDetail.tsx, OrgStructureHierarchy.tsx

**63. sd_customers soft-delete doesn't populate deleted_at/deleted_by** (Area: Master-data, SB-ids: SB0742) — Columns exist live but the delete code path only sets `status='deleted'`; the controller doesn't even accept `@CurrentUser()`. **Fix:** Change `execSdCustomerSoftDelete(cid, deletedBy?)` to also `SET deleted_at=NOW(), deleted_by=...`. Thread the param through `drizzle-sd-customers.repo.ts`'s `softDelete`, `sd-customers.service.ts`'s `softDelete`, and the controller's delete handler (add `@CurrentUser()`, already imported). Optionally align the WHERE clauses at lines 30/54/77 to also check `deleted_at IS NULL` once populated. **Files:** apps/api/src/common/database/queries-sd.ts, apps/api/src/modules/sd/infrastructure/repositories/drizzle-sd-customers.repo.ts, application/sd-customers.service.ts, presentation/sd-customers.controller.ts

**64. Material edit form uses manual validation, not zodResolver** (Area: Master-data, SB-ids: SB0756, SB0757) — Backend UPDATE endpoint + Zod DTO are real; FE form still validates with plain `if (!field.trim())` checks. **Fix:** In `MaterialCardsPage.tsx`, replace individual `useState` fields and manual trim-checks in `handleSave`/`handleUpdate` with react-hook-form + zodResolver, mirroring `CreateWarehouseMaterialSchema`/`UpdateWarehouseMaterialSchema` shape already in `warehouse-catalog.controller.ts`, per `FE_STANDARTLAR.md`. **Files:** artifacts/erp-dashboard/src/pages/MaterialCardsPage.tsx, apps/api/src/modules/compatibility/warehouse-catalog.controller.ts

**65. Dead MaterialsService/materials-table code** (Area: Master-data, SB-ids: SB0762) — `materials` table (0 rows) is read by a fully DI-wired `MaterialsService` with zero real consumers anywhere (confirmed dead code, not just a redundant table). **Fix:** Per project rule Q-46 (dead code must be removed, not left half-wired): delete `materials.service.ts`, `drizzle-materials-svc.repo.ts`, `i-materials-svc.repo.ts`, and remove their registration (import/provider/export + `MATERIALS_SVC_REPO` token) in `mm.module.ts`. Leave the empty `materials` DB table itself untouched (dropping tables needs owner approval per Q-35). **Files:** apps/api/src/modules/mm/materials/materials.service.ts, drizzle-materials-svc.repo.ts, i-materials-svc.repo.ts, apps/api/src/modules/mm/mm.module.ts

---

## Owner-Data Themes (grouped)

| # | Theme | Root causes | Areas |
|---|---|---|---|
| 1 | Card/karta attribute master-data empty (razryad_level_id, rbac_tier, salary_type/min/max, base_salary/payroll_rows, otdeleniye_no) | 8 | Auth/RBAC, Moliya/GL, Org-struktura, HR, Xavfsizlik |
| 2 | card_id / employee_cards occupancy incomplete, card-login-gate off, stake_fraction/is_acting defaults, 0 operator-role users | 7 | IoT, Org-struktura, MES, Xavfsizlik, HR |
| 3 | Org-chart manager/head-of-department linkage incomplete (head_user_id ~13% filled, no manager_id column) | 3 | Auth/RBAC, Org-struktura, HR |
| 4 | Vacancy tracking table completely empty (aging/SLA code ready, 0 rows) | 2 | Org-struktura, HR |
| 5 | ЦКП/OKR/governance target & config data empty (error_catalog rows, tskp_target/formula, OKR cascade linkage, council_members, workflow_rules) | 5 | CKP, Hisobot, Frontend/UX |
| 6 | Built pipelines with zero live transactional usage/adoption (ckp_fact_values, razryad_requests/history, 70%-advance→PP golden chain) | 3 | CKP, Razryad, Moliya/GL |
| 7 | IoT/telemetry & camera hardware master data absent (sensors registry, sensor readings/alerts, camera_ai_configs, shift-handover usage, energy — deliberate honest-501) | 6 | AI, IoT |
| 8 | PP capacity/work-center norma & department-linkage data incomplete (production_orders.org_department_id 0 filled; work_centers norma partial) | 2 | PP |
| 9 | LMS course-to-card binding empty (courses.card_id 0/5, cascades to enrollments) | 1 | LMS |
| 10 | QC instrument-calibration registry empty (schema ready, 0 instruments) | 1 | QC |
| 11 | SD credit-limit/debtor master data absent (nothing to gate advance-bypass against) | 1 | SD |
| 12 | Customer/material master-data gaps (customer segment/manager_id unfilled; material_layer_config/material_kind unset for corrugated materials) | 2 | Master-data |

**Total: 41 owner-data root causes.**

---

## Owner-Decision Themes (grouped)

### Cross-cutting (span multiple areas)
1. **Activate card-based permission system** — `CARD_PERMISSION_SOURCE_READY` hardcoded `false`, no `card_permissions` table (position ids 1-92 numerically collide with card ids 19-173, so naive reuse would fabricate false-allows) — 4 root causes — Auth/RBAC, Org-struktura, HR, Xavfsizlik.
2. **Flip CARD_LOGIN_GATE_ENABLED** — gate is fully computed but off by default; data coverage is now ~100% (was near-zero) — 2 — Auth/RBAC, HR.
3. **No field-level RBAC / PII masking** for salary, razryad, AI evaluation scores, confidential HR docs — 2 — Auth/RBAC, HR.
4. **Multi-tenancy rollout incomplete** — TenantFilterGuard+decorator built but never registered anywhere; only ~10 files/~35-40 tables tenant-aware; `users` has no tenant_id — 2 — Auth/RBAC, Xavfsizlik.
5. **Absence auto-block cron oversteps E1 human-confirm principle** — fully disables account on day-3 absence with no pre-approval gate, notifies HR only after the fact — 2 — AI, Auth/RBAC.

### Area-specific decisions
- **AI:** desktop JARVIS voice client is a separate out-of-repo app (SB0507/520); SD→PP→MES AI model call deliberately unwired pending EP-AI-085 governance (SB0516).
- **LMS:** onboarding-stage pay coefficients undefined, storage location undecided (SB0130); course-versioning/re-certification semantics undefined (SB0131).
- **Auth/RBAC:** acting-role (i.o.) cards have no scoped/reduced permission set (SB0198/219/229); new card creation has no 2-signature approval unlike razryad changes (SB0199/231).
- **CKP:** `ai_ckp_scores` dormant duplicate table — build vs. drop decision needed (SB0019/30/50); ЦКП hard-gate has no blocked→HR unlock override path, by design (SB0021); `company_tskp` orphaned table — wire up as lookup or drop (SB0052).
- **Hisobot/dashboard:** KPIs are company-wide only, not card-attributed (SB0371/388/393); dashboard disconnected from golden-thread event chain (SB0389, cross-cutting known issue); no Dept×Operation 2-axis nav (SB0376); 7-otdeleniye KPI aggregate never built/seeded (SB0407); no role-based dashboard-widget config/permission enforcement (SB0409/410).
- **Razryad:** exam_pass_threshold/max_retakes pending EP-ORG-055/056 (deliberately reverted after unapproved seed) (SB0763/770/781/786/792); no recurring re-certification/attestation scaffold (SB0774/790); no automatic razryad-decrease trigger, only manual (SB0795).
- **IoT (11 distinct, mostly "feature doesn't exist yet" decisions):** operator competency/razryad machine-access gate (SB0301); material sarf-norma↔MES integration (SB0303); norma-versioning RD-4 workflow (SB0309/327); downtime reason-code taxonomy mismatch, 11 EuroPrint vs 6 SAP codes (SB0307/337/353/364); AI auto-approval gate for GSD/MES review (SB0328); Andon shop-floor board doesn't exist (SB0330); card_activity_logs/A-System integration deferred (SB0340); camera cross-check (report vs AI) service (SB0341); energy cost never auto-posted to GL (SB0359); camera safety-violation conflict-resolution workflow (SB0362); cross-machine material-loss tracking, gofra 3% threshold (SB0367).
- **Moliya/GL:** 4-account revenue-split only manual-triggered, not on real cash-inflow events (SB0799/809); fuel/transport costing entirely undefined (SB0821); ЦКП deadline spec choice 16h vs 3h undecided (SB0800); `entries` debit/credit account-id varchar/integer schema-type drift, ripples ~5 files (SB0833).
- **PP:** production_orders assignee columns (manager/supervisor/qc_inspector) have no FK, not card-redesigned (SB0234); qc_gate not enforced as a blocking transition (SB0238); no split-order/partial-delivery logic, all-or-nothing batches (SB0265/275); AI-approval chain for release is bypassable by SUPER_ADMIN (SB0273).
- **Org-struktura:** org-tree fragmented + 3-way parallel org base (org_departments/org_functions/departments all populated) (SB0149/187); multi-card stake-share ratio has no real data source, always defaults to 1.0 (SB0160/174); cross-department `workflow_rules` mechanism built but zero rules ever defined (SB0167/183 — same table flagged as owner-data from the Frontend area's audit).
- **CRM (13 distinct):** no card_id on any CRM entity linking into KARTA model (SB0629/633/637); no unified "corporate number" identity model (SB0630); credit_limit/open_debt/is_blocked columns exist but nothing gates on them (SB0631); no takeover mechanism for a departed/on-leave manager's existing book (SB0632); funnel stages not mapped to Vysotskiy-7 org-level approval (SB0634); "papka" (folder) grouping model doesn't exist (SB0636); no field-visit/mobile GPS check-in workflow (SB0643/660/672); `crm_proposals` has no approval workflow (SB0649/658); no CRM↔MM/SD pricing/quotation integration (SB0650/676); lead→deal conversion has no sales-rahbari approval gate (SB0661); no discount/compensation abuse-flag criteria (SB0664); stage transitions have no prerequisite-artifact checks (klishe/STP/pricing/contract) (SB0670); `crm_companies` ABC-scoring columns never computed — duplicate of SD's own ABC calc, a two-world split (SB0674).
- **QC:** no parent_order_id FK for rework-cycle lineage (SB0458); no Cost-of-Quality table/service (SB0462); no CAPA entity/Kanban linkage (SB0464); no QC-outcome-specific payroll gate distinct from the general ЦКП gate (SB0468).
- **MES:** no version/effective-date on routing-level tables, only one level up on technology_cards (unapproved) (SB0421/442); no material-norm auto-calculation/approval logic (SB0413); no standing brigade/team master table, only ad hoc per-session crew (SB0427).
- **SD:** two parallel unreconciled order-status vocabularies (real 15-state machine vs. schema's 23-state `master_status`, 100% NULL, never written) (SB0587/620/624); no klishe/die ownership-and-retention asset registry (SB0595/604).
- **Xavfsizlik:** RBAC granularity standard inconsistent — module:LEVEL everywhere except POS's ~20 dotted action-keys (SB0722).
- **WMS/POS:** karta-model (razryad) not integrated into POS/WMS RBAC (SB0535/577/562); MES→WMS automatic material-issue-per-norma link missing, only manual REST dispatch (SB0538/555); PWA has no true Background Sync API, only foreground push/pull (SB0575).
- **Master-data:** multi-master materials overlap (mm_materials/raw_materials/materials/products vs. canonical material_cards) — explicitly logged "EGASI QARORI KERAK" (SB0733/740/747); material/customer business-key code standard inconsistent, customer_code generator missing (SB0735/739); no link between Technology/PP material specs and MES execution data — structural gap (SB0749); Customer-360 has 10 flat tabs, no grouping — UX decision (SB0758); `units` table is a confirmed true orphan — wire up or drop (SB0761).

**Total: 82 owner-decision root causes** (12 folded into the 5 cross-cutting themes, 70 area-specific).

---

## Now-Resolved Counts

| Area | Now-resolved |
|---|---|
| Golden-thread / event / oqim | 5 |
| AI (per-karta + planning) | 5 |
| LMS / Darslik | 7 |
| Auth / RBAC / login | 3 |
| CKP / maqsad / KPI | 6 |
| Hisobot / dashboard / analitika | 7 |
| Razryad / malaka / o'sish | 4 |
| IoT / telemetriya | 7 |
| Moliya / GL / kassa | 7 |
| PP / ishlab-chiqarish reja | 11 |
| Org-struktura / KARTA-markazlilik | 3 |
| HR / Xodim | 6 |
| CRM | 6 |
| QC / sifat | 8 |
| MES / sex / ish-sessiya | 3 |
| SD / sotuv-buyurtma | 5 |
| Xavfsizlik / multi-tenancy | 2 |
| WMS / Ombor + POS Monitor | 16 |
| Frontend / dizayn-tizim / UX | 4 |
| Master-data | 1 |
| **Total** | **116** |

**Noteworthy resolutions** (material system-capability changes, not just doc drift):
- **GL two-worlds P0 closed**: POS GL postings previously landed only in `pos_gl_postings`, never the canonical `entries` ledger; `AutoGlPostingService` now mirrors every posting into `entries` too (Moliya, SB0817/820).
- **Cashier payout approval chain built**: payroll→cashier payout now a real 4-stage chain (ai_checked→hr_approved→finance_approved→director_approved), cashier-PIN gated (Moliya, SB0797/806).
- **Kassir X/Z report + shift reconciliation fully built**, including a daily automated Z-report cron (Moliya, SB0801).
- **VAT rate de-hardcoded**: 12% VAT now read from the `settings` table at runtime with a named fallback (Moliya, SB0808).
- **Period lock enforced** at GL-posting write-time, not just a flag (Moliya, SB0810).
- **employee_cards coverage went from ~1 row to 31/31 (100%)** of non-exempt users, driven by a new onboarding-probation→auto-card-assignment flow (HR, SB0079/100, SB0072/101) — this is the data precondition several "flip the flag" owner-decisions above are now waiting on.
- **Hardcoded bcrypt hash for password `test123`** was found embedded in a shipped migration file — since removed (Auth, SB0210).
- **IotGateway WebSocket namespace was dead code** (never registered as a NestJS provider, blocking any live Andon-style push) — now fixed (IoT, SB0315/358).

---

## Skip-Low-Value Counts

| Area | Skip-low-value |
|---|---|
| Golden-thread / event / oqim | 6 |
| AI (per-karta + planning) | 10 |
| LMS / Darslik | 7 |
| Auth / RBAC / login | 6 |
| CKP / maqsad / KPI | 9 |
| Hisobot / dashboard / analitika | 7 |
| Razryad / malaka / o'sish | 6 |
| IoT / telemetriya | 14 |
| Moliya / GL / kassa | 9 |
| PP / ishlab-chiqarish reja | 15 |
| Org-struktura / KARTA-markazlilik | 5 |
| HR / Xodim | 7 |
| CRM | 9 |
| QC / sifat | 11 |
| MES / sex / ish-sessiya | 11 |
| SD / sotuv-buyurtma | 9 |
| Xavfsizlik / multi-tenancy | 4 |
| WMS / Ombor + POS Monitor | 17 |
| Frontend / dizayn-tizim / UX | 12 |
| Master-data | 3 |
| **Total** | **177** |

---

## Totals

| Category | Count |
|---|---|
| Fixable-now | 65 |
| Owner-data | 41 |
| Owner-decision | 82 |
| Skip-low-value | 177 |
| **Total distinct root causes (20 areas)** | **365** |

No exact duplicate sbIds were found across the 65 fixable-now items (each sbId appears in exactly one area's fixable-now bucket); a few pairs are topically related but address distinct gaps and were kept separate (e.g. IoT's OEE-formula-correctness item #18 vs. MES's OEE-grouping-levels item #46; the `workflow_rules` table appears once as an owner-data item and once as an owner-decision item from two different areas' independent audits, noted above rather than merged since the categories genuinely differ in framing).
agentId: a4e5389075278aab1 (use SendMessage with to: 'a4e5389075278aab1', summary: '<5-10 word recap>' to continue this agent)
<usage>subagent_tokens: 241849
tool_uses: 26
duration_ms: 729855</usage>