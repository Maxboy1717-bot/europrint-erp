# SAP-AUDIT 031–045 — Finance (reports/tax/payroll), HR (to'liq), LMS, Security

> READ-ONLY tahlil · 2026-06-06 · 5-tomonlama + SAP-taqqos
> Manba: jonli kod (`apps/api/src/modules/`) + DB (`_audit/q.cjs`) + FE (`artifacts/erp-dashboard/src/pages/`)
> ⚠️ DB = `europrint` (qurilish bosqichi): seed bor (employees=30, users=31, accounts=42, daily_reports=3150, candidates=11, enps_responses=19, pip_plans=4, offboarding_cases=3, visitor_log=4), lekin ko'p jadval bo'sh. `pg_stat_user_tables.n_live_tup` STALE (0 ko'rsatadi) → har doim `count(*)` bilan tasdiqlandi.

---

## [031] Finance reports/loans

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| trial-balance | reports.controller.ts:34 → drizzle-reports.repo.ts:18 (real Drizzle, accounts╳entries) | entries=0, accounts=42 | FinanceDashboard/FinanceReports | smoke yo'q (reports) | balans bo'sh (entries=0) | REAL (bo'sh) |
| profit-loss | drizzle-reports.repo.ts:37 (entries╳accounts, revenue−expense) | entries=0 | FinanceDashboard | — | net=0 (data yo'q) | REAL (bo'sh) |
| weekly/monthly-summary | drizzle-reports.repo.ts:57,73 (daily_financial_metrics) | daily_financial_metrics=0 | FinanceDashboard | — | bo'sh | REAL (bo'sh) |
| kpi-dashboard | drizzle-reports.repo.ts:89 (financial_kpis) | financial_kpis=0 | DailyKPIDashboard | smoke bor | bo'sh | REAL (bo'sh) |
| financial-reports/* (kassa/ombor/debitorlar/balans) | financial-reports.controller.ts:42-130 (FinancialReportsQueryService real) | — | FinanceDashboard | — | real query, sof DB | REAL |
| **GET /finance/loans** | finance-main.controller.ts:150-153 `notImplemented()` | loans jadval YO'Q | — | — | 501 | **501-STUB** |
| GET /finance/reports | finance-main.controller.ts:104-107 `notImplemented()` | — | — | — | 501 | **501-STUB** |

### SAP-taqqos
SAP FI'da Trial Balance / P&L / Balance Sheet GL'ning real-time agregati; kredit/qarz (loans) = TRM yoki alohida moduli. Bizda: trial-balance/P&L kodi REAL (entries╳accounts ustida), lekin entries=0 → hisobotlar bo'sh. **Loans umuman YO'Q** (501 + jadval yo'q). SAP-darajasi: hisobot mexanikasi yaxshi (DB-driven), lekin GL'da yozuv yo'q + loans 0.

### Muammolar
1. trial-balance `entries.debitAccountId = accounts.id::varchar` cast (drizzle-reports.repo.ts:25) — entries.account string, accounts.id int; ishlaydi lekin GL ikki-dunyo (`entries` canon ╳ `gl_journal_entries` unused) muammosini meros qiladi.
2. GET /finance/loans = 501, jadval ham yo'q → FE loans sahifasi bo'sh qoladi.
3. GET /finance/reports = 501 (dublikat — `financial-reports/*` real ishlaydi).

### 3-qator xulosa
Hisobot mexanikasi (trial-balance, P&L, balans, KPI) REAL Drizzle bilan yozilgan va sof; ammo `entries`/`daily_financial_metrics`/`financial_kpis` = 0 → hammasi bo'sh ko'rinadi.
Loans funksiyasi to'liq YO'Q (501 + jadvalsiz). Reports listing dublikat 501.
Verdikt: reports REAL-lekin-bo'sh, loans 501.

---

## [032] Finance tax/salary

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| GeneralTaxService (VAT INCLUSIVE/EXCLUSIVE) | finance/application/general-tax.service.ts:75-153 (tax_rate_config'dan o'qiydi) | **tax_rate_config jadval YO'Q** (✗ exists) | — (HTTP route yo'q) | — | runtime'da loadActiveRate CRASH | **ORPHAN** (route yo'q + jadval yo'q) |
| fi/tax/general-tax.service.ts | fi/tax/general-tax.service.ts:5 (re-export shim) | — | — | — | shim | DUPLICATE (shim) |
| POST /payroll/calculate-tax (INPS8+JSHD12) | payroll-periods.controller.ts:64-74 (hardcoded TAX_RATE=12, PENSION=8, controller ichida hisob) | DB yozmaydi | PayrollAutomation/FinanceDashboardPayrollTab | smoke (Payroll) | qaytaradi lekin saqlamaydi | **GREEN-LIE** (echo-calc) |
| GET /payroll/tax-summary | finance-payroll.controller.ts:43 → service→repo.taxSummary | payroll_* = 0 | FinanceDashboardPayrollTab | — | bo'sh | REAL (bo'sh) |
| GET /finance/payroll/tax-calendar | finance-extended-payroll.controller.ts:96 | — | — | — | ko'rilmadi (deferred) | (tekshirilmadi) |

### SAP-taqqos
SAP'da soliq = tax procedure (TAXUZ) condition-based, VAT/INPS/JSHD avtomatik posting bilan; payroll soliqlari PY moduli. Bizda IKKI ZID FALSAFA: (a) GeneralTaxService VAT'ni DB-config'dan to'g'ri hisoblaydi — lekin `tax_rate_config` jadval yo'q + hech qaysi controller chaqirmaydi (o'lik); (b) `calculate-tax` INPS/JSHD'ni controller ichida hardcode qiladi (Qoida 12 buzilishi) va saqlamaydi; (c) qolgan payroll esa "gross-only, soliq 1C'da" deydi (finance-extended-payroll.service.ts:18). Uchta nuqtai nazar bir-biriga zid. SAP-darajasi past — soliq yagona, izchil, persistent emas.

### Muammolar
1. `tax_rate_config` jadval DB'da YO'Q (`q.cjs` → "отношение не существует") → GeneralTaxService.loadActiveRate ishga tushsa CRASH; lekin uni hech kim chaqirmaydi → ORPHAN.
2. `calculate-tax` (payroll-periods.controller.ts:68-69) magic number 12/8, controller ichida hisob (Qoida 6+12), DB'ga yozilmaydi → GREEN-LIE.
3. "gross-only (1C)" ╳ "calculate-tax (12%/8%)" ╳ "GeneralTaxService (config)" = 3 ta nomuvofiq soliq yondashuvi; FE qaysi biriga ishonadi — noaniq.

### 3-qator xulosa
Soliq mantig'i parchalangan: GeneralTaxService o'lik (jadvalsiz, routsiz), calculate-tax hardcoded echo (saqlamaydi), HR/payroll esa gross-only deydi.
Hech bir yo'l soliqni real DB'ga persist qilmaydi.
Verdikt: tax ko'p qismi ORPHAN/GREEN-LIE; professional ERP soliq moduli darajasidan uzoq.

---

## [033] Finance payroll-posting (balans)

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| PayrollClosureService.buildJournal (balansli debit/credit) | hr/payroll/payroll-closure.service.ts:117-141 (sof domain, balans tekshiruvi >0.5) | — | — | spec ehtimol bor | balansli jurnal quradi | REAL (domain) |
| closePeriod → insertGlJournalLines | hr/payroll/payroll.service.ts:55-106 + drizzle-hr-payroll.repo.ts:87-115 | gl_journal_entries=0 | FinanceDashboardPayrollTab | — | yozadi LEKIN buzuq | **GREEN-LIE (semantik)** |
| GL insert: debit==credit account | drizzle-hr-payroll.repo.ts:101-103 (`debit_account: line.account, credit_account: line.account`) | — | — | — | qo'sh-yozuv buziladi | **BUG** |
| POST /hr/payroll/:id/post-to-gl | hr-payroll.controller.ts:133-142 (faqat status='paid', GL yozmaydi) | — | — | — | nom yolg'on | **GREEN-LIE** |
| FinanceExtendedPayroll.calculate (real INSERT) | finance-extended-payroll.service.ts:177-204 (payroll_calculations INSERT .returning) | payroll_calculations=0 | CalculatePayrollDialog | — | real saqlaydi | REAL (bo'sh) |
| runPayroll (batch, idempotent) | finance-extended-payroll.service.ts:369-417 | payroll_contracts=0 | PayrollAutomation | smoke | real, idempotent | REAL (bo'sh) |
| finance/payroll calculatePeriod | finance/payroll/payroll.service.ts:53-78 (in-memory hisob, faqat status update — rows persist YO'Q) | — | FinanceDashboardPayrollTab | — | qator-calc saqlanmaydi | **GREEN-LIE (qisman)** |

### SAP-taqqos
SAP PY'da posting run → balansli FI hujjat (Dr xarajat / Cr majburiyat) gl_account'larga, qat'iy debit=credit. Bizda buildJournal balansni TO'G'RI quradi (sof domain), lekin repo uni `gl_journal_entries`ga yozayotganda HAR satr uchun `debit_account == credit_account` qo'yadi (drizzle-hr-payroll.repo.ts:101-103) → qo'sh-yozuv yo'qoladi, har satr o'z-o'ziga Dr/Cr. Bundan tashqari `gl_journal_entries` = UNUSED GL model (canon = `entries`/`gl_entries` per memory) → posting noto'g'ri jadvalga tushadi. `post-to-gl` endpoint umuman GL yozmaydi (faqat status). SAP-darajasi: domain balans mantig'i bor, lekin persistence buzuq + noto'g'ri ledger → real double-entry EMAS.

### Muammolar
1. **drizzle-hr-payroll.repo.ts:101-103** — `debit_account: line.account, credit_account: line.account` har satr bir xil hisob ikki tomonda → buildJournal'ning balansli Dr/Cr semantikasi YO'Q bo'ladi (dalil: kod).
2. `gl_journal_entries` ga yozadi (=0); memory bo'yicha canon GL = `entries`/`gl_entries` → posting parallel/o'lik modelga tushadi (ikki-GL-dunyo).
3. `POST /hr/payroll/:id/post-to-gl` (hr-payroll.controller.ts:133) faqat `status='paid'` — GL jurnal YOZMAYDI, ammo nomi "post-to-gl" → GREEN-LIE.
4. finance/payroll `calculatePeriod` (payroll.service.ts:58-66) net'ni in-memory hisoblaydi, payroll_rows'ga qayta yozmaydi → keyingi o'qishda calc yo'qoladi.

### 3-qator xulosa
Payroll-balans domain mantig'i (buildJournal) to'g'ri va balansli, lekin DB-persist bosqichida debit==credit qilib yoziladi → qo'sh-yozuv buziladi va unused GL jadvaliga tushadi.
post-to-gl endpoint nomiga qarshi GL yozmaydi (GREEN-LIE); calculate/runPayroll esa REAL (lekin bo'sh).
Verdikt: posting "yashil lekin noto'g'ri" — Q-40 ning klassik holati.

---

## [034] HR employees

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| CRUD (list/get/create/update/delete) | hr-employees.controller.ts:103-147 (POST/PUT/PATCH/DELETE) | employees=30, users=31 | Employees.tsx | Employees.smoke.test.tsx | real CRUD | REAL |
| status PATCH | hr-employees.controller.ts:132 | — | Employees | smoke | real | REAL |
| documents POST/DELETE | hr-employees.controller.ts:180,205 | — | EmployeeProfile/EmployeeFilesPage | — | real | REAL |
| salary-review POST | hr-employees.controller.ts:216 | salary_history=0 | EmployeeProfile | — | real | REAL (bo'sh) |
| dashboard-stats | hr-dashboard.controller.ts:122-134 (employees COUNT) | 30 | HRDashboard | smoke | real | REAL |
| employees.manager_id | (memory) 0/30 NULL | 30 | — | — | manager bog'lanmagan | **DATA-GAP** |

### SAP-taqqos
SAP HCM PA (Personnel Administration): infotype-based, hire-to-retire. Bizda employees CRUD to'liq (12 route), users↔employees link backfill qilingan (memory). Yetishmaydi: `manager_id` 0/30 NULL (org-ierarxiya buzuq), infotype/effective-dating yo'q. SAP-darajasi: yarim — CRUD ishlaydi, lekin org-hierarchy + tarix yo'q.

### Muammolar
1. `employees.manager_id` 0/30 NULL (memory tasdiqlangan) → MANAGER_OF_SENDER va boshqaruv ierarxiyasi ishlamaydi.
2. salary_history=0 — salary-review yozadi lekin data yo'q.
3. employees.user_id backfill self-heal'ga bog'liq (OrgStructureService.onModuleInit) — fragil.

### 3-qator xulosa
Employees moduli backend CRUD jihatidan REAL va FE+smoke test bilan; eng to'liq HR bo'lim.
Asosiy nuqson — manager_id 0/30 NULL (org ierarxiya) va effective-dating/infotype yo'q.
Verdikt: REAL, SAP-yarim (hierarchy gap).

---

## [035] HR daily-report

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| POST submit | daily-report.controller.ts:48 → repo:22 (INSERT hr_daily_reports) | daily_reports=3150 | DailyReportPage.tsx | DailyReportPage.smoke.test.tsx | real saqlaydi | REAL |
| PATCH override | daily-report.controller.ts:64 → repo:49 (UPDATE+audit insert) | hr_daily_report_audit | DailyReportPage | smoke | real | REAL |
| stats / by-date / employee / department | daily-report.controller.ts:75-151 (real runQuery) | 3150 | DailyReportPage | smoke | real | REAL |
| hr_daily_reports = VIEW | pg_views: `SELECT ... FROM daily_reports` (avto-updatable) | — | — | — | INSERT view→base lands | REAL (view ✓) |
| /hr/daily-reports* (dashboard) | hr-dashboard.controller.ts:167-183 (stats qaytaradi, reportlar emas) | — | — | — | chalg'ituvchi | **DUPLICATE** |

### SAP-taqqos
SAP'da kunlik ishlab chiqarish hisoboti = PP confirmations (CO11N) yoki CATS time-sheet. Bizda `hr-v2/daily-reports` REAL: INSERT→daily_reports (3150 qator jonli!), HR override + audit trail bor. SAP-darajasi: yaxshi — eng "to'la" HR feature (real data bilan).

### Muammolar
1. `hr_daily_reports` VIEW, lekin repo unga INSERT qiladi (repo:22) — Postgres single-table view avto-updatable bo'lgani uchun ISHLAYDI va `daily_reports`ga tushadi (tasdiqlandi: pg_views definition). Bu xavfsiz, lekin nozik.
2. `/hr/daily-reports`, `/hr/daily-reports/my`, `/hr/daily-reports/department` (hr-dashboard.controller.ts:167-183) faqat stats qaytaradi (reportlar emas) → `hr-v2/daily-reports` bilan DUPLICATE + chalg'ituvchi.

### 3-qator xulosa
Daily-report eng pishiq HR feature: real INSERT/UPDATE, audit trail, 3150 jonli qator, FE+smoke.
hr_daily_reports VIEW orqali yoziladi (avto-updatable → daily_reports'ga tushadi) — ishlaydi.
Verdikt: REAL (ishlab turibdi); faqat /hr dashboard'dagi dublikat stub chalg'ituvchi.

---

## [036] HR dashboard

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| birthdays/milestones/abc/alerts/discipline | hr-dashboard.controller.ts:25-68 (svc, real) | employees=30 | HRDashboard.tsx | HRDashboard.smoke.test.tsx | real | REAL |
| dashboard-stats | :122 (employees COUNT) | 30 | HRDashboard | smoke | real | REAL |
| enps/surveys/results, 360/reviewable, hrc-tests/* | :234-349 (real db.execute) | bo'sh | HRDashboard | smoke | real query | REAL (bo'sh) |
| **documents/employee, /my, /pending** | :306-319 `return { items: [], total: 0 }` | — | HRDashboard | — | soxta bo'sh | **GREEN-LIE** ×3 (Qoida 10) |
| **birthdays/settings (GET/POST/GET:id)** | :269-284 `NOT_IMPLEMENTED` | — | HRDashboard | — | 501 | **501-STUB** ×3 |
| hr-dashboard-extra: contracts | hr-dashboard-extra.controller.ts:89 `notImplemented()` | — | — | — | 501 | **501-STUB** |
| hr-capital: courses, stats | hr-dashboard-extra.controller.ts:111,118 `notImplemented()` | — | — | — | 501 | **501-STUB** ×2 |

### SAP-taqqos
SAP'da HR dashboard = SAP Fiori "My Team"/Manager Insights, real-time analytics. Bizda asosiy kartalar REAL (birthdays, abc, alerts, daily-report stats, employees count). Lekin documents=fake bo'sh (×3), birthdays/settings=501 (×3), contracts/hr-capital courses=501 (×3). SAP-darajasi: yarim — ko'rinishi to'la, lekin ~9 endpoint stub/soxta.

### Muammolar
1. `documents/employee|my|pending` → `{ items: [], total: 0 }` (hr-dashboard.controller.ts:306-319) — Qoida 10 buzilishi, FE hujjatlar doim bo'sh ko'radi.
2. `birthdays/settings` 3 endpoint 501 → tug'ilgan kun sozlamasi saqlanmaydi.
3. `hr-capital/courses` 501 dublikat — LMS `/lms/courses` REAL bor (DUPLICATE intent).

### 3-qator xulosa
HR dashboard yadrosi REAL (real DB agregatlar, FE+smoke), lekin ~9 endpoint soxta-bo'sh (documents ×3) yoki 501 (settings ×3, contracts/hr-capital ×3).
documents/* GREEN-LIE foydalanuvchini chalg'itadi (doim bo'sh).
Verdikt: yarim — ko'rinish to'la, ichi qisman teshik.

---

## [037] HR vacancies/recruiter

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| GET/POST vacancies | hr-vacancies.controller.ts:81-189 (svc real, create→DB) | vacancies=0 | RecruitingKanban | RecruitingKanban.smoke.test.tsx | real CRUD | REAL (bo'sh) |
| **createVacancy salary/type/deadline tashlanadi** | hr-vacancies.controller.ts:179-187 (izoh: "NOT persisted") | — | — | — | FE maydonlar yo'qoladi | **DATA-DRIFT** |
| publish/channel-status/telegram-announce | :194-222 (recordFunnelHistory real) | hr_candidate_funnels=11 | — | — | funnel history yozadi | REAL |
| patch channels/portret | :228-269 (UPDATE hr_vacancy_profiles jsonb) | hr_vacancy_profiles=0 | — | — | real upsert | REAL (bo'sh) |
| **market-analysis GET/POST** | :145-251 (mavjudni qayta o'qiydi, POST yangi hisob YO'Q) | — | — | — | POST hech narsa hisoblamaydi | **GREEN-LIE** |
| recruitment-offers | recruitment-offers.controller.ts | hr_job_offers=0 | — | — | (tekshirilmadi) | — |

### SAP-taqqos
SAP E-Recruiting / SuccessFactors RCM: requisition→posting→pipeline→offer. Bizda vacancy CRUD + multi-channel publish + funnel history REAL, lekin: vacancies=0, salary/type/deadline saqlanmaydi (drift), market-analysis POST soxta. SAP-darajasi: yarim — skelet bor, ma'lumot va analitik to'liqmas.

### Muammolar
1. createVacancy salary/vacancy_type/deadline_working_days saqlamaydi (controller izohi:179) → FE forma maydonlari yo'qoladi (Q-43 forma-saqlash buzilishi).
2. POST market-analysis (`:245-251`) faqat mavjud analizni qaytaradi, yangi hisob/persist yo'q → GREEN-LIE.
3. vacancies=0 — funnel (candidates=11, funnels=11) bog'liq vakansiyalarsiz.

### 3-qator xulosa
Vacancy CRUD + publish + funnel history backend REAL va FE/smoke bor.
Lekin forma maydonlari (salary/type/deadline) saqlanmaydi va market-analysis POST soxta hisob.
Verdikt: REAL-yadro, jiddiy forma-drift + bitta GREEN-LIE.

---

## [038] HR pipeline

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| pipeline controller | hr-vacancies-pipeline.controller.ts (vacancies.controller.ts:61 export) | hr_candidate_funnels=11, candidates=11 | RecruitingKanban | smoke | funnel | REAL |
| recordFunnelHistory | hr-vacancies.service.ts (real INSERT funnel) | 11 | — | — | real | REAL |
| recruitment-funnel.service | recruitment-funnel.service.ts:49,206,231 `TODO H.9-FOLLOW-UP` (Funnel aggregate persist + gateway subscribe + quick-reject hali yo'q) | — | — | — | qisman | **PARTIAL** |

### SAP-taqqos
SAP RCM pipeline = candidate status funnel, stage gates. Bizda funnel history real yoziladi (candidates=11, funnels=11), lekin Funnel domain-aggregate persist + WS gateway subscribe + quick-reject 3 ta TODO bilan chala (recruitment-funnel.service.ts). SAP-darajasi: yarim — funnel ishlaydi, domain modeli to'liq ulanmagan.

### Muammolar
1. recruitment-funnel.service.ts:49 — Funnel.create() hidratsiya + persist hali YO'Q (TODO).
2. :206 — RecruitmentGateway subscribe ulanmagan (real-time pipeline yangilanmaydi).
3. :231 — isQuickRejected holatida funnel yozish chala.

### 3-qator xulosa
Pipeline funnel-history darajasida REAL ishlaydi (11 candidate/funnel jonli).
Domain-aggregate persistence + WS real-time + quick-reject 3 ta TODO bilan chala.
Verdikt: REAL-asos, PARTIAL chuqurlik.

---

## [039] HR compat/tests

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| POST /hr/360/review | hr-compat-a.controller.ts:82 → repo:45 (INSERT employee_360_assessments) | employee_360_assessments=0 | HRCapitalTests/HR360 | — | real | REAL (bo'sh) |
| employee-skills GET/POST | :114-130 (svc real) | bo'sh | — | — | real | REAL (bo'sh) |
| health-checkups GET/POST | :132-173 (real, flex schema) | bo'sh | HRHealthMonitoring | — | real | REAL (bo'sh) |
| hrc-tests/sessions, tool-test/questions | :175-183 (svc real) | test_questions=0 | HRCapitalTests | — | real | REAL (bo'sh) |
| hrc-tests question UPDATE/DELETE | :244-260 (UPDATE/DELETE test_questions) | 0 | HRCapitalTests | — | real | REAL (bo'sh) |
| hrc-tests/employee/:id/results | :264-269 (hr_tool_test_results) | hr_tool_test_results=0 | — | — | real | REAL (bo'sh) |
| discipline-records POST | :202-208 | bo'sh | — | — | real | REAL (bo'sh) |

### SAP-taqqos
SAP'da xodim baholash = SAP Performance & Goals; psixometrik test ERP'dan tashqarida bo'ladi. Bizda 360-review, IQ/tool-test, skills, health-checkups hammasi REAL DB CRUD bilan (lekin barcha jadval=0). SAP-darajasi: yarim — feature keng, ma'lumot yo'q, FE test (smoke) yo'q (faqat HRCapitalTests sahifa bo'lingan).

### Muammolar
1. Barcha test/assessment jadvallari 0 qator (test_questions, hr_tool_test_results, employee_360_assessments) → real lekin foydalanilmagan.
2. 360-review YOZISH compat-a'da (`POST /hr/360/review`), O'QISH feedback-360'da (`GET /360/feedback`) — ikki controller, lekin BIR XIL jadval (employee_360_assessments) → izchil (DUPLICATE controller emas, split).
3. `hr_360_feedback` jadval ishlatilmaydi (orphan duplicate).

### 3-qator xulosa
HR compat/tests qatlam REAL CRUD (360, skills, IQ-test, health, discipline) — barchasi haqiqiy DB yozadi/o'qiydi.
Lekin barcha jadval 0 qator + smoke test yo'q → ishlaydi-yu bo'sh.
Verdikt: REAL (bo'sh), `hr_360_feedback` orphan.

---

## [040] HR 360/enps

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| eNPS CRUD (create/close/launch) | enps.controller.ts:59-82 → repo:43 (INSERT enps_surveys) | enps_surveys=1 | HR eNPS sahifa | — | real | REAL |
| **eNPS respond** | enps.controller.ts:88-93 (`surveyId: 0` hardcoded!) → repo:78 | enps_responses=19 | — | — | javob survey'ga bog'lanmaydi | **BUG/GREEN-LIE** |
| 360 feedback (READ) | feedback-360.controller.ts:31-79 (faqat GET, real svc) | employee_360_assessments=0 | HR360 | — | o'qish | REAL (bo'sh) |
| 360 create | hr-compat-a.controller.ts:82 (yozish — alohida controller) | 0 | — | — | real | REAL (bo'sh) |
| auth-gate (PIP/eNPS) | (memory FIX1: @Roles HR-set, fail-open tuzatilgan) | — | — | — | himoyalangan | REAL |

### SAP-taqqos
SAP/SuccessFactors'da eNPS = pulse survey, 360 = Calibration/360 review. Bizda eNPS CRUD REAL (survey=1, responses=19 jonli), 360 read/write REAL. SAP-darajasi: yarim — survey ishlaydi, lekin respond bug + 360 bo'sh.

### Muammolar
1. **enps.controller.ts:89** — `respond` har doim `surveyId: 0` (izoh: "placeholder for now") → 19 ta javob survey'ga bog'lanmagan (orphan responses). RespondSchema'da survey_id yo'q. Bu GREEN-LIE: javob saqlanadi, lekin survey'ga ulanmaydi.
2. enps `respond` `enps_responses`ga yozadi, lekin dashboard `enps_survey_responses`'dan o'qiydi (hr-dashboard.controller.ts:343) → ikki javob jadvali (DUPLICATE), agregat noto'g'ri bo'lishi mumkin.
3. 360 assessment jadvallari 0.

### 3-qator xulosa
eNPS CRUD REAL va jonli data bor (survey=1, responses=19); 360 read/write REAL lekin bo'sh.
Asosiy bug: respond `surveyId:0` hardcode → javoblar survey'ga bog'lanmaydi + enps_responses╳enps_survey_responses ikki jadval.
Verdikt: REAL-yadro, bitta jiddiy respond bug.

---

## [041] HR career/offboarding

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| career-path CRUD (create/steps/progress) | career-path.controller.ts:64-120 (svc real) | career_paths=0 | HR career sahifa | — | real | REAL (bo'sh) |
| offboarding cases CRUD | hr-offboarding.controller.ts:34-121 (create/checklist/exit-interview/finalize/cancel) | offboarding_cases=3 | HRAlumni/Offboarding | — | real | REAL |
| exit-interview/finalize/cancel | :87-121 (svc real) | 3 | — | — | real | REAL |
| offboarding/questions | hr-dashboard.controller.ts:201 (offboarding_checklist_items) | — | — | — | real | REAL |

### SAP-taqqos
SAP'da career = Talent/Succession (career path, development plan); offboarding = SAP HCM separation workflow + exit interview. Bizda career-path REAL (ladder, steps, progress) lekin 0 qator; offboarding to'liq workflow REAL (3 case jonli, exit-interview, finalize, cancel). SAP-darajasi: offboarding yaxshi, career bo'sh.

### Muammolar
1. career_paths=0 — ladder/steps yaratilmagan (real lekin foydalanilmagan).
2. career-path `createdBy || 1`, `completedBy || 1` (controller:70,119) — fallback user=1 (auditni buzadi).

### 3-qator xulosa
Offboarding to'liq workflow REAL va jonli (3 case, exit-interview→finalize→cancel) — SAP-ga yaqin.
Career-path REAL CRUD lekin 0 qator + createdBy fallback=1.
Verdikt: offboarding REAL/kuchli, career REAL-bo'sh.

---

## [042] HR payroll/advances (PIN)

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| POST /finance/advances/request | finance-advance.controller.ts:59 → check-advance.handler.ts:30 (real recordAdvance INSERT) | advance jadvallar bo'sh | FinanceApproval | FinanceApproval.smoke.test.tsx | real avans cheki | REAL (bo'sh) |
| override / pending / list | finance-advance.controller.ts:69-90 (real) | — | FinanceApproval | smoke | real | REAL |
| advance limit (baseSalary×percent) | check-advance.handler.ts:35-44 (advance_percent setting'dan) | — | — | — | sozlanadigan limit | REAL |
| HR payroll calculate/approve/post | hr-payroll.controller.ts:68-142 (savePayroll real, post-to-gl status-only) | payroll bo'sh | PayrollAutomation | smoke | real (post-to-gl soxta) | REAL + 1 GREEN-LIE |
| idempotency keys | sd_advance_idempotency_keys jadval mavjud | — | — | — | takror-himoya | REAL (struktura) |
| "PIN" | (kod'da PIN-auth topilmadi — pin'lar false-positive: spinner/pinned) | — | — | — | — | YO'Q |

### SAP-taqqos
SAP'da avans = payroll advance / off-cycle payment, limit konfiguratsiya bilan. Bizda advance limit DB-setting'dan (advance_percent), real recordAdvance, override-with-reason, idempotency keys (Phase 4 70%-avans bilan bog'liq) — bu juda SAP-ga yaqin. "PIN" punkt nomidagi atama kod'da topilmadi (ehtimol PINFL/idempotency-key/avans-tasdiq nazarda tutilgan). SAP-darajasi: avans qismi yaxshi.

### Muammolar
1. "PIN" so'zi kod'da yo'q — punkt nomi noaniq (idempotency-key yoki avans-PIN tasdiq emas, balki PINFL/manzilli emas). Faraz: avans-approval gate.
2. HR `post-to-gl` (hr-payroll.controller.ts:133) GL yozmaydi (faqat status='paid') — [033] bilan bir xil GREEN-LIE.
3. advance jadvallari bo'sh.

### 3-qator xulosa
Advance moduli backend REAL va puxta: sozlanadigan limit, override+sabab, idempotency keys, real INSERT — SAP-ga yaqin.
HR payroll calculate/approve REAL; faqat post-to-gl GL yozmaydi (GREEN-LIE).
Verdikt: advances REAL/kuchli; "PIN" atamasi kod'da tasdiqlanmadi.

---

## [043] LMS courses

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| GET courses (list/get) | lms-courses.controller.ts:60-82 → drizzle-lms.repo.ts:69-75 (`courses` jadval) | courses=0 | Courses.tsx | Courses.smoke.test.tsx | real | REAL (bo'sh) |
| POST create | :87-107 → repo:86 (INSERT courses) | 0 | Courses | smoke | real | REAL (bo'sh) |
| PATCH (avval green-lie edi, tuzatilgan) | :113-131 (UPDATE courses COALESCE) | — | Courses | smoke | real | REAL |
| DELETE (soft is_active=false) | :137-145 → repo:146-148 | — | Courses | smoke | real | REAL |
| POST enroll | :150-165 → repo:125 (INSERT enrollments ON CONFLICT) | enrollments=0 | CourseDetail | smoke | real | REAL (bo'sh) |
| **lms_courses jadval** | (LMS repo `courses`ga yozadi, `lms_courses`ga EMAS) | lms_courses=0 | — | — | dublikat | **ORPHAN/DUP** |

### SAP-taqqos
SAP SuccessFactors Learning (LMS): course catalog, enrollment, completion. Bizda LMS courses CRUD + enroll REAL (`courses`+`enrollments` jadvallariga), lekin 0 qator. Yana `lms_courses`/`lms_*` jadval oilasi (lms_modules, lms_lessons, lms_exams...) repo tomonidan ISHLATILMAYDI → katta dublikat/orphan to'plam. SAP-darajasi: yarim — CRUD ishlaydi, ikki-jadval-dunyo (courses ╳ lms_courses) + data yo'q.

### Muammolar
1. LMS repo canon `courses`/`enrollments`/`certificates`'ga yozadi; `lms_courses`/`lms_certificates`/`lms_enrollments` (hammasi 0) ishlatilmaydi → ORPHAN dublikat oila (~20 lms_* jadval).
2. courses=0, enrollments=0 — katalog bo'sh.
3. Ikki controller: `lms-courses.controller.ts` (@Controller lms/courses) ╳ `courses.controller.ts` — potensial dublikat (alohida tekshirish kerak).

### 3-qator xulosa
LMS courses CRUD + enroll REAL (canon `courses`/`enrollments`), FE Courses.tsx + smoke bor.
Lekin butun `lms_*` jadval oilasi (≈20) o'lik dublikat — repo ularni ishlatmaydi; data 0.
Verdikt: REAL-yadro (bo'sh) + katta orphan lms_* to'plam.

---

## [044] LMS certificates/drift

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| POST issue | lms-certificates.controller.ts:55-74 → IssueCertificateCommand | certificates=0 | Certificates.tsx | Certificates.smoke.test.tsx | real | REAL (bo'sh) |
| GET operator/:id, :employeeId | :79-92 (queryBus + repo) | 0 | Certificates | smoke | real | REAL (bo'sh) |
| POST :id/revoke | :98-105 (updateCertificateStatus) | — | Certificates | smoke | real | REAL |
| POST check-mes (operator cert gate) | :110-116 (checkOperatorCertForMes) | — | — | — | MES bog'lanish | REAL |
| **drift: `certificates` ╳ `lms_certificates`** | repo `certificates`'ga yozadi; `lms_certificates`(0) ishlatilmaydi | both=0 | — | — | dublikat | **DRIFT/ORPHAN** |
| qc_certificates | alohida jadval (sifat sertifikatlari) | — | — | — | boshqa domen | — |

### SAP-taqqos
SAP'da kvalifikatsiya/sertifikat = Qualifications Catalog (HCM) yoki SF Learning certification, muddat + qayta-sertifikatlash. Bizda issue/revoke/check-mes REAL (`certificates` jadval), MES integratsiyasi (operatorni sertifikatsiz bloklash) bor — bu yaxshi. "Drift": `certificates` canon ╳ `lms_certificates` orphan ╳ `qc_certificates` (boshqa domen). SAP-darajasi: yarim — funksiya yaxshi, jadval drift + data yo'q.

### Muammolar
1. `lms_certificates` jadval (0) ishlatilmaydi — repo canon `certificates`'ga yozadi → DRIFT/ORPHAN.
2. certificates=0 — sertifikat berilmagan; check-mes har doim valid=false → operatorlar bloklanishi mumkin (yoki cert yo'qligi e'tiborga olinmaydi).
3. `certificates` ╳ `qc_certificates` ╳ `lms_certificates` — uch sertifikat jadval (master-data overlap).

### 3-qator xulosa
Certificate issue/revoke/check-mes REAL (canon `certificates`), MES-gate integratsiyasi bor, FE+smoke.
"Drift": lms_certificates orphan + qc_certificates alohida domen + data 0.
Verdikt: REAL-yadro (bo'sh), jadval-drift tasdiqlandi.

---

## [045] Security visitors

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| POST visitors (create) | security.controller.ts:263-279 (INSERT security_visitors) | security_visitors=0 | SecurityDashboard.tsx | SecurityDashboard.smoke.test.tsx | real | REAL (bo'sh) |
| GET visitors | :183-188 (SELECT security_visitors) | 0 | SecurityDashboard | smoke | real | REAL (bo'sh) |
| PATCH/POST visitors/:id/exit | :173-178, :253-259 (UPDATE exited_at) | — | SecurityDashboard | smoke | real | REAL (DUP route ×2) |
| POST incidents | :283-298 (INSERT security_incidents) | — | SecurityDashboard | smoke | real | REAL |
| POST ppe-checks | :302-310 (INSERT security_ppe_checks) | — | — | smoke | real yozadi | REAL |
| **GET ppe-checks/stats/violations, daily-summary, fire-sensors** | :220-247 `notImplemented()` ×5 | — | — | — | 501 | **501-STUB** ×5 |
| **visitor_log ╳ security_visitors** | controller `security_visitors`'ga (0), `visitor_log`(4) boshqa yozuvchi | sv=0, vl=4 | — | — | ikki jadval | **DUPLICATE** |

### SAP-taqqos
SAP'da tashrif/xavfsizlik EHS yoki alohida access-control tizimida (SAP EHSM / 3rd-party). Bizda visitor create/exit/list + incident + PPE-check yozish REAL. Lekin: 5 ta GET (ppe-checks/stats/violations, daily-summary, fire-sensors) = 501 → PPE yozsa ham O'QIB BO'LMAYDI (asimmetrik). Yana `visitor_log`(4) ╳ `security_visitors`(0) ikki jadval. SAP-darajasi: yarim — yozish ishlaydi, o'qish/analitika teshik + jadval dublikat.

### Muammolar
1. **Asimmetriya**: `POST ppe-checks` REAL INSERT (`:302`), lekin `GET ppe-checks`/`ppe-stats`/`ppe-violations` 501 (`:233-247`) → PPE ma'lumotini kiritasiz, ko'rolmaysiz.
2. `security_visitors`(0) ╳ `visitor_log`(4) — controller bo'shiga (security_visitors) yozadi, jonli 4 qator boshqa jadvalda (visitor_log) → DUPLICATE, FE qaysi birini ko'radi noaniq.
3. `visitors/:id/exit` ikki marta e'lon (POST :173 + PATCH :253) — dublikat route.
4. daily-summary, fire-sensors 501.

### 3-qator xulosa
Visitor/incident/ppe-check YOZISH REAL (security_visitors/incidents/ppe_checks INSERT), FE SecurityDashboard + smoke bor.
Lekin 5 GET 501 (PPE o'qilmaydi) + visitor_log(4)╳security_visitors(0) dublikat + exit route ×2.
Verdikt: REAL-yozuv, lekin o'qish/analitika yarim 501 + jadval dublikat.

---

## UMUMIY XULOSA (031–045)

**Eng kuchli (SAP-ga yaqin):** [035] daily-report (3150 jonli qator, audit trail), [041] offboarding (to'liq workflow + 3 case), [042] advances (sozlanadigan limit + idempotency), [034] employees (to'liq CRUD).

**Eng zaif:** [032] tax (GeneralTaxService orphan + jadval yo'q + calculate-tax hardcoded echo), [033] payroll-posting (debit==credit bug + unused GL jadval), [031] loans (501 + jadvalsiz).

**Takrorlanuvchi pattern (verify-don't-trust tasdiqlari):**
- "REAL lekin bo'sh" — aksariyat HR/LMS jadvallari 0 qator (qurilish bosqichi).
- GREEN-LIE (semantik) — payroll post-to-gl (status-only), GL debit==credit, market-analysis POST, enps respond surveyId:0, calculate-tax echo, hr-dashboard documents/*.
- DUPLICATE/ORPHAN jadval — gl_journal_entries╳entries, lms_*╳courses/certificates/enrollments, visitor_log╳security_visitors, hr_360_feedback, enps_responses╳enps_survey_responses, daily_reports VIEW oilasi.
- DATA-DRIFT (FE↔BE) — createVacancy salary/type/deadline tashlanadi.

**Soliq nuqtai nazari ZID (3 xil):** gross-only(1C) ╳ calculate-tax(12%/8% hardcode) ╳ GeneralTaxService(config) — yagona izchil soliq modeli yo'q.
