# 180-PUNKT SAP-AUDIT — Bo'lak 016 (punktlar 016–030)

> QAT'IY READ-ONLY tahlil. Har da'vo jonli kod (fayl:satr) + jonli DB (`_audit/q.cjs`) bilan tasdiqlangan.
> Jonli DB = `europrint` (qurilish bosqichi — ko'p jadval BO'SH; bu kod/struktura masalasi, migratsiya emas).
> Sana: 2026-06-06.

---

## [016] PP production-orders

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| GET/POST `pp/orders` (CQRS) | `pp/presentation/pp-orders.controller.ts:55,93` → `create-production-order.handler.ts:30` → `queries-pp.ts:execSavePo:12` | `production_orders`=0 | ERPProduction.tsx | yo'q | 3-checkpoint + BOM→MRP material-requirement; REAL INSERT | REAL |
| `ProductionOrdersService` (REST CRUD) | `pp/production-orders/production-orders.service.ts` + `drizzle-pp-production-orders.repo.ts` | `production_orders`=0 | — (hech qaysi controller chaqirmaydi) | yo'q | provider'da ro'yxatda, lekin HECH BIR controller inject qilmaydi | ORPHAN |
| `pp-planning.repository.ts` 2-yozuvchi | `pp/infrastructure/repositories/pp-planning.repository.ts:28` (`INSERT INTO production_orders`) | `production_orders`=0 | — | yo'q | uchinchi yozuv yo'li (raw SQL) | DUPLICATE |

- **DB:** `production_orders`=0, `production_order_operations`=0, `production_order_components`=0 (hammasi bo'sh — qurilish bosqichi).
- **Drift:** `production_orders_int` (`schema-ext-a-3.ts:31` = canonicalProductionOrders) VA orphan `productionOrders` (`schema-compat-3.ts:23`) — ikkalasi ham AYNAN `production_orders` jadvaliga ishora qiladi (jadval bir xil, kod yo'li uchta).

### SAP-taqqos
SAP PP'da production order = release→confirm→GR→backflush + costing + reservation. Bizda: 3-checkpoint gate + BOM→material-requirement bor (yaxshi), lekin confirm/backflush/costing yo'q, 3 ta parallel yozuv yo'li (1 REAL CQRS + 1 ORPHAN service + 1 raw repo). **SAP-darajasi: 🟡 yarim.**

### Muammolar
1. KOD↔KOD: `ProductionOrdersService` to'liq CRUD repo bilan yozilgan, providerda (`pp.module.ts:126`) ro'yxatda, lekin HECH BIR controller uni inject qilmaydi → o'lik kod (ORPHAN).
2. KOD↔KOD: 3 ta yozuv yo'li bir jadvalga (`execSavePo` ORM, `ProductionOrdersService.create` ORM, `pp-planning.repository.ts:28` raw SQL) — bir manba qoidasi buzilgan.
3. TEST: 0 ta spec.

### 3-qator xulosa
CQRS `pp/orders` yo'li REAL (checkpoint+MRP). `ProductionOrdersService` = to'liq yozilgan, lekin ulanmagan ORPHAN. Jadval bo'sh — confirm/backflush/costing yetishmaydi (SAP yarim).

---

## [017] MES sessions

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| `mes/production-sessions` (REST) | `mes-production-sessions.controller.ts` → `mes-production-sessions.repo.ts:34` (`INSERT INTO mes_sessions`) | `mes_sessions`=0 | MESExtended.tsx | yo'q | REAL INSERT mes_sessions + downtime_events | REAL |
| `mes/sessions` (CQRS) | `mes-sessions.controller.ts:38` → `start-session.handler.ts`/`complete-session.handler.ts` | `mes_sessions`=0 | MESExtended.tsx | yo'q | Start/Complete + tx + QC trigger | REAL |
| Record downtime | `mes-production-sessions.repo.ts:64` (`INSERT INTO downtime_events`) | `downtime_events`=0 | MES | yo'q | REAL INSERT | REAL |

- **DB:** `mes_sessions`=0, `mes_production_sessions`=0, `downtime_events`=0.
- **Drift:** IKKI controller (`mes/sessions` CQRS + `mes/production-sessions` REST) bir xil `MesProductionSessionsService`/`mes_sessions` ustida — qisman dublikat sirt.

### SAP-taqqos
SAP MES (ME/PP-PI): operation confirmation, time-ticket, downtime, scrap. Bizda: session start/complete/pause/resume + downtime + material-consumption bor. Yaxshi. Lekin xato yutilishi (catch→`[]`/`null`) muvaffaqiyatsizlikni yashiradi. **SAP-darajasi: 🟡 yarim (ikki controller dublikati).**

### Muammolar
1. KOD↔KOD: ikkita session controller (`mes/sessions` + `mes/production-sessions`) — endpoint sirt dublikati.
2. KOD: repo catch bloklari `[]`/`null` qaytaradi (`mes-production-sessions.repo.ts:28,42`) → INSERT muvaffaqiyatsizligi 200+null bilan yashirinadi (potensial GREEN-LIE faqat xato holatida).
3. TEST: 0 ta.

### 3-qator xulosa
MES sessions REAL — `mes_sessions`+`downtime_events`ga haqiqiy INSERT, complete→QC trigger. Ikki controller (CQRS+REST) bitta servis ustida — dublikat sirt. Jadval bo'sh.

---

## [018] MES facts/OEE

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| GET `mes/oee` | `mes-shifts-stats.controller.ts:80` → `mes-shifts-stats.repo.ts:55` | `production_sessions`=0 | MESExtended | yo'q | `AVG(ps.oee)` per equipment — REAL aggregat | REAL |
| GET `mes/stats` | `mes-shifts-stats.repo.ts:65` | `mes_production_sessions`=0 | MES | yo'q | active/completed/produced/defect_rate — REAL | REAL |
| MES→QC trigger | `complete-session.handler.ts:71` → `qc/mes-completed.listener.ts:28` (`INSERT INTO qc_inspections`) | `qc_inspections` | — | yo'q | ⭐ REAL (avval no-op edi, endi pending QC ochadi) | REAL |
| Gamification leaderboard | `mes-shifts-stats.repo.ts:78` | `gamification_points`=77 | MES | yo'q | REAL — JONLI ma'lumot (77 qator) | REAL |

- **DB:** `production_sessions`=0, `mes_production_sessions`=0, `production_facts`=0, `mes_telemetry`=0; lekin `gamification_points`=77 (JONLI).
- **⭐ MEMORY tuzatish:** "MES→QC no-op stub" da'vosi ESKI — `qc/mes-completed.listener.ts:28` endi REAL `INSERT INTO qc_inspections`.

### SAP-taqqos
SAP OEE = Availability×Performance×Quality (real timeticket'dan). Bizda OEE `production_sessions.oee` ustunini AVG qiladi — ya'ni OEE oldindan hisoblangan ustun, A×P×Q dekompozitsiyasi yo'q. **OEE manba jadvali drift:** OEE `production_sessions`dan o'qiydi, lekin sessiyalar `mes_sessions`/`mes_production_sessions`ga yoziladi → OEE hech qachon yangi sessiyani ko'rmaydi. **SAP-darajasi: 🟡 yarim.**

### Muammolar
1. KOD↔KOD drift: OEE `FROM production_sessions` (`repo:59`), session-write `INTO mes_sessions` (`repo:37`) — uch xil "sessiya" jadvali (production_sessions / mes_sessions / mes_production_sessions), OEE noto'g'ri jadvaldan o'qiydi.
2. SAP gap: OEE = oldindan-hisoblangan ustun AVG, real Availability/Performance/Quality dekompozitsiyasi yo'q.
3. TEST: 0 ta.

### 3-qator xulosa
OEE/stats SQL REAL, MES→QC trigger endi REAL (no-op TUZATILGAN). Lekin OEE `production_sessions`dan o'qiydi, sessiyalar `mes_sessions`ga yoziladi — uch-jadval drift. Faqat gamification (77) JONLI.

---

## [019] WMS inventory-materials

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| GET `/inventory/materials` | `inventory-materials.controller.ts:58` → `inventory-materials.repository.ts:21` (`FROM mm_materials ... JOIN wms_stock_levels`) | `mm_materials`=YO'Q, `wms_stock_levels`=YO'Q | WMSMaterials.tsx:103, WarehouseMaterial360.tsx | yo'q | jadval mavjud EMAS → catch→`[]` | GREEN-LIE |
| POST `/inventory/materials` | `inventory-materials.controller.ts:118` | — | WMSMaterials | yo'q | `throw NotImplementedException` (Stage 4.1) | 501-STUB |
| GET `/inventory/materials/:id/360-card` | `inventory-materials.service.ts:25` | `mm_materials`=YO'Q | WarehouseMaterial360 | yo'q | catch→bo'sh | GREEN-LIE |
| PUT/DELETE material | `inventory-materials.repository.ts:78,87` (`UPDATE mm_materials`) | `mm_materials`=YO'Q | WMSMaterials | yo'q | jadval yo'q → xato | GREEN-LIE |

- **DB ISBOT:** `to_regclass('public.mm_materials')`=**null**, `to_regclass('public.wms_stock_levels')`=**null** — JADVALLAR UMUMAN YO'Q. Kanonik `material_cards`=21, `warehouse_stock`=25.

### SAP-taqqos
SAP MM material master = MARA/MARC/MARD + valuation. Bizda `/inventory/materials` butun sahifa MAVJUD BO'LMAGAN jadvalga (`mm_materials`+`wms_stock_levels`) so'rov yuboradi → har doim bo'sh. Create = 501. Kanonik `material_cards` (21 qator JONLI) hech qachon ko'rsatilmaydi. **SAP-darajasi: 🔴 yo'q (sahifa o'lik jadvalga ulangan).**

### Muammolar
1. KOD↔DB: `inventory-materials.repository.ts` butunlay `mm_materials` + `wms_stock_levels` jadvallariga so'rov yuboradi — ULAR YO'Q (`to_regclass`=null). Har so'rov catch→`[]`/Err.
2. FE↔BE end-to-end GREEN-LIE: `WMSMaterials.tsx:103` `GET /api/inventory/materials` → BE 200 + bo'sh `{items:[],total:0}`, lekin `material_cards`da 21 material JONLI turibdi — foydalanuvchi bo'sh ro'yxat ko'radi.
3. POST = 501-STUB (material birlashtirilmagan, Stage 4.1 ga belgilangan).

### 3-qator xulosa
`/inventory/materials` butun sahifa MAVJUD EMAS jadvalga (`mm_materials`/`wms_stock_levels`) ulangan → har doim bo'sh (GREEN-LIE). Kanonik `material_cards`=21 ko'rinmaydi. Create=501. Eng yomon punkt.

---

## [020] WMS material-kits (drift)

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| GET `iot/material-kits` | `iot-material-kits.controller.ts:38` → `iot-enhanced.repository.ts:34` (`FROM mm_material_kits`) | `mm_material_kits`=YO'Q | WarehouseMaterialKits.tsx | yo'q | jadval yo'q → catch→Err | GREEN-LIE |
| POST `iot/material-kits` | `iot-enhanced.repository.ts:43` (`INSERT INTO mm_material_kits (production_order_id,...)`) | `mm_material_kits`=YO'Q | WarehouseMaterialKits | yo'q | jadval yo'q + ustun drift | GREEN-LIE |
| Prepare/Ready/Generate | `iot-enhanced.repository.ts:90` (`UPDATE mm_material_kits`) | `mm_material_kits`=YO'Q | WarehouseMaterialKits | yo'q | jadval yo'q | GREEN-LIE |

- **DB ISBOT:** `to_regclass('public.mm_material_kits')`=**null** (YO'Q). Kanonik `material_kits` MAVJUD (=0 qator), lekin ustunlari **boshqacha**: DB'da `order_id`, `kit_number`, `machine_task_id` — kod esa `production_order_id`, `prepared_by` yozadi (ustun drift).

### SAP-taqqos
SAP'da kitting / staging = WM transfer order + staging area. Bizda material-kit CRUD to'liq yozilgan, lekin (a) MAVJUD EMAS jadval `mm_material_kits`ga, (b) kanonik `material_kits` ustun nomlari mos kelmaydi (`order_id` ≠ `production_order_id`). **SAP-darajasi: 🔴 yo'q (jadval + ustun ikki tomonlama drift).**

### Muammolar
1. KOD↔DB jadval drift: kod `mm_material_kits` (YO'Q), DB kanonik `material_kits`.
2. KOD↔DB ustun drift: kod `production_order_id`/`prepared_by` yozadi; kanonik `material_kits`da `order_id`/`kit_number` (production_order_id YO'Q).
3. FE↔BE: `WarehouseMaterialKits.tsx` GET/POST → har doim xato/bo'sh.

### 3-qator xulosa
Material-kits = audit nomidagi "drift"ning yorqin misoli: kod MAVJUD EMAS `mm_material_kits`ga yozadi, kanonik `material_kits` ustunlari (`order_id`) mos kelmaydi. To'liq CRUD, lekin to'liq ishlamas (GREEN-LIE).

---

## [021] WMS stock (warehouse_stock vs wms_stock)

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| GET `wms/stock` (list) | `wms-stock.controller.ts:53` → `get-stock-inventory.handler.ts:29` (`FROM warehouse_stock JOIN material_cards`) | `warehouse_stock`=25 | WMS stok | yo'q | ⭐ REAL — kanonik warehouse_stock (25 JONLI) | REAL |
| PATCH/DELETE `wms/stock/:id` | `wms-stock.controller.ts:112,125` → `WmsCrudService` → `wms-crud.repository.ts:109` (`UPDATE wms_stock`) | `wms_stock`=YO'Q | WMS | yo'q | jadval YO'Q → xato | GREEN-LIE |
| POST `wms/stock` (create) | — | — | — | — | A2 retire (2026-06-05): o'chirilgan (green-lie tozalandi) | (olib tashlangan) |
| POST `wms/stock/reserve` | `wms-stock.controller.ts:91` → `ReserveMaterialCommand` | `stock_reservations` | WMS | yo'q | REAL reserve | REAL |

- **DB ISBOT:** `warehouse_stock`=25 (kanonik, JONLI), `current_stock`=25 (warehouse_stock ustidagi VIEW), `wms_stock`=**YO'Q** (`to_regclass`=null), `stocks`=0 (world-3 bo'sh).
- **Ikki olam:** list/inventory **kanonik `warehouse_stock`** o'qiydi (to'g'ri), lekin CRUD (`WmsCrudService`) **`wms_stock`**ga yozadi (`schema-compat-5`) — u DB'da YO'Q.

### SAP-taqqos
SAP stock = MARD/MCHB + movement types. Bizda READ yo'li (list/inventory) kanonik `warehouse_stock`ga (25 JONLI) — bu to'g'ri va SAP-yaqin. Lekin WRITE yo'li (patch/delete) MAVJUD EMAS `wms_stock`ga → read/write bo'lingan. **SAP-darajasi: 🟡 yarim (read REAL, write o'lik jadvalga).**

### Muammolar
1. KOD↔DB: `wms-crud.repository.ts:109,124` `UPDATE wms_stock` — `wms_stock` jadvali YO'Q (`to_regclass`=null) → patch/delete har doim xato.
2. Read/Write split: list `warehouse_stock` (25), patch/delete `wms_stock` (YO'Q) — bir resurs, ikki jadval.
3. TEST: 0 ta.

### 3-qator xulosa
Stock LIST kanonik `warehouse_stock` (25 JONLI) o'qiydi — REAL, eng to'g'ri ulangan WMS qismi. Lekin patch/delete MAVJUD EMAS `wms_stock`ga yozadi (GREEN-LIE). Read/write jadval bo'linishi.

---

## [022] WMS catalog/transactions

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| `warehouse/reports/abc-analysis` | `wms-catalog.controller.ts:39` → `abc-aging-expiry.service.ts:20` (`FROM material_cards`) | `material_cards`=21 | WMS hisobot | yo'q | REAL ABC — kanonik material_cards | REAL |
| `warehouse/stats/total`, `dashboard/kpis` | `dashboard.service.ts:40,50` (`material_cards`+`warehouse_stock`) | 21 / 25 | WMS dashboard | yo'q | REAL aggregat | REAL |
| `warehouse/dashboard` | `wms-catalog.controller.ts:99` | — | WMS | yo'q | `return { totalItems:0, lowStock:0, ... }` hardcoded | FAKE-DATA |
| `warehouse/transactions` | `wms-catalog.controller.ts:145` (`FROM warehouse_transactions`) | `warehouse_transactions`=0 | WMS | yo'q | REAL select (jadval bo'sh) | REAL |

- **DB:** `material_cards`=21, `warehouse_stock`=25 (REAL manba), `warehouse_transactions`=0, `wms_transactions`=0.

### SAP-taqqos
SAP WM analytics = LX02/LX03 + ABC + slow-mover. Bizda ABC/aging/expiry/turnover/stock-balance kanonik `material_cards`+`warehouse_stock`dan REAL hisoblanadi — yaxshi. Lekin `dashboard` endpoint hardcoded nol qaytaradi (FAKE), tranzaksiyalar jadvali bo'sh. **SAP-darajasi: 🟡 yarim.**

### Muammolar
1. KOD: `wms-catalog.controller.ts:99` `getDashboard()` → `{ totalItems:0, lowStock:0, pendingReceipts:0, pendingTransfers:0 }` — DB'ga bormaydi, hardcoded (FAKE-DATA). Qoida 6 (controllerda logika yo'q) + Qoida 10 buzilgan.
2. KOD: ABC/aging/turnover metodlari `WmsCatalogService`ga ko'chirilgan (yaxshi), lekin controllerda hali ham raw `db.execute` (`transactions`, `orders-by-date`) — Qoida 15 buzilishi.
3. TEST: 0 ta.

### 3-qator xulosa
Catalog hisobotlari (ABC/aging/turnover/stock-balance) kanonik `material_cards`+`warehouse_stock`dan REAL. `dashboard` endpoint hardcoded nol (FAKE). Tranzaksiya jadvallari bo'sh (qurilish).

---

## [023] WMS warehouses (9–13 turlari)

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| GET/POST `wms/warehouses` | `wms-warehouses.controller.ts:50,77` (`INSERT INTO warehouses`) | `warehouses`=12 | Ombor Dashboard | yo'q | REAL INSERT/SELECT warehouses (12 JONLI) | REAL |
| PATCH `:id/toggle-active` | `wms-warehouses.controller.ts:105` (`UPDATE warehouses`) | `warehouses`=12 | Ombor | yo'q | REAL UPDATE | REAL |
| GET `:id/inventory` | `wms-warehouses.controller.ts:127` (`FROM warehouse_stock JOIN material_cards`) | `warehouse_stock`=25 | Ombor | yo'q | REAL — kanonik stok | REAL |
| DELETE `:id` | `wms-warehouses.controller.ts:153` → `WmsCrudService.softDeleteWarehouse` | `warehouses`=12 | Ombor | yo'q | REAL soft-delete | REAL |
| warehouse turlari (9 tur) | `warehouse_types`=9 (HECH BIR WMS controller o'qimaydi) | `warehouse_types`=9 | Ombor Tabs | yo'q | 9 tur jadvalda, lekin WMS unga so'rov yubormaydi | ORPHAN(data) |

- **DB:** `warehouses`=12, `wms_warehouses`=12 (ikkala olam ham 12), `warehouse_types`=9 (JONLI, lekin WMS controllerlar ishlatmaydi).

### SAP-taqqos
SAP'da warehouse = T300 (WM warehouse number) + storage types. Bizda warehouse CRUD kanonik `warehouses` (12 JONLI) ustida REAL — eng mustahkam WMS qismi. 9 ombor-turi `warehouse_types`da bor, lekin `warehouses.type` text ustuni orqali bog'lanadi, `warehouse_types` jadvali WMS'da o'qilmaydi. **SAP-darajasi: ✅ SAP-darajasi (CRUD to'liq, kanonik jadval, JONLI data).**

### Muammolar
1. DATA↔KOD: `warehouse_types`=9 JONLI, lekin hech bir WMS controller undan o'qimaydi (turlar `warehouses.type` matn ustunidan keladi) — `warehouse_types` orphan-data.
2. KOD↔KOD: `warehouses` (12) va `wms_warehouses` (12) ikki olam parallel — qaysi kanonik aniq emas (controllerlar `warehouses` ishlatadi).
3. TEST: 0 ta.

### 3-qator xulosa
Warehouses = bu bo'lakdagi ENG SOG'LOM punkt: CRUD kanonik `warehouses` (12 JONLI) ustida REAL, inventory kanonik `warehouse_stock`. `warehouse_types`=9 jadval o'qilmaydi (orphan-data). SAP-darajasi.

---

## [024] POS movements

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| POST `pos/movements` | `movements.controller.ts:86` → `pos-movement.service.ts:57` (`insertMovement`+lines) | `pos_movements`=2 | POS Monitor | yo'q | ⭐ REAL — balance-guard, kirim/chiqim turlari, audit, event; JONLI (2 qator) | REAL |
| PATCH `:id/status` | `movements.controller.ts:96` → `pos-movement-status.service.ts:89` | `pos_movements`=2 | POS | yo'q | REAL status o'tish + inline GL + WMS-sync | REAL |
| POST `qc-decision` | `movements.controller.ts:110` → `recordQcDecision` | — | POS | yo'q | REAL QC qaror | REAL |
| GET `:id/pdf`, `:id/history` | `movements.controller.ts:128,155` | — | POS | yo'q | REAL PDF + audit history | REAL |

- **DB:** `pos_movements`=2 (JONLI — bu bo'lakdagi kam JONLI tranzaksion jadvallardan biri).
- **Permission:** `@RequirePermission('pos.movements.*')` — granular, SAP-darajasi avtorizatsiya.

### SAP-taqqos
SAP IM/WM goods movement = MIGO + movement types (101/201/...) + availability check. Bizda: movement-type kodi (INTERNAL_ISSUE/EXTERNAL_OUT/...), balance-guard (qoldiq tekshirish, 409 override), FEFO, audit, QC. **Bu eng SAP-yaqin modul.** Yetishmaydi: real-time ATP, batch determination to'liqligi. **SAP-darajasi: ✅ SAP-darajasi.**

### Muammolar
1. TEST: 0 ta spec (eng murakkab modul testsiz).
2. SAP gap: movement type kodi enum, SAPdagidek konfiguratsiyalanadigan movement-type jadvali yo'q (kod ichida).
3. (Kichik) status emit `pos.movement.data.${status}` — `completed` event'i [025]dagi dead-letter listener'ga boradi (lekin GL inline qilingan, double-write yo'q).

### 3-qator xulosa
POS movements = bu bo'lakdagi ENG YAXSHI modul: balance-guard, FEFO, audit, granular permission, JONLI data (2). SAP-darajasi. Faqat testsiz.

---

## [025] POS GL log (dead-letter)

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| GET `pos/gl/movement/:id`, `/pending`, `/journal` | `gl.controller.ts:35,74,81` → `gl-posting-log.service.ts:37,48` | `pos_gl_postings`=0 | GLPostingMonitor.tsx | yo'q | REAL select gl_posting_log | REAL |
| Inline auto-GL (completion'da) | `pos-movement-status.service.ts:203-220` (`glRepo.insertLog` AWAITING_REVIEW) | `pos_gl_postings`=0 | POS | yo'q | ⭐ REAL — completion'da inline GL log yoziladi | REAL |
| POST `pos/gl/entry/:id/approve` → ledger | `gl-posting-log.service.ts:64` → `postToLedger:102` → `gl-posting-log.repository.ts:147` (`INSERT INTO entries`) | `entries`=0 | GLPostingMonitor | yo'q | REAL — tasdiqda kanonik `entries`ga post (mapping bo'lsa) | REAL |
| `PosGlAutoListener` (event) | `pos-gl-auto.listener.ts:34` (`@EventsHandler PosMovementCompletedEvent`) | — | — | yo'q | ⭐ DEAD-LETTER — hech kim event publish qilmaydi | ORPHAN |
| `PosWmsSyncCompletedListener` (event) | `pos-wms-sync-completed.listener.ts` | — | — | yo'q | DEAD-LETTER — hech kim publish qilmaydi | ORPHAN |

- **DB:** `pos_gl_postings`=0, `entries`=0, `gl_account_mappings`=6 (mapping JONLI). `pos_movements`=2.
- **⭐ MEMORY tuzatish:** "POS→GL→log not ledger" da'vosi qisman ESKI — tasdiqlash endi `gl-posting-log.repository.ts:147` orqali kanonik `entries`ga REAL post qiladi (`postMovementToLedger`).

### SAP-taqqos
SAP'da inventory movement → avtomatik FI hujjat (account determination OBYC). Bizda 5-bosqich AI-GL log (EXTRACT/CLASSIFY/COMPUTE/VERIFY/POST) → AWAITING_REVIEW → Finance tasdiq → kanonik `entries` post. Yaxshi arxitektura, lekin: (a) ledger-post NON-FATAL (mapping yo'q bo'lsa jim post-qilmaydi), (b) ikki event-listener O'LIK (dead-letter). **SAP-darajasi: 🟡 yarim.**

### Muammolar
1. KOD: `PosGlAutoListener` (`:34`) va `PosWmsSyncCompletedListener` `@EventsHandler(PosMovementCompletedEvent)` ga obuna, lekin `grep`: HECH BIR joy `new PosMovementCompletedEvent` publish qilmaydi → ikki listener O'LIK (ORPHAN/dead-letter). GL aslida inline qilingan (`pos-movement-status.service.ts:203`).
2. KOD: `postToLedger` NON-FATAL — `gl_account_mappings` bo'lmasa jim o'tadi (warning) → harakat tasdiqlanadi-yu, `entries`ga hech narsa tushmaydi (mapping=6 bor, lekin to'liqligi tekshirilmagan).
3. TEST: 0 ta. `entries`=0, `pos_gl_postings`=0 — hali hech qaysi harakat tasdiqlanmagan.

### 3-qator xulosa
POS GL endi kanonik `entries`ga REAL post qiladi (no-op TUZATILGAN), inline 5-bosqich log AWAITING_REVIEW bilan. Lekin ikki event-listener O'LIK dead-letter (publisher yo'q). Ledger-post non-fatal (mapping bo'lmasa jim).

---

## [026] POS quarantine/QC

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| `moveToQuarantine` | `quarantine-workflow.service.ts:32` → `quarantine-workflow.repository.ts` (`INSERT INTO warehouse_stock`, `UPDATE pos_movements`) | `warehouse_stock`=25 | warehouse-features | yo'q | REAL — QC-HOLD omborga material ko'chiradi | REAL |
| `escalateExpiredQuarantine` (cron) | `quarantine-workflow.service.ts:63` + `pos-quarantine-check.job.ts` | `pos_movements`=2 | — | yo'q | REAL cron — muddati o'tgan → qc_review | REAL |
| GET `quarantine` ro'yxat | `warehouse-features.controller.ts:232` → `listQuarantine` | `pos_movements`=2 | warehouse-features | yo'q | REAL list | REAL |
| QC qaror | `movements.controller.ts:110` (`recordQcDecision`) | — | POS | yo'q | REAL | REAL |

- **DB:** `warehouse_stock`=25, `pos_inventory_passport` (UPDATE qilinadi), `pos_movements`=2.

### SAP-taqqos
SAP QM = inspection lot + usage decision + quality stock (QI). Bizda: QC-HOLD ombor (quality stock ekvivalenti), karantinga ko'chirish, muddat-eskalatsiya cron, QC qaror (APPROVED/REWORK/REJECTED). Yaxshi mos. Yetishmaydi: inspection-lot/sampling plan, sertifikat. **SAP-darajasi: 🟡 yarim (ishonchli workflow, lekin QM lot/sampling yo'q).**

### Muammolar
1. KOD: workflow REAL, lekin `pos-quarantine-check.job.ts` cron — jonli sinov yo'q (jadval kam JONLI).
2. SAP gap: inspection-lot, sampling plan, quality certificate yo'q.
3. TEST: 0 ta.

### 3-qator xulosa
Quarantine/QC workflow REAL — QC-HOLD omborga ko'chirish, muddat-eskalatsiya cron, QC qaror, `warehouse_stock`/`pos_movements` real yoziladi. SAP QM lot/sampling yetishmaydi. Testsiz.

---

## [027] POS printer-config

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| GET/POST/PATCH `pos/printer-config` | `printer-config.controller.ts` → `pos-printer-config.service.ts:18` → `pos-printer-config.repository.ts:36` (`db.insert(posPrinterConfig)`) | `pos_printer_configs`=mavjud | POS Printer Config | yo'q | REAL INSERT/UPDATE pos_printer_configs | REAL |
| GET `active` | `printer-config.controller.ts:43` → `labelService.getPrinterConfig` | `pos_printer_configs` | POS | yo'q | REAL (fallback default) | REAL |
| POST `v2/pos/printer-config/:id/test` | `pos-printer-config-v2.controller.ts:31` → `labelService.sendToPrinter` | — | POS | yo'q | REAL printer ulanish testi (alias) | DUPLICATE(alias) |
| Label print | `printer-config.controller.ts` → `LabelService` (ZPL) | — | POS | yo'q | REAL ZPL chiqarish | REAL |

- **DB ISBOT:** `to_regclass('public.pos_printer_configs')`=mavjud (REAL jadval).

### SAP-taqqos
SAP'da output/label = NACE + SAPscript/Smart Forms + condition records. Bizda: printer config CRUD (IP/port/format ZPL), aktiv config, ulanish testi, ZPL label. Bu nicheli funksiya uchun yetarli. **SAP-darajasi: ✅ SAP-darajasi (printer-config domeni uchun to'liq).**

### Muammolar
1. KOD↔KOD: IKKI controller — `printer-config.controller.ts` (`pos/printer-config`) + `pos-printer-config-v2.controller.ts` (`v2/pos/printer-config`). v2 = bitta `test` endpoint alias (bir xil servis) — yengil dublikat.
2. TEST: 0 ta.
3. (Kichik) `getActive` fallback `{isConfigured:false}` — config yo'qligida xato emas, bu to'g'ri.

### 3-qator xulosa
Printer-config REAL — `pos_printer_configs`ga haqiqiy CRUD, ulanish testi, ZPL label. v2 controller = yengil alias dublikat. Domen uchun SAP-darajasi.

---

## [028] Finance GL/entries (3-olam)

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| POST `finance/gl postDocument` | `gl.service.ts:86` → `drizzle-finance-gl.repo.ts:44` (`db.insert(glDocuments)`) | `gl_documents`=0 | GLDocuments.tsx | yo'q | balance-invariant tekshiradi, lekin FAQAT header (`gl_documents`), satr YO'Q | GREEN-LIE |
| GET `trial-balance` | `drizzle-finance-gl.repo.ts:90` (`FROM entries`) | `entries`=0 | Finance | yo'q | REAL select, lekin BOSHQA jadval (`entries` ≠ `gl_documents`) | REAL(uzilgan) |
| GET `ledger` | `drizzle-finance-gl.repo.ts:104` (`FROM entries`) | `entries`=0 | Finance | yo'q | REAL, lekin `entries`dan | REAL(uzilgan) |
| COA seed/create account | `gl.service.ts:105,122` → `accounts` jadval | `accounts`=42 | GL Accounts | yo'q | ⭐ REAL — 42 hisob JONLI | REAL |
| Payment/GL post → entries | `gl-posting.service.ts:85` → `drizzle-gl-posting.repo.ts:43` (`db.insert(entries)`) | `entries`=0 | Finance | yo'q | REAL — kanonik entries (double-entry tx) | REAL |

- **DB ISBOT (3-olam):** `entries`=0 (kanonik, payment GL bu yerga yozadi), `gl_documents`=0 (manual postDocument bu yerga, FAQAT header), `gl_entries`=0, `gl_journal_entries`=0, `gl_lines`=0 (ishlatilmaydi). `accounts`=42 (COA JONLI), `gl_account_mappings`=6.

### SAP-taqqos
SAP FI = BKPF (header) + BSEG (lines) bitta hujjatda, balanced. Bizda **3+ GL olam**: (1) `entries` — payment/POS GL bu yerga REAL post qiladi (kanonik); (2) `gl_documents` — manual `postDocument` faqat HEADER yozadi (satr yo'q!), trial-balance buni KO'RMAYDI; (3) `gl_journal_entries`+`gl_lines` — ishlatilmaydigan o'lik model. **SAP-darajasi: 🔴 yo'q (3-olam, post↔read uzilgan).**

### Muammolar
1. KOD↔DB GREEN-LIE: `postDocument` (`repo:44`) `glDocuments`ga FAQAT header yozadi (lines yo'q), `getTrialBalance` (`repo:96`) `entries`dan o'qiydi → manual GL hujjat to'lov balansiga HECH QACHON ta'sir qilmaydi. Post 200 qaytaradi, lekin trial-balance o'zgarmaydi.
2. KOD↔KOD: payment GL `gl-posting.service.ts` → kanonik `entries` (to'g'ri olam), lekin `finance/gl postDocument` → `gl_documents` (boshqa olam) — ikki yozuvchi ikki jadvalga.
3. DATA: `accounts`=42 (COA REAL JONLI) — yagona JONLI GL ma'lumot; barcha jurnal jadvallari 0.

### 3-qator xulosa
GL 3-olam: payment-GL kanonik `entries`ga REAL post (to'g'ri), lekin manual `postDocument` `gl_documents`ga faqat header yozadi va trial-balance `entries`dan o'qigani uchun KO'RINMAYDI (GREEN-LIE). COA=42 JONLI, jurnallar 0. Post↔read uzilgan.

---

## [029] Finance payments

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| POST `finance/payments/record` | `finance-payments.controller.ts:100` → `record-payment.handler.ts:39` (`recordPayment`+`postCustomerPayment`→`entries`) | `payments`=0, `entries`=0 | FinanceApproval.tsx | yo'q | ⭐ REAL — invoice topish, overpay-guard, cents-arifmetika, partial/full, GL provodka, domain event | REAL |
| POST `finance/payments` (root) | `finance-payments.controller.ts:73` | — | Finance | yo'q | `throw NotImplementedException` (record ishlatish kerak) | 501-STUB |
| POST `:paymentId/verify` | `finance-payments.controller.ts:123` | — | Finance | yo'q | `return { message:'Payment verified' }` — DB'ga yozmaydi | GREEN-LIE |
| GET `:invoiceId/outstanding` | `finance-payments.controller.ts:134` | — | Finance | yo'q | `return { outstanding: 0 }` hardcoded | FAKE-DATA |
| PATCH/POST `:id/approve` | `finance-payments.controller.ts:86,143` → `FinanceActionsService.approvePayment` | `payments`=0 | Finance | yo'q | REAL approve | REAL |

- **DB:** `payments`=0, `invoices`/`finance_invoices`=0, `entries`=0 (qurilish bosqichi).

### SAP-taqqos
SAP FI-AR = F-28 incoming payment + clearing + partial/residual. Bizda `record` yo'li SAP-darajasi: invoice lookup, overpayment guard, cents-precision (NaN-himoya), partial/full status, kanonik `entries`ga GL provodka (Dr Cash/Cr AR), domain event. Lekin `verify` GREEN-LIE va `outstanding` FAKE. **SAP-darajasi: 🟡 yarim (record REAL, verify/outstanding soxta).**

### Muammolar
1. KOD GREEN-LIE: `verifyPayment` (`:123`) `{ message:'Payment verified', paymentId }` qaytaradi — HECH QANDAY DB UPDATE yo'q. "Tasdiqlandi" deydi, lekin holat o'zgarmaydi (Q-40/Q-43 buzilishi).
2. KOD FAKE-DATA: `getOutstandingPayment` (`:134`) `{ outstanding: 0 }` hardcoded — invoice qoldig'ini hisoblamaydi, doim 0.
3. TEST: 0 ta.

### 3-qator xulosa
`record` yo'li SAP-darajasi REAL (overpay-guard, cents, partial/full, kanonik `entries` GL, event). Root POST=501 (to'g'ri). Lekin `verify`=GREEN-LIE (yozmaydi) va `outstanding`=FAKE (doim 0).

---

## [030] Finance budgets

### 5-tomonlama jadval
| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| GET/POST `finance/budgets` | `budgets.service.ts:38` → `drizzle-finance-budgets.repo.ts:62` (`db.insert(budgets)`) | `budgets`=0 | BudgetManagement.tsx | yo'q | REAL CRUD budgets | REAL |
| PATCH update / approve | `budgets.service.ts:46,55` (approve: draft-gate) | `budgets`=0 | BudgetManagement | yo'q | REAL — faqat draft tasdiqlanadi (status-gate) | REAL |
| DELETE (soft) | `budgets.service.ts:66` → `softDelete` | `budgets`=0 | BudgetManagement | yo'q | REAL soft-delete | REAL |
| Budget lines | `budgets.service.ts:85` → `repo:110` (`db.insert(budget_lines)`) | `budget_lines`=0 | BudgetManagement | yo'q | REAL satr CRUD | REAL |

- **DB:** `budgets`=0, `budget_lines`=0 (qurilish bosqichi).

### SAP-taqqos
SAP CO budget = KP06/KPZ2 + availability control (AVAC). Bizda budget header+lines CRUD + approve workflow (draft→approved status-gate). Yaxshi asos. Yetishmaydi: availability control (xarajatni budjet bilan tekshirish), commitment, fund reservation. **SAP-darajasi: 🟡 yarim (CRUD+approve REAL, AVAC yo'q).**

### Muammolar
1. SAP gap: budjet faqat saqlanadi — haqiqiy xarajat/PO bilan solishtirish (availability control) yo'q, ya'ni budjet hali "passiv".
2. FE↔BE: BudgetManagement.tsx smoke-test bor, lekin unit/integration test yo'q.
3. TEST: 0 ta backend spec.

### 3-qator xulosa
Budgets REAL — header+lines CRUD, approve status-gate (faqat draft), soft-delete, hammasi `budgets`/`budget_lines`ga haqiqiy yoziladi. SAP availability-control yo'q (passiv budjet). Jadval bo'sh.

---

## UMUMIY XULOSA (016–030)

**Verdikt taqsimoti (asosiy feature darajasida):**
- **REAL:** [016] CQRS PP, [017] MES sessions, [018] OEE/stats+MES→QC, [021] stock LIST, [022] catalog hisobotlar, [023] warehouses (eng sog'lom), [024] POS movements (eng yaxshi), [025] GL→entries post, [026] quarantine, [027] printer, [028] payment→entries+COA, [029] payment record, [030] budgets.
- **501-STUB (to'g'ri, qasddan):** [019] create material, [029] root POST payment.
- **GREEN-LIE (200 lekin yozmaydi/o'qimaydi):** [019] butun /inventory/materials (mm_materials YO'Q), [020] material-kits (mm_material_kits YO'Q), [021] patch/delete stock (wms_stock YO'Q), [028] postDocument↔trial-balance uzilgan, [029] verifyPayment.
- **DUPLICATE:** [016] ProductionOrdersService+pp-planning, [017] ikki session controller, [027] v2 printer alias.
- **ORPHAN:** [016] ProductionOrdersService, [023] warehouse_types data, [025] ikki dead-letter listener.
- **FAKE-DATA:** [022] dashboard hardcoded nol, [029] outstanding=0.

**Eng muhim 5 muammo:**
1. **[019]+[020] o'lik jadvallar:** `/inventory/materials` (`mm_materials`+`wms_stock_levels`) va material-kits (`mm_material_kits`) — bu jadvallar DB'da UMUMAN YO'Q (`to_regclass`=null) → butun sahifalar GREEN-LIE; kanonik `material_cards`=21 ko'rinmaydi.
2. **[028] GL 3-olam uzilishi:** manual `postDocument`→`gl_documents` (faqat header), trial-balance→`entries` o'qiydi → manual GL hujjat balansga ta'sir qilmaydi.
3. **[021] stock read/write split:** list kanonik `warehouse_stock` (25 REAL), lekin patch/delete `wms_stock` (YO'Q).
4. **[029] verify/outstanding soxta:** `verifyPayment` hech narsa yozmaydi (GREEN-LIE), `outstanding` doim 0 (FAKE).
5. **TEST cho'li:** 5 modul (PP/MES/WMS/POS/Finance) bo'ylab JAMI 1 ta backend spec (`wms-counts.dto.spec.ts`) — kritik moliya/stok yo'llari testsiz.

**Eng yaxshi (SAP-darajasi):** [023] warehouses, [024] POS movements, [027] printer-config. **Eng yomon (🔴):** [019] inventory-materials, [020] material-kits, [028] GL 3-olam.

**Qurilish konteksti:** ko'p jadval bo'sh (production_orders/mes_sessions/entries/budgets=0) — bu kutilgan (qurilish bosqichi). JONLI tranzaksion ma'lumot: `pos_movements`=2, `gamification_points`=77, `material_cards`=21, `warehouse_stock`=25, `warehouses`=12, `accounts`=42, `warehouse_types`=9, `gl_account_mappings`=6.
