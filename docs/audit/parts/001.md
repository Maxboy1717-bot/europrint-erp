# 180-PUNKT SAP-AUDIT — Bo'lak 001 (punktlar 001–015)

> Qat'iy READ-ONLY tahlil. Manba: jonli kod (`apps/api/src/modules/`), jonli DB (`_audit/q.cjs`, `europrint` bo'sh-qurilish DB), FE (`artifacts/erp-dashboard/src/pages/`).
> Eslatma: DB deyarli BO'SH (qurilish bosqichi) — "0 qator" demak yozuvchi yo'q degani EMAS; har jadval uchun yozuvchi-kod alohida tekshirildi.

---

## [001] CRM/Savdo core

### 5-tomonlama jadval

| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| Leads list/get/create/stage/qualify | crm-leads.controller.ts:100–203 → leads.service.ts → ICrmLeadsRepository | crm_leads 0 (yozuvchi bor: repo.create/update/softDelete) | CRMWorkspace.tsx, CRMActivities.tsx | crm-leads-ops.dto.spec (faqat Zod) | REAL — lead boshqaruvi ishlaydi | REAL |
| Lead email log | crm-leads.controller.ts:185 → leadsService.logEmail | crm_activities 0 (yozuvchi: logEmail) | CRMWorkspace | yo'q | REAL — pochta provayderi yo'q, halol "queued" activity yoziladi (avval green-lie edi, tuzatilgan) | REAL |
| Deals list/get/create/won/stage/patch/delete | crm-deals.controller.ts:79–203 → CommandBus (Create/MarkWon/UpdateStage/Update/Delete) + DealsService.findAll/create | deals 0 (yozuvchi: CQRS handlerlar) | CRMWorkspace | yo'q (integ.) | REAL — bitim hayot-sikli to'liq | REAL |
| Quick deal | crm-deals.controller.ts:191 → dealsService.create | deals 0 | CRMWorkspace QuickCreate | yo'q | REAL | REAL |
| Companies / contacts / activities / pipelines / custom-fields | crm-companies/contacts/activities/pipelines/custom-fields.controller.ts | crm_companies/contacts/activities/pipelines/custom_fields = 0 (yozuvchi controllerlar) | CRMWorkspace, CRMSettings | dto.spec lar | REAL (CRUD jadvalga bog'langan) | REAL |

### SAP-taqqos
SAP CRM / Odoo CRM: lead→opportunity→quotation→order pipeline, faollik tarixi, scoring. **Bizda:** lead/deal/company/contact/activity/pipeline CRUD jonli, CQRS + Result pattern, Roles-guard. **Gap:** lead↔deal↔SD-order ko'prik kodi to'la avtomatlashmagan (qo'lda convert); pipeline-stage konfiguratsiyasi cheklangan; aktiv 0 qator = real foydalanish hali yo'q. **SAP-darajasi: 🟡 yarim** (arxitektura SAP-darajasi, lekin integratsiya zanjiri va jonli ma'lumot yo'q).

### Muammolar
- KOD↔DB mos: yozuvchilar bor, lekin 5 jadval ham 0 qator — modul qurilgan, ishlatilmagan (FOYDA potensial, jonli emas).
- TEST gap: faqat DTO (Zod) spec lar — controller/service integ-test YO'Q (5-tomonning TEST tomoni zaif).
- `getById`/`getLeadEmails` `findOne` natijasini ataylab `unwrapOrThrow`'siz qaytaradi (izoh bilan to'g'ri) — lekin shakl nomuvofiqligi xavfi bor.

### 3-qator xulosa
CRM core to'liq REAL: lead/deal/company/contact CRUD jonli DB'ga yozadi, CQRS+Result+Roles bilan SAP-uslub arxitektura.
Asosiy kamchilik — integ-test yo'qligi va barcha jadvallar bo'sh (qurilgan, ishlatilmagan), hamda lead→deal→order avtomatik ko'prik to'la emas.
Foydaga tayyor, lekin jonli ma'lumotsiz va zanjir uzilgan.

---

## [002] CRM AI-extended (green-lie timestamp-id)

### 5-tomonlama jadval

| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| **createNbaTask** | crm-ai-extended.controller.ts:138-141 → `return { created: true, taskId: Date.now() }` | crm_tasks 0 — **jadval BOR, lekin YOZILMAYDI** | crm/AIAnalysisPanel.tsx (chaqiradi) | yo'q | **YO'Q — foydalanuvchi "vazifa yaratildi" deydi, hech narsa saqlanmaydi** | GREEN-LIE |
| autofill (svc) | crm-ai-extended.service.ts:13-28 → hardcoded `industry:'Manufacturing', company_size:'50-200', budget:'50M-200M'` | DB'ga bormaydi | AiCrmPage.tsx | crm-ai-extended.dto.spec (Zod) | YO'Q — soxta to'ldirish | FAKE-DATA |
| analyzeChurn (svc) | crm-ai-extended.service.ts:30-42 → hardcoded `churn_risk:'medium', risk_score:0.42, factors[...]` | DB'ga bormaydi | AiCrmPage | yo'q | YO'Q — har mijozga bir xil javob | FAKE-DATA |
| suggestAutoTasks (svc) | crm-ai-extended.service.ts:44-56 → 3 ta hardcoded vazifa | DB'ga bormaydi | AiCrmPage | yo'q | YO'Q | FAKE-DATA |
| getAiLeads / getAiNba (svc) | crm-ai-extended.service.ts:93-114 → `leads:[], total:0, recommendations:[]` | DB'ga bormaydi | AiCrmPage | yo'q | YO'Q — doim bo'sh | FAKE-DATA |
| getAiQuickScore (svc) | crm-ai-extended.service.ts:116-127 → hardcoded `score:65, grade:'B'` | DB'ga bormaydi | AiCrmPage | yo'q | YO'Q | FAKE-DATA |
| postNba / postChurnRescue / scoreLeadV2 / postAutofill (controller) | crm-ai-extended.controller.ts:146-259 | crm_leads o'qiydi (CrmAiService orqali REAL) | AiCrmPage | dto.spec | YARIM — REAL CrmAiService natijasini i18n-label bilan o'raydi (REAL asos) | REAL (wrapper) |
| **CrmAiService** (alohida, ai.controller) | crm-ai.service.ts:15-122 → repo.getLeadWithActivity/updateLeadScore + real scoreLead() formula | crm_leads/deals/activities o'qiydi+score yozadi | AiCrmPage, AIAnalysisPanel | crm-ai.dto.spec | REAL — DB-asosli lead scoring + NBA | REAL |

### SAP-taqqos
SAP Sales Cloud / Einstein: ML-asosli lead scoring, churn prediction real signal'lardan. **Bizda IKKI dunyo:** (a) `CrmAiService` (ai.controller) — REAL: DB o'qiydi, deterministik scoreLead/forecastDeal/NBA hisoblaydi, lead_score yozadi (SAP-uslub, ML emas lekin halol rule-based). (b) `CrmAiExtendedService` (ai-extended controller, `/crm/ai/*`) — **butunlay FAKE**: hardcoded `'Manufacturing'`, `score:65`, `churn:medium`. **Gap:** ai-extended dunyosi soxta; `createNbaTask` green-lie (crm_tasks bor lekin yozilmaydi). **SAP-darajasi: 🔴 yo'q** (ai-extended); halol rule-based qism 🟡.

### Muammolar (dalil bilan)
- **GREEN-LIE (eng jiddiy):** `crm-ai-extended.controller.ts:140` — `{ created: true, taskId: Date.now() }`. `crm_tasks` jadvali MAVJUD (DB tasdiq), lekin INSERT yo'q. FE `AIAnalysisPanel.tsx` buni chaqiradi — foydalanuvchi vazifa yaratildi deb o'ylaydi, real yo'q. Bu Q-40/Qoida 10 buzilishi.
- **FAKE-DATA (7 metod):** butun `CrmAiExtendedService` DB'ga umuman bormaydi — har so'rovga aynan bir xil qotirilgan javob (industry/segment/budget/score/churn).
- **Ikki AI servis chalkashligi:** `CrmAiService` (REAL) ╳ `CrmAiExtendedService` (FAKE) — ikkalasi ham `/crm/ai/*` ostida, dublikat yo'nalish riski (`/crm/ai/nba/:entityType/:entityId` ikkala controllerda bor).
- TEST: faqat DTO spec — soxta-javoblarni ushlovchi test yo'q.

### 3-qator xulosa
Punkt nomi to'g'ri: `createNbaTask` aynan green-lie (timestamp-id, DB yozmaydi) — `crm_tasks` jadvali bo'sh qoladi.
Butun `CrmAiExtendedService` (autofill/churn/score/leads/nba) FAKE-DATA — hardcoded javoblar, foydasi nol.
Yagona haqiqiy qism — alohida `CrmAiService` (DB-asosli rule-based scoring); ikki servisni birlashtirib soxtalarini olib tashlash kerak.

---

## [003] SD orders (sales_orders/sd_orders kanonik?)

### 5-tomonlama jadval

| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| List/get/create/status orders | sd-orders.controller.ts:79–213 → CommandBus/QueryBus (Create/UpdateStatus/...) | sales_orders 0 (yozuvchi: drizzle-sd-orders.repo.ts) | Sales/Orders FE | yo'q | REAL — buyurtma hayot-sikli | REAL |
| Advance payment + bypass + tech-checkpoint | sd-orders.controller.ts:163–200 → ConfirmAdvancePayment/ApproveAdvanceBypass/ApproveTechCheckpoint | sales_orders + sd_advance_idempotency_keys (yozuvchi bor) | Finance/SD FE | yo'q | REAL — avans 70% darvozasi (owner asosiy vizyon) | REAL |
| CSV export | sd-orders.controller.ts:51-75 | sales_orders o'qiydi | SD FE | yo'q | REAL | REAL |
| Order repo writes | drizzle-sd-orders.repo.ts:30–123 → `db.select/update().from(salesOrders)` + `sales_orders` soft-delete | **sales_orders = BASE (kanonik)** | — | yo'q | REAL | REAL |

### Kanoniklik (asosiy savol)
DB tasdiq: `sales_orders` = **BASE TABLE** (yozuvchi: drizzle-sd-orders.repo); `sd_sales_orders` = **VIEW** (sales_orders ustidan, alohida dunyo EMAS). Repo barcha yozuvni `salesOrders`/`sales_orders`'ga qiladi (repo.ts:30,81,88,96,104,115). **Xulosa: KANONIK = `sales_orders`**; `sd_orders` nomli jadval umuman yo'q (faqat nomli modul papkasi). Memory tasdig'iga mos.

### SAP-taqqos
SAP SD (VA01/VA02): sales order + item + schedule line + credit check + ATP. **Bizda:** order header CRUD + avans-darvoza + tech-checkpoint + dept fan-out (owner vizyoni) REAL; item'lar `sales_order_items` (0, yozuvchi bor). **Gap:** kredit-limit/ATP/availability check yo'q; ikki order-dunyo (`sales_orders` ╳ `orders` BASE) hali to'la yarashmagan (`orders` jadvali 2026-06-06 DROP qilingan — yaxshilangan). **SAP-darajasi: 🟡 yarim**.

### Muammolar
- Kanoniklik HAL: `sales_orders` BASE, `sd_sales_orders` VIEW — eski "ikki dunyo" tashvishi SD ichida yo'q (faqat `orders` BASE bilan PP tomonida edi, u DROP bo'lgan).
- TEST YO'Q: SD orders'da integ-test yo'q — avans/checkpoint kabi muhim moliyaviy oqim testsiz.
- Barcha jadval 0 qator — jonli buyurtma yo'q.

### 3-qator xulosa
Kanonik javob aniq: `sales_orders` = BASE (yagona haqiqat), `sd_sales_orders` = VIEW, `sd_orders` yo'q.
Order CRUD + avans-darvoza + tech-checkpoint to'liq REAL, owner asosiy vizyoniga (70% avans→dept fan-out) xizmat qiladi.
Kamchilik — kredit/ATP yo'q, integ-test yo'q, jonli data yo'q.

---

## [004] SD customers/quotations

### 5-tomonlama jadval

| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| Customers list/get/create/update/delete | sd-customers.controller.ts:107–225 → SdCustomersService | sd_customers 0 (BASE, yozuvchi bor) | Customer360Page.tsx | yo'q | REAL — mijoz CRUD | REAL |
| Customer 360 + contacts/interactions/documents/competitors/nps/complaints | sd-customers.controller.ts:174–446 | sd_customer_contacts/interactions(BASE) + competitors/complaints/documents(VIEW) | Customer360Page | yo'q | REAL — to'liq mijoz kartasi | REAL |
| Delete customer/contact/document | sd-customers.controller.ts:219,276,326 → `return {}` (LEGACY_NOOP izoh: svc.softDelete REAL ishlaydi) | sd_customers soft-delete | Customer360 | yo'q | REAL (javob bo'sh, lekin DB yoziladi) | REAL |
| Quotations CRUD + send/approve/convert | sd-quotations.controller.ts:66–188 → SdQuotationsService → repo | sd_quotations=**VIEW** (sales_orders ustidan, is_quotation), sd_quotation_items BASE 0 | Quotation FE | sd-quotations.dto.spec | REAL — taklif→buyurtma konvertatsiya | REAL |
| Price formulas (get/put) + calculate-price | sd-quotations.controller.ts:95–113,234–239 | sd_price_formulas BASE (singleton id=1) | SDSettings FE | yo'q | REAL — narx kalkulyatori | REAL |
| Contracts/payments/KPI/funnel | sd-quotations.controller.ts:89–230 | sd_contracts BASE, sd_payments VIEW | SD FE | yo'q | REAL | REAL |

### SAP-taqqos
SAP SD: customer master (XD01), quotation (VA21)→order. Odoo: customer + quotation→sale order. **Bizda:** mijoz-master to'liq (360, contacts, NPS, complaints, competitors, internal-intelligence layer); quotation→order convert REAL; narx-formula singleton. **Gap:** `customers` jadvali yo'q — AI/ba'zi modul `customers` kutadi, SD `sd_customers` ishlatadi (bo'linish riski, memory'da qayd); kredit-limit yo'q. **SAP-darajasi: 🟡 yarim** (mijoz-master SAP-darajasiga yaqin, lekin master-data bo'linishi va jonli data yo'q).

### Muammolar
- `return {}` (delete:219,276,326) — Qoida 10 nazarda "soxta" ko'rinadi, lekin izoh + service tekshiruvi: `softDelete` REAL DB yozadi, faqat javob shakli bo'sh (FE o'qimaydi). Ehtiyot: javob-shakl drift.
- Master-data bo'linishi: `sd_customers` (SD ishlatadi) ╳ `customers` (mavjud emas, AI kutadi) — integratsiya riski.
- `sd_quotations`/`sd_invoices`/`sd_payments` = VIEW lar — yozuv asosiy BASE jadvalga (sales_orders) boradi; tushunmovchilik xavfi.
- TEST: faqat quotations DTO spec; customers'da test yo'q.

### 3-qator xulosa
Customers (360 ko'rinish, kontakt/NPS/shikoyat) va quotations (convert-to-order, narx-formula) to'liq REAL, service+repo qatlamli.
`sd_quotations`/`invoices`/`payments` VIEW lar — yozuv kanonik `sales_orders`/BASE jadvallarga; arxitektura toza lekin VIEW chalkashligi bor.
Master-data bo'linishi (`sd_customers` ╳ kutilgan `customers`) va integ-test yo'qligi asosiy kamchilik.

---

## [005] Marketing core

### 5-tomonlama jadval

| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| Campaign list/get/create/update/delete/launch | marketing.controller.ts:59–149 → CampaignsService | marketing_campaigns 0 (kanonik, owner 2026-06-05); `campaigns`(uuid)=orfan | MarketingCampaigns.tsx | MarketingCampaigns.smoke | REAL — kampaniya CRUD | REAL |
| Campaign launch | marketing.controller.ts:139-149 → update status='active' | marketing_campaigns | MarketingCampaigns | smoke | REAL | REAL |
| Marketing leads CRUD + status | marketing-analytics.controller.ts:89–136 → LeadsService | marketing_leads 0 (yozuvchi bor) | MarketingLeads.tsx | smoke | REAL | REAL |
| NPS create/list/stats + churn-risk + hot-leads | marketing-analytics-stubs.controller.ts:118–179 → MarketingExtService + INSERT nps_responses | nps_responses 1, marketing_leads | MarketingDashboard | smoke | REAL — NPS jonli yoziladi (1 qator bor!) | REAL |
| Overview aggregate | marketing-analytics-stubs.controller.ts:565-576 → real COUNT(*) | bir nechta jadval | MarketingDashboard | smoke | REAL | REAL |

### SAP-taqqos
SAP Marketing Cloud / Odoo Marketing: campaign, lead nurturing, NPS, segmentation. **Bizda:** campaign CRUD (kanonik marketing_campaigns), lead CRUD, NPS jonli (nps_responses=1 — bo'limda yagona real yozilgan jadval), churn/hot-leads MarketingExtService orqali REAL. **Gap:** segmentation/journey-automation yo'q; `campaigns`(uuid) orfan jadval CQRS create yo'lidan qolgan. **SAP-darajasi: 🟡 yarim**.

### Muammolar
- Ikki campaign jadval: `marketing_campaigns` (kanonik, FE o'qiydi) ╳ `campaigns` (uuid, orfan — FE o'qimaydi, created_by uuid≠int). Controller izohi to'g'ri yo'naltirgan, lekin orfan jadval qoldiq.
- Barcha jadval ~0 qator (nps_responses=1, exhibitions=1 bundan mustasno) — jonli foydalanish minimal.
- TEST: smoke (render) bor, integ yo'q.

### 3-qator xulosa
Marketing core REAL: campaign/lead CRUD kanonik jadvalga, NPS jonli yozadi (bo'limda yagona to'lgan jadval — nps_responses=1).
`campaigns`(uuid) orfan jadval CQRS yo'lidan qolgan — service kanonik marketing_campaigns'ga yo'naltiradi.
Segmentation/automation yo'q, integ-test yo'q — lekin asos mustahkam.

---

## [006] Marketing PR/inbox

### 5-tomonlama jadval

| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| PR list/get/create/update/delete | marketing-analytics-stubs.controller.ts:405–465 → real SQL `pr_activities` | pr_activities 0 (yozuvchi bor) | MarketingPR.tsx | smoke | REAL — PR-faollik CRUD | REAL |
| Inbox conversations list/messages | marketing-analytics-stubs.controller.ts:217–239 → `social_conversations/social_messages` SELECT | social_conversations/messages 0 (yozuvchi bor) | MarketingInbox FE | smoke | REAL — ijtimoiy inbox o'qish | REAL |
| Inbox reply | marketing-analytics-stubs.controller.ts:241–258 → INSERT social_messages + UPDATE conversation | social_messages | MarketingInbox | smoke | REAL — javob yoziladi | REAL |
| Inbox status update | marketing-analytics-stubs.controller.ts:263–270 → UPDATE | social_conversations | MarketingInbox | smoke | REAL | REAL |
| Inbox stats | marketing-analytics-stubs.controller.ts:211–215 → MarketingExtService | — | MarketingInbox | smoke | REAL | REAL |
| **Inbox AI-reply** | marketing-analytics-stubs.controller.ts:260-261 → `stub(...)` | — | MarketingInbox | yo'q | YO'Q — 501 | 501-STUB |

### SAP-taqqos
SAP Emarsys / Odoo Social Marketing: unified inbox (Telegram/IG/FB), PR tracking. **Bizda:** PR-faollik to'liq CRUD (`pr_activities`); inbox o'qish + javob + status REAL (`social_conversations`/`social_messages`). **Gap:** AI-reply 501; tashqi platforma webhook real ulanish kodi sozlanmagan (social_api_configs bor, lekin live sync yo'q). **SAP-darajasi: 🟡 yarim**.

### Muammolar
- AI-reply (501) — FE tugma bo'lsa, "tez orada" javobi.
- Inbox 0 qator + tashqi platforma webhook live emas — inbox bo'sh qoladi (yozuvchi bor lekin manba ulanmagan).
- TEST: smoke; integ yo'q.

### 3-qator xulosa
PR-faollik va inbox (o'qish/javob/status) REAL SQL bilan — `pr_activities`, `social_conversations`, `social_messages` jadvallariga to'la CRUD.
Yagona 501 — AI-reply; tashqi platforma webhook'lari live ulanmagan, shu sababli inbox bo'sh.
Arxitektura SAP-uslubga yaqin, lekin jonli ijtimoiy-tarmoq sync yetishmaydi.

---

## [007] Marketing exhibitions/settings/ab-tests

### 5-tomonlama jadval

| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| Exhibitions list/get/create/update/delete | marketing-analytics-stubs.controller.ts:279–402 → real SQL `exhibitions` | **exhibitions 1** (jonli data!) | MarketingExhibitions.tsx | smoke | REAL — ko'rgazma CRUD | REAL |
| Exhibition leads (list/create) | marketing-analytics-stubs.controller.ts:296–354 → `exhibition_leads` | exhibition_leads 0 (yozuvchi bor) | MarketingExhibitions | smoke | REAL — QR-scan lead | REAL |
| Exhibition QR generate | marketing-analytics-stubs.controller.ts:356–363 → UPDATE qr_code | exhibitions | MarketingExhibitions | smoke | REAL (kod yoziladi; rasm generatsiya yo'q) | REAL |
| Settings get/save/patch | marketing-analytics-stubs.controller.ts:468–550 → `marketing_settings` upsert | marketing_settings 0 (yozuvchi bor) | MarketingSettings FE | smoke | REAL — sozlama upsert | REAL |
| Social-API config CRUD + telegram-webhook | marketing-analytics-stubs.controller.ts:473–541 → `social_api_configs` | social_api_configs 0 | MarketingSettings | smoke | REAL (token maydoni SELECT'da yashiriladi — xavfsizlik) | REAL |
| A/B tests list | marketing-analytics-stubs.controller.ts:273–276 → `marketing_ab_tests` SELECT | marketing_ab_tests 0 (yozuvchi bor) | Marketing FE | smoke | YARIM — faqat GET, create/update YO'Q | REAL (cheklangan) |

### SAP-taqqos
SAP/Odoo Events: exhibition/event mgmt, lead capture (QR), A/B test, channel settings. **Bizda:** exhibition to'liq CRUD + QR + lead capture (jonli — exhibitions=1!); settings/social-API config REAL + token-maskirovka. **Gap:** A/B test faqat o'qiladi (CRUD to'la emas, marketing_ab_tests bo'sh); QR faqat kod (rasm yo'q). **SAP-darajasi: 🟡 yarim** (exhibition qismi 🟢 ga yaqin).

### Muammolar
- A/B test: faqat `GET /ab-tests` — create/run/result yo'q; `marketing_ab_tests` hech qachon to'lmaydi (faqat-GET sahifa anti-pattern, Qoida 19 ruhi).
- QR: kod-string yoziladi, lekin haqiqiy QR-rasm generatsiyasi yo'q (FE o'zi chizishi mumkin).
- Exhibitions=1 — bo'limda kam jadvallardan biri jonli.
- TEST: smoke only.

### 3-qator xulosa
Exhibitions (CRUD+QR+lead capture) va settings/social-API REAL SQL bilan — exhibitions jadvali jonli (1 qator), token-maskirovka mavjud.
A/B test faqat o'qish rejimida — yozuvchi yo'q, `marketing_ab_tests` bo'sh qoladi (yarim funksiya).
Eng to'lgan marketing qismi; A/B CRUD va QR-rasm yetishmaydi.

---

## [008] Marketing analytics/content

### 5-tomonlama jadval

| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| Content posts CRUD + publish | marketing-content.controller.ts:46–135 → MarketingExtService | content jadval (yozuvchi bor) | MarketingContent.tsx | smoke | REAL — kontent boshqaruvi | REAL |
| Content calendar/analytics | marketing-content.controller.ts:91–101 → svc | — | MarketingCalendar.tsx | smoke | REAL | REAL |
| Social accounts + posts CRUD | marketing-content.controller.ts:103–124 + analytics.controller.ts:58–87 → svc | social jadval | MarketingContent | smoke | REAL | REAL |
| Email templates CRUD | marketing-analytics.controller.ts:138–167 → svc | email_templates (yozuvchi bor) | Marketing FE | smoke | REAL | REAL |
| Analytics overview/campaigns/audience/conversion/funnel | marketing-analytics.controller.ts:169–219 → svc.getAnalyticsOverview/getMarketingFunnel/getLeadsBySource | aggregatlar | MarketingDashboard | smoke | REAL — real COUNT/aggregate | REAL |
| Leads loss-analysis | marketing-analytics.controller.ts:211-219 → leadsSvc.getLossAnalysis | marketing_leads | MarketingDashboard | smoke | REAL | REAL |
| **Content AI-generate** | marketing-analytics-stubs.controller.ts:114-115 → `stub(...)` | — | MarketingContent | yo'q | YO'Q — 501 | 501-STUB |
| **Blog AI-generate** | marketing-analytics-stubs.controller.ts:561-562 → `stub(...)` | — | Marketing FE | yo'q | YO'Q — 501 | 501-STUB |

### SAP-taqqos
SAP Marketing / Odoo: content calendar, social scheduling, email campaigns, funnel analytics. **Bizda:** content/social/email CRUD + analytics aggregatlari REAL (real SQL aggregate, soxta emas). **Gap:** AI content/blog generatsiya 501; analytics ML-prognoz emas, tavsifiy aggregate. **SAP-darajasi: 🟡 yarim**.

### Muammolar
- 2 ta AI 501 (content-gen, blog-gen) — FE tugmasi "tez orada".
- Analytics — tavsifiy (descriptive) aggregate, prognoz/atribusiya yo'q (SAP CJA darajasiga yetmaydi).
- TEST: smoke; aggregate to'g'riligi uchun integ-test yo'q.

### 3-qator xulosa
Content/social/email CRUD va analytics-aggregatlari REAL — soxta emas, real SQL COUNT/funnel/loss-analysis.
Faqat AI generatsiya (content+blog) 501; analitika tavsifiy (prognoz yo'q).
Soxta-javob yo'q (501 lar halol) — bu bo'lim ehtiyotkor va to'g'ri belgilangan.

---

## [009] Design core

### 5-tomonlama jadval

| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| Design list/get | design.controller.ts:75–107 → QueryBus (GetDesignOrders/GetDesignOrder) | design_orders 0 (yozuvchi bor) | DesignDashboard.tsx | DesignDashboard.smoke | REAL | REAL |
| Request design / update status | design.controller.ts:109–155 → CommandBus (RequestDesign/UpdateDesignStatus) | design_orders + ow_tech_cards (Phase4) | DesignApproval.tsx | smoke | REAL — buyurtma→dizayn so'rovi (owner vizyon) | REAL |
| Statistics | design.controller.ts:167–175 → designRepo.getStatistics | design_orders aggregate | DesignDashboard | smoke | REAL | REAL |
| Notifications/messages list | design.controller.ts:157–214 → raw SQL `designOrderNotifications`/`designOrderMessages` | 0 (yozuvchi bor) | Design FE | smoke | REAL | REAL |
| Create order-message | design.controller.ts:236–247 → INSERT `designOrderMessages` | 0 | Design FE | smoke | REAL | REAL |
| Order list/templates/dashboard (extended) | design-extended.controller.ts:33–52 → DesignExtendedService → repo | design_orders/designs/templates | DesignExtended.tsx | DesignExtended.smoke | REAL | REAL |
| **Create design order (thin DTO)** | design.controller.ts:216-230 → `NotImplementedException` | — | Design FE | yo'q | YO'Q — 501 (halol, izohli) | 501-STUB |

### SAP-taqqos
SAP PLM / dizayn-workflow: design request→approval→revision→release. **Bizda:** request-design + status + xabar/notifikatsiya + statistika REAL, CQRS+repo; owner vizyoniga (order→design dept fan-out, ow_tech_cards) bog'langan. **Gap:** `createOrder` (thin DTO) 501 — fragmentlangan design-create yuzasi (createOrder ╳ requestDesign ╳ OrdersService.create — 3 yo'l). **SAP-darajasi: 🟡 yarim**.

### Muammolar
- Fragmentatsiya: 3 ta design-create yo'li (design.controller.createOrder 501, requestDesign REAL, design/orders/orders.service.ts alohida) + 2 schema (design_orders ╳ designs) — izoh tan oladi, "keyingi bosqich" deb belgilangan (Q-33).
- TEST: smoke (render) bor, integ yo'q.
- Barcha jadval 0 qator.

### 3-qator xulosa
Design core REAL: request→status→xabar→notifikatsiya→statistika, CQRS+raw SQL, owner order→design fan-out vizyoniga xizmat qiladi.
Yagona 501 — `createOrder` (halol, design-create yuzasi fragmentlangan, unifikatsiya kelajakka belgilangan).
Soxta-javob yo'q; asosiy kamchilik — 3 yo'lli design-create chalkashligi va jonli data yo'q.

---

## [010] Design tooling/notifications/messages (+AI)

### 5-tomonlama jadval

| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| Tooling list + wear-forecast | design.controller.ts:177–204 → raw SQL `design_tooling` + remaining_uses hisob | design_tooling 0 (yozuvchi bor) | Design FE | smoke | REAL — asbob eskirish prognozi (deterministik) | REAL |
| Notifications mark-read | design-extended.controller.ts:77–83 → svc.markNotificationRead | designOrderNotifications | Design FE | smoke | REAL | REAL |
| Order revisions | design-extended.controller.ts:70–75 → svc.getOrderRevisions | design_revisions | DesignExtended | smoke | REAL | REAL |
| **generateDesigns (AI)** | design-extended.repository.ts:78-96 → INSERT `designs` (saqlaydi) | designs 0 (yozuvchi bor) | AIDesignGenerator.tsx | AIDesignGenerator.smoke | YARIM — DB'ga yozadi, lekin image_url YO'Q (rasm generatsiya qilinmaydi) | REAL (bo'sh rasm) |
| approveDesign / rejectDesign | design-extended.repository.ts:106-114 → UPDATE `designs` status | designs | AIDesignGenerator | smoke | REAL (avval echo edi, tuzatilgan) | REAL |
| **verifyDesign (AI check)** | design-extended.repository.ts:98-99 → `Math.random()` score | DB'ga bormaydi | AIDesignGenerator | smoke | YO'Q — har safar tasodifiy ball | FAKE-DATA |
| **generateMockup (3D)** | design-extended.repository.ts:102-103 → `/mockups/${id}.png` placeholder URL | DB'ga bormaydi | AIDesignGenerator | smoke | YO'Q — mavjud bo'lmagan fayl yo'li | FAKE-DATA |

### SAP-taqqos
SAP PLM + tooling: asbob-eskirish (tool life), 3D vizualizatsiya, AI dizayn. **Bizda:** tooling wear-forecast REAL (deterministik remaining_uses), notifikatsiya/revizion REAL; generateDesigns endi DB'ga yozadi (avval green-lie edi). **Gap:** `verifyDesign` = `Math.random()` (soxta ball), `generateMockup` = mavjud bo'lmagan URL, generateDesigns rasmsiz (image_url null). **SAP-darajasi: 🔴 yo'q** (AI qismi soxta); tooling/revizion 🟡.

### Muammolar (dalil)
- **FAKE-DATA:** `design-extended.repository.ts:99` — `passed: Math.random() > 0.2, score: Math.round(70 + Math.random()*30)`. Har chaqiruv boshqa ball — sifat-tekshiruv ma'nosiz.
- **FAKE-DATA:** `:103` — `mockupUrl: /mockups/${designId}-${productType}.png` — bunday fayl yaratilmaydi, 404 beradi.
- generateDesigns (`:78-96`) REAL INSERT lekin `image_url` hech qachon to'ldirilmaydi — "AI dizayn" rasmsiz yozuv (yarim foyda).
- TEST: smoke; soxta-AI'ni ushlovchi test yo'q.

### 3-qator xulosa
Tooling wear-forecast, notifikatsiya, revizion REAL; generateDesigns/approveDesign endi DB'ga yozadi (avvalgi green-lie tuzatilgan).
Lekin `verifyDesign` (`Math.random()` ball) va `generateMockup` (soxta URL) — toza FAKE-DATA, foydasi nol.
"AI dizayn" yozuvlari rasmsiz (image_url null) — yarim foyda; AI yuzasi soxta, real generatsiya yo'q.

---

## [011] QC inspections

### 5-tomonlama jadval

| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| Inspections list/get | qc-inspections.controller.ts:52–71 → QueryBus (GetInspections) + QcNewService | qc_inspections 0 (yozuvchi bor) | InspectionPage.tsx | InspectionPage.smoke | REAL | REAL |
| Create inspection | qc-inspections.controller.ts:73–82 → CommandBus (CreateInspection) | qc_inspections | InspectionPage | smoke | REAL | REAL |
| Submit inspection | qc-inspections.controller.ts:84–96 → CommandBus (SubmitInspection) | qc_inspections | InspectionPage | smoke | REAL — pass/fail + sabab | REAL |
| Update / delete inspection | qc-inspections.controller.ts:98–130 → QcNewService (mavjudlik tekshiruvi + 404) | qc_inspections | InspectionPage | smoke | REAL | REAL |

### SAP-taqqos
SAP QM (QA01/QA11): inspection lot, sampling, results recording, usage decision. **Bizda:** inspection CRUD + submit (pass/fail+sabab+supplier) REAL CQRS; 404 to'g'ri. **Gap:** sampling-plan (AQL), inspection-characteristic (qc_parameters bor lekin inspection bilan to'la bog'lanmagan), usage-decision workflow yengil. **SAP-darajasi: 🟡 yarim**.

### Muammolar
- `createInspection` `@Body()` tipi inline (DTO/Zod yo'q) — submit/create body validatsiyasi zaif (Qoida 3 ruhi).
- AQL sampling-plan yo'q; inspection↔parameter↔SPC zanjiri to'la avtomatlashmagan.
- TEST: smoke; integ yo'q. Barcha jadval 0 qator.

### 3-qator xulosa
QC inspections to'liq REAL: CRUD+submit(pass/fail) CQRS bilan, 404 to'g'ri, FE InspectionPage bor.
SAP QM ga nisbatan AQL sampling va usage-decision workflow yengil; create/submit body Zod-validatsiyasiz.
Soxta-javob yo'q; asos mustahkam, lekin jonli data va integ-test yetishmaydi.

---

## [012] QC defects/braks

### 5-tomonlama jadval

| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| Defects list/get/stats | qc-defects.controller.ts:66–100 → QueryBus (GetDefects) | qc_defects 0 (yozuvchi bor) | QC FE | yo'q | REAL | REAL |
| Report defect | qc-defects.controller.ts:102–116 → CommandBus (ReportDefect) | qc_defects | QC FE | yo'q | REAL — brak qayd | REAL |
| Resolve defect | qc-defects.controller.ts:118–133 → CommandBus (ResolveDefect) | qc_defects | QC FE | yo'q | REAL | REAL |
| Braks cost-impact (stage bo'yicha) | qc-defects.controller.ts:135–148 → real SQL `qc_braks` GROUP BY stage | qc_braks 0 | QC FE | yo'q | REAL — brak-narx aggregate | REAL |
| Pending QC defects | qc-defects.controller.ts:150–161 → SQL `qc_defects` | qc_defects | QC FE | yo'q | REAL | REAL |
| QC approve/reject/inspector-submit (orderId) | qc-defects.controller.ts:163–249 → `_setQcStatus` UPDATE `qc_inspections` | qc_inspections | QC FE | yo'q | REAL — avval echo edi, endi DB UPDATE | REAL |

### SAP-taqqos
SAP QM: defect recording (QF01), quality notification (QM01), cost-of-quality. **Bizda:** defect report/resolve + brak cost-impact (stage bo'yicha aggregate) + QC status-transition (approve/reject/submit endi DB yozadi). **Gap:** 8D/CAPA workflow yo'q; quality-notification yengil. **SAP-darajasi: 🟡 yarim**.

### Muammolar
- `braks/cost-impact` SQL'da `WHERE deleted_at IS NULL OR deleted_at IS NULL` — takror shart (xato emas, lekin nusxa-joylash qoldig'i).
- `_setQcStatus` (avval echo {approved:true} edi) endi REAL UPDATE qiladi — izoh tan oladi (oldingi green-lie tuzatilgan).
- 8D/CAPA yo'q; TEST yo'q; 0 qator.

### 3-qator xulosa
QC defects/braks to'liq REAL: report/resolve CQRS, brak cost-impact aggregate, QC approve/reject/submit endi `qc_inspections`'ga yozadi (avvalgi echo tuzatilgan).
SAP QM ga nisbatan 8D/CAPA va sifat-notifikatsiya workflow yengil.
Soxta-javob yo'q; cost-impact SQL'da takror-shart kosmetik qoldiq, integ-test yo'q.

---

## [013] QC SPC/control-charts (formula)

### 5-tomonlama jadval

| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| **DPMO + Six Sigma** | dpmo.service.ts:98–134 → real formula (Abramowitz-Stegun normalQuantile + Motorola 1.5σ shift) + Cp/Cpk | qc_inspections o'qiydi | QC FE | yo'q | REAL — matematik to'g'ri sigma-level | REAL |
| DPMO by-process | qc-dpmo.controller.ts:36–47 → dpmoSvc.getProcessDpmoData (qcRepo) | qc_inspections | QC FE | yo'q | REAL | REAL |
| **X-bar/R chart + Cp/Cpk** | spc.service.ts:120–182 → Shewhart konstantalari (A2/D3/D4/d2, ISO 7870-2) + decimal.js | hisob (subgroup) | QC FE | yo'q | REAL — sanoat-standart SPC | REAL |
| Control-chart (UCL/LCL) | spc.service.ts:184–232 → `qcRepo.findSpcReadings` + μ±3σ + outOfControl | qc_spc_data 0 (o'qiydi) | QC FE | yo'q | REAL — chegaradan chiqqan nuqtalar | REAL |
| Control-charts wiring | qc-new.controller.ts:105–141 → spcSvc.getControlChart + SQL aggregate | qc_spc_data | QC FE | yo'q | REAL | REAL |

### SAP-taqqos
SAP QM-SPC / minitab-darajasi: X-bar/R, Cp/Cpk, DPMO, sigma-level. **Bizda:** TO'LIQ matematik to'g'ri — Shewhart A2/D3/D4/d2 (ISO 7870-2/AIAG), Cp=(USL-LSL)/6σ, Cpk=min(cpu,cpl), DPMO→sigma Abramowitz-Stegun normal-quantile + Motorola 1.5σ shift, decimal.js precision, 1.33/1.67 thresholds. **Bu bo'lim — butun auditda eng yuqori sifat (SAP-grade).** **Gap:** `qc_spc_data` bo'sh (o'lchov yozuvchisi yo'q) — formula tayyor, ma'lumot yo'q. **SAP-darajasi: ✅ SAP-darajasi** (formula); ma'lumot oqimi 🟡.

### Muammolar
- `qc_spc_data` = 0 va MES→o'lchov→qc_spc_data yozuvchi kodi yo'q — dunyo-darajasidagi formula bo'sh quvur bilan (FOYDA potensial, jonli emas).
- TEST YO'Q: DPMO/SPC formulalari uchun unit-test yo'q (eng test-talab qiladigan qism testsiz — bu jiddiy kamchilik).
- DPMO controller `opportunities:1` qotirilgan by-process'da (sodda hol).

### 3-qator xulosa
SPC/DPMO — butun auditdagi ENG yuqori sifatli kod: Shewhart konstantalari, Cp/Cpk, sigma-level (Abramowitz-Stegun+Motorola), decimal.js — haqiqiy SAP-grade.
Yagona muammo — `qc_spc_data` bo'sh va o'lchov-yozuvchi yo'q (formula tayyor, manba ulanmagan) hamda formula uchun unit-test yo'qligi.
Bu bo'lim foydaga to'la tayyor, faqat MES→o'lchov oqimi va testlar yetishmaydi.

---

## [014] PP routing (work_centers=0)

### 5-tomonlama jadval

| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| Routings list/get/update/delete/approve | pp-routing.controller.ts:50–125 → QueryBus/CommandBus + RoutingsService | routings 0, routing_operations 0 (yozuvchi bor) | RoutingConfiguration FE | yo'q | REAL (lekin operatsiyalar yozilmaydi) | REAL |
| **Create routing** | pp-routing.controller.ts:73-85 → `NotImplementedException` | — | RoutingConfiguration | yo'q | YO'Q — 501 (halol: production-world bo'sh) | 501-STUB |
| Work-centers list/stats/create/update/toggle | pp-work-centers.controller.ts:46–135 → QueryBus/CommandBus (Create/Update WorkCenter) | **work_centers 0** (yozuvchi bor: CreateWorkCenterCommand) | MESWorkCenters.tsx | MESWorkCenters.smoke | REAL — ish-markaz CRUD | REAL |
| Work-center get-by-id / toggle | pp-work-centers.controller.ts:65–135 → GetWorkCenters({}) keyin `.find()` | work_centers | MESWorkCenters | smoke | YARIM — barchasini olib `.find()` (samarasiz), `wc.id===id` int≠string drift | REAL (buggy) |

### SAP-taqqos
SAP PP routing (CA01) + work center (CR01): operation sequence, work-center capacity, standard times. **Bizda:** routing header CRUD + approve REAL; work-center CRUD REAL (CQRS). **Gap:** (1) **`createRouting` 501** — production-world bo'sh (work_centers=0, routings=0, product catalog yo'q); (2) RoutingsService.create faqat header yozadi, `operations[]` yozmaydi — yarim; (3) work-center get-by-id barchani olib `.find()` (N+1 emas lekin samarasiz) + `wc.id===id` (int ╳ string) — topa olmaslik xavfi; (4) `getAll` GET'da `@Body()` o'qiydi (drift). **SAP-darajasi: 🟡 yarim**.

### Muammolar (dalil)
- **work_centers=0** — ish-markazlar kiritilmagan; routing 501 ham shunga bog'liq (FK RESTRICT routing_operations→work_centers, bo'sh dunyoga yozish FK-fail beradi). Izoh to'g'ri tan oladi (Stage 0.0 ga belgilangan).
- `pp-work-centers.controller.ts:74` — `wc.id === id` (id string param, wc.id int) → getById/toggleActive doim topa olmasligi mumkin (tip-drift bug).
- `pp-work-centers.controller.ts:50` — `getAll(@Body() dto)` GET so'rovda body — REST drift.
- RoutingsService.create operations[] yozmaydi (501 izohida tan olingan).
- TEST: work-centers smoke bor; routing test yo'q.

### 3-qator xulosa
Work-center CRUD va routing list/approve/update/delete REAL (CQRS), lekin `createRouting` 501 — production-world bo'sh (work_centers=0, product catalog yo'q).
Bug'lar: work-center getById `wc.id===id` int≠string drift (topa olmaslik), `getAll` GET'da @Body o'qiydi, RoutingsService.create operations[] yozmaydi.
work_centers=0 sababli butun routing zanjiri bo'sh-quvur; halol 501 lar bor, lekin tip-drift bug'lar tuzatilishi kerak.

---

## [015] Technology cards

### 5-tomonlama jadval

| feature | KOD fayl:satr | DB qator | FE sahifa | TEST | FOYDA | verdikt |
|---|---|---|---|---|---|---|
| Tech dashboard/orders/approval-log | technology.controller.ts:36–55 → TechnologyService → repo (real SQL) | papka_orders aggregate | Technology FE | yo'q | REAL | REAL |
| Tech-cards list (tech_cards + technology_cards) | technology.controller.ts:64–108 → repo.findTechCards / findTechnologyCards (SQL) | tech_cards 0, technology_cards 0 (yozuvchi bor) | Technology FE | yo'q | REAL | REAL |
| Order tech-card / card-by-id | technology.controller.ts:57–124 → repo SQL `technology_cards` | technology_cards | Technology FE | yo'q | REAL | REAL |
| Approve/reject order (3-checkpoint) | technology.controller.ts:85–101 → svc.approveOrder/rejectOrder → repo (DB) | papka_orders + log | Technology FE | yo'q | REAL — texnolog tasdig'i (owner checkpoint vizyon) | REAL |
| **runAiCheck** | technology.controller.ts:78-83 → repo.runAiCheck (DB o'qiydi, deterministik ball) | tech_card/format o'qiydi | Technology FE | yo'q | REAL — rule-based tekshiruv (Math.random EMAS) | REAL |
| **getMaterialAlternatives** | technology.repository.ts:53-67 → hardcoded `'12% tejash'/'20% tejash'` | DB'ga bormaydi | Technology FE | yo'q | YO'Q — material nomini shablonlaydi, soxta tejash | FAKE-DATA |
| Dashboard avgProcessingHours | technology.repository.ts:73 → `avgProcessingHours: 4` qotirilgan | — | Technology FE | yo'q | YARIM — boshqa statlar real, bu hardcoded | FAKE-DATA (qisman) |
| **generateCard** | technology.controller.ts:110-115 → `notImplemented(...)` | — | Technology FE | yo'q | YO'Q — 501 | 501-STUB |
| **optimizeCard** | technology.controller.ts:126-131 → `notImplemented(...)` | — | Technology FE | yo'q | YO'Q — 501 | 501-STUB |

### SAP-taqqos
SAP PP-routing + recipe/PI sheet (master recipe), Odoo MRP routing/work-order instructions. **Bizda:** tech-card o'qish (tech_cards ╳ technology_cards — AI-generated boyroq), 3-checkpoint approve/reject (owner vizyoni), runAiCheck rule-based REAL. **Gap:** generateCard/optimizeCard 501 (asosiy "texnologik karta yaratish" funksiyasi yo'q); getMaterialAlternatives soxta. **SAP-darajasi: 🟡 yarim** (o'qish+approval REAL, generatsiya yo'q).

### Muammolar (dalil)
- **501 (asosiy funksiya):** `generateCard` (`:114`) va `optimizeCard` (`:130`) — texnologik karta YARATISH/OPTIMALLASH hali yo'q. Bu modulning markaziy qiymati (karta yaratish) ishlamaydi.
- **FAKE-DATA:** `technology.repository.ts:53-67` — `getMaterialAlternatives` hardcoded `'12% tejash'`, `'20% tejash'`; material nomini shablonga qo'yadi, real katalog-narx solishtiruvi yo'q.
- **Qisman fake:** `:73` `avgProcessingHours: 4` qotirilgan (boshqa dashboard statlar real).
- **IKKI tech-card jadval:** `tech_cards` (sodda) ╳ `technology_cards` (AI-generated boy) — dublikat-semantik, qaysi kanonik aniq emas.
- runAiCheck — halol REAL (warning'lardan deterministik ball, Math.random emas; design verifyDesign'dan farqli — yaxshi misol).
- TEST yo'q; barcha jadval 0.

### 3-qator xulosa
Tech-card o'qish (2 jadval) va 3-checkpoint approve/reject REAL (owner texnolog-darvoza vizyoni), runAiCheck halol rule-based.
Lekin modulning markaziy funksiyasi — `generateCard`/`optimizeCard` — 501 (karta yaratish yo'q); `getMaterialAlternatives` soxta tejash-foizlari bilan FAKE.
`tech_cards` ╳ `technology_cards` dublikat-semantik; karta-generatsiya yo'qligi va material-alternativ soxtaligi asosiy kamchilik.

---

## Umumiy xulosa (15 punkt)

**Eng kuchli:** [013] SPC/DPMO — haqiqiy SAP-grade matematika (Shewhart, Cp/Cpk, sigma-level). [011]/[012] QC, [001] CRM core, [003]/[004] SD — mustahkam REAL CQRS+Result arxitektura.

**Eng zaif:** [002] CRM AI-extended — `createNbaTask` green-lie + butun servis FAKE-DATA. [010] Design AI — `Math.random()` verifyDesign + soxta mockup URL. [015] Technology — markaziy generateCard/optimizeCard 501 + material-alternativ FAKE.

**Asosiy naqshlar:** (1) Core CRUD ham har joyda REAL; (2) "AI" yuzalar deyarli har modulda soxta yoki 501 (CRM-extended, design-AI, tech-gen); (3) 501 lar asosan HALOL belgilangan (Qoida 17/Q-40 hurmati), lekin bir nechta haqiqiy green-lie/fake qoldi; (4) DB deyarli bo'sh — yozuvchilar bor, jonli data yo'q; (5) integ-test deyarli yo'q (faqat DTO Zod + FE smoke), SPC formulalari ham testsiz.
