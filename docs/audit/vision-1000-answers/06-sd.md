# SD — 50 tavsiya-javob

1. Har qator uchun ALOHIDA ishlab chiqarish buyurtmasi (papka ichida alohida pozitsiya), chunki har qatorning muddati/resursi/marshruti farq qilishi mumkin; trigger = `OrderLineConfirmed` eventi (EventEmitter2/outbox), PP har qatorni MPS ga alohida yozuv sifatida oladi.
2. Signal `MaterialRequiredEvent` outbox orqali uzatiladi (payload: buyurtma_id, material_kodi, miqdor, muddat); MM rad etsa yoki 24 soat ichida javob bermasa SD `Ожд.Сырьё` statusida qoladi va sotuv rahbariga + direktorga Telegram eslatmasi ketadi (eskalatsiya darajasi: 24s→rahbar, 48s→direktor).
3. Uch shart KETMA-KET baholanadi (1-kredit limit → 2-to'lov → 3-maket), qisman o'tgan holat `sales_orders.gate_status` JSONB maydonida saqlanadi (har shart `ok|pending|failed` bilan); barcha uch shart `ok` bo'lganda buyurtma avtomatik ishlab chiqarishga o'tadi — bitta shart `failed` bo'lsa zanjir to'xtaydi.
4. FIFO partiya narxi buyurtma tasdiqlash vaqtida MUZLATILADI (`sales_order_items.unit_cost_snapshot`ga yoziladi); buyurtma davomida ombor narxi o'zgarsа buyurtma narxi o'zgarmaydi; GL yozuvida `cost_snapshot` va joriy `warehouse_stock` narxi o'rtasidagi farq `inventory_variance` hisobiga boriladi.
5. Kotirovka muddati (14 kun) o'tganda narx FIFO joriy partiya narxiga avto-yangilanadi; yangilanish mijozga Telegram xabari bilan birga keladi; yangilangan narx avvalgi narxdan farq qilsa (+5%dan oshsa) menejer tasdig'i so'raladi; bir kotirovka maksimal 3 marta yangilanishi mumkin, undan keyin yangi kotirovka ochilishi shart.
6. Davallческое material `warehouse_stock`ga ALOHIDA `owner_type='client'` belgisi bilan qabul qilinadi (mijozga biriktirilgan, ombor balansi ta'sir qilmaydi); qabul/qaytarish aktida menejer javobgar; kafolat depoziti Moliya modulida alohida balans sifatida saqlanadi.
7. Ishlab chiqarish ±10% og'ish ruxsatli; agar 15% ortiq chiqsa tizim menejerga Telegram eslatmasi yuborib tasdiq so'raydi (avto blok qilmaydi); hisob-faktura SHARTNOMADAGI miqdordan emas, REAL chiqgan miqdordan tuziladi; ortiqcha mahsulot `warehouse_stock`ga `overage` belgisi bilan qo'yiladi.
8. Jarima % `order_cancellation_rules` jadvalida saqlanadi (bosqich: maket=30%/bosildi=70%/tayyor=100%; foizlar sozlanadigan, faqat egasi o'zgartira oladi); jarima summasi GL'ga `entries`ga Debit (debitor) / Credit (daromad) sifatida kiradi; mijoz e'tiroz qilsa Coordination modulida nizo-hal vazifasi ochiladi.
9. `debtorControl` GSD = faqat shu menejerning o'z mijozlaridagi OCHIQ qarz summasi; kechikkan qarz (30+ kun) 1.5x og'irlik koeffitsiyenti bilan hisobga olinadi; hisob haftalik dushanba avto-yangilanadi.
10. HR `EmployeeDeactivated` eventi SD'ga yetadi → menejerning barcha mijozlari rahbar tomonidan TANLANGAN yangi menejerga o'tkaziladi (avto round-robin emas, rahbar qaror qiladi); o'tish davridagi ochiq buyurtmalar yangi menejer ismi ostida davom etadi; mijozga ayirboshlash haqida Telegram xabari ketadi.
11. Avans to'lovi bank tasdig'ini KUTADI (Moliya moduli bank ekstraktini tasdiqlagach); tasdiqlanganidan so'ng SD bloki avtomatik ochiladi; bu jarayonni Finance moduli boshqaradi, SD faqat `PaymentConfirmedEvent`ni tinglaydi.
12. Chegirma faqat TO'LIQ 100% avans faktida beriladi (95% = chegirmasiz); qisman to'lov holatida `100% avans` chegirmasi qo'llanilmaydi; tekshiruv BE tomonida `quotation_service.ts`da: `if (advancePercent < 100) discount5pct = false`.
13. Barcha chegirmalar yig'indisi UMUMIY shifti (≈15%) chegarasiga bo'ysunadi; tiraj chegirmasi + avans chegirmasi + doimiy mijoz chegirmasi hammasi birgalikda hisoblanib, 15%dan oshsa tizim bloklaydi; faqat menejer manualiga alohida chegirma limiti yo'q — umumiy shift bitta chegirma poli; logika `sd-quotations.service.ts::checkDiscountCap()` metodida.
14. Klishe saqlash muddati ≈3 yil; muddatga 30 kun qolganda `cron` (haftalik skanerlash) ogohlantirish yuboradi: mijozga Telegram + menejerga bildirishnoma; mijoz 30 kun ichida javob bermasa klishe "hisobdan chiqarish" statusiga o'tadi va egasi tasdig'i bilan yo'q qilinadi, hujjatga akt tuziladi.
15. Mavsumiy mahsulotlar uchun mavsum-oldi signal mavsumdан 8 hafta oldin cron ishga tushiradi; signal ichida o'tgan yil shu mijoz necha dona buyurtma berganini AI tahlil qilib tavsiya miqdor ko'rsatadi; tavsiya SD dashboardida `Маvsumий takliflar` panelida menejerga ko'rinadi (Telegram xabari ham).
16. Bitta qatorda `Ожд.Сырьё` statusi bo'lsa BOSHQA qatorlar davom etishi mumkin (global qoida: qisman blok); bu siyosat `sales_orders.partial_hold_policy='per_line'` (global default; har buyurtmada o'zgartirib bo'lmaydi — egasi global qoidani belgilagan).
17. Bir buyurtmada bir necha tur rang bo'lsa (oltin тиснение + 4 offset rang) HAR TUR alohida hisoblanadi; bo'yoq xarajati = rang_turi × qoplama% × yuza; bu hisob PP marshrut operatsiyasidagi bo'yoq sarf-normasiga to'g'ridan-to'g'ri bog'lanadi (tarif master-data jadvalidan).
18. Shared forma (klishe/shtamp boshqa mijozda ham ishlatilyapti) tizim tomonidan AVTOMATIK ANIQLANADI va menejerga ogohlantirish beriladi; blok qo'yilmaydi (shared forma texnik jihatdan ruxsat); lekin ogohlantirish audit-logga yoziladi.
19. Menejer CRP dan keyinroq sana va'da qilsa tizim `sales_orders.promised_date` va `crp_earliest_date` o'rtasidagi farqni `delay_risk_days` sifatida qayd etadi; PP'ga `urgent=true` bayrog'i ketadi; AI kechikish xavfini 0-100% ball bilan baholab Director dashboardda ko'rsatadi.
20. Reklamatsiya SD'dan ochilganda `ReclamationOpenedEvent` (outbox) yetkaziladi — QC modulida yangi reklamatsiya yozuvi avtomatik yaratiladi; QC hal qilgach (qaytarish/qisman to'lov/qayta ishlab chiqarish) `ReclamationResolvedEvent` yetib keladi → GL'da `entries`ga kredit-nota yozuvi kiritiladi, WMS'da qaytarilgan mahsulot `warehouse_stock`ga qayta qo'yiladi.
21. Leaderboard DUSHANBADAN-YAKSHANBAGA hisoblanadi (izolyatsiyalangan hafta); menejer HR tomonidan tasdiqlangan ta'til/kasallik belgisida bo'lsa o'sha kun(lar) KPI hisobdan CHIQARILADI (hafta kvotasi proporsional qisqaradi); HR `LeaveApprovedEvent` SD'ga yetadi.
22. Buyurtma ID = `sales_orders.id` (integer, avto-increment) + `order_number` maydoni (SO-2026-NNNNNN format); barcha modullar (PP/MES/WMS/GL) bu ID'ga INTEGER FK sifatida bog'lanadi — event payload'da ham `sales_order_id` integer sifatida uzatiladi (string emas), izchillik shu.
23. PDF ikki versiyali shablon saqlanadi: `internal` (tannarx+margin ko'rsatiladi) va `external` (mijozga, faqat sotuv narxi); PDF render servis `generateQuotePdf(orderId, version='external')` chaqiriganda margin maydonlari shablonga umuman kiritilmaydi — BE tomonida `@Roles` guard + shablon tanlovi.
24. Qisman yetkazilgan miqdorga ALOHIDA hisob-faktura chiqariladi (`invoice_type='partial'`); GL'da har qisman faktura uchun alohida debitor yozuvi (`entries`da Debit receivables / Credit revenue); to'liq yetkazganda yakuniy faktura, debitorlar yopiladi.
25. Fallback: Telegram ID yo'q → SMS (telefon raqam bor bo'lsa) → email → faqat menejerga Telegram; kanal preferencesi `sd_customers.notification_channel` maydonida saqlanadi (Telegram/SMS/Email/menejer).
26. ABC segmentatsiya EVENT-BASED qayta hisoblanadi (yangi buyurtma yopilganda trigger); mijoz A dan B ga tushsa Daromadlar bo'limi rahbariga + sotuv rahbariga Telegram xabari ketadi; kredit limiti ham avtomatik ravishda yangi toifaning standart limitiga tushadi (Finance moduli `CreditLimitAdjustedEvent` ishga tushiradi).
27. Nofaol mijoz triggeri CRON-JOB bilan ishlaydi (har kuni tunda); 60 kunlik chegara konfiguratsiyada segmentga qarab o'zgartirish mumkin (`crm_inactivity_rules` jadvalida: A=90kun/B=60kun/C=30kun); nofaol bo'lgan mijoz rahbar tasdig'i bilan boshqa menejerga qayta taqsimlanadi.
28. AI avtomatik tavsiya qiladi (o'lcham/tiraj/rang soniga qarab Офсет vs Флексо); agar tanlangan yo'nalishda mashina yuklamasi 100% bo'lsa alternativ yo'nalish taklif qilinadi (ogohlantirish bilan, faqat tavsiya — blok emas); menejer yakuniy tanlovni o'zi qiladi.
29. Ko'p qatorli buyurtmada har qator uchun alohida muddat ko'rsatilishi mumkin; PP har qatorni ALOHIDA ishlab chiqarish buyurtmasi sifatida rejalashtiradi; bu qaror SD buyurtma tasdiqlash vaqtida `per_line_scheduling=true` belgisi bilan aniqlanadi.
30. 1C raqamini bog'lash INN/telefon match logikasi `sd-customers-import.service.ts`da ishlaydi; 1C raqami bo'yicha qidiruv migration tugashidan keyin ham DOIMIY saqlanadi (tarixiy hujjat uchun foydalaniladi); match topilmasa yangi mijoz yozuvi yaratiladi.
31. WMS EXTERNAL_OUT darvozasida mashina hajmi va pallet o'lchamlari taqqoslanadi; mos kelmasa (yuk sig'masa) tizim OGOHLANTIRADI va logistika mas'uliga Telegram yuboradi; qarorni logistika mas'uli + menejer birgalikda qabul qiladi (AI tavsiya beradi: boshqa mashina yoki qisman yetkazish).
32. `Папка №` `sales_orders.folder_number` maydoni — unikal constraint (UNIQUE INDEX); bir papkaga BIR NECHA buyurtma biriktirilishi mumkin (1 papka : N buyurtma, bir mijoz bir papkada bir necha SKU); PP `papka_orders` VIEW orqali papka bo'yicha guruhlaydi.
33. Tizim oxirgi (eng yuqori revision raqamli) versiyani AKTIV sifatida ishlatadi; eski versiyalar IMMUTABLE saqlanadi (o'zgartirib bo'lmaydi); mijoz v1 tasdiqlasa lekin v2 mavjud bo'lsa — menejerga ogohlantirish, v2 avtomatik bekor qilinadi va v1 asosida buyurtma yaratilib munosabat audit-logga yoziladi.
34. Faqat MENEJERNING SHAXSIY chegirmasi (toifa standart paketidan ortiq bergan qism) bonus hisoblashdan tushiriladi; toifa A uchun standart paket chegirmasi bonus hisobiga ta'sir qilmaydi; hisob `payroll_calculations`da `manager_discount_pct` vs `category_standard_pct` farqi sifatida qayd etiladi.
35. Davallческое material QC karantin tekshiruvidan O'TADI; QC rad etsa mijozga Telegram xabari + menejerga bildirishnoma ketadi, buyurtma `QC_HOLD` statusiga tushadi; qayta qabul yoki buyurtma bekor — menejer hal qiladi.
36. Mijoz AI tavsiyasiga QARSHI marka tanlasa (`E1` printsipi): AI xavfni belgilaydi, menejer TASDIQLAYDI (imzo/elektron tasdiqlash); bu tanlam `sd_order_tech_specs.client_override_log` JSONB maydonida qayd etiladi; reklamatsiya yoki brak kelganda audit shu yozuvga murojaat qiladi.
37. Hisob-faktura raqam ketma-ketligi DB SEQUENCE (`invoices_number_seq`) orqali ta'minlanadi; race condition yo'q — sequence atomic; logika `sd-invoices.service.ts::generateInvoiceNumber()` metodida (DB darajasida, application darajasida emas).
38. Oyning o'rtasida bozor holati o'zgarsa maqsadni sotuv rahbari rasmiy so'rov yuboradi → yuqori rahbar tasdiqlaydi; leaderboard RETROAKTIV o'zgartirilmaydi (o'tgan natijalar muzlatiladi); yangi maqsad keyingi haftadan kuchga kiradi.
39. INN/telefon dublikat topilsa QATTIQ BLOK qo'yiladi (merge qilmasdan yangi yozuv yaratib bo'lmaydi); dublikat topilsa operator ikki yozuvni ko'rib MERGE ni qo'lda bajaradi (avto-merge yo'q); merge workflow Coordination modulida vazifa sifatida ochiladi.
40. Кашировка buyurtmasida offset bosma + gofra quti SINXRON talab: PP ikki alohida ishlab chiqarish buyurtmasi yaratadi; gofra qutisi `predecessor_order_id` = offset buyurtma (offset tugaguncha gofra yig'ishtirilmaydi); bu cheklov MES smena rejasida HARD constraint — birini boshlashdan oldin ikkinchisi tayyor bo'lishi shart.
41. Nofaol menejerning shartnoma/KP hujjatlari IMMUTABLE qoladi (F5 printsipi — tasdiqlangan hujjat o'zgarmaydi); ERP'da yangi mas'ul menejer ko'rsatiladi (ko'rinish uchun), lekin hujjat matniga tegib bo'lmaydi; mijozga yangi mas'ul menejer haqida Telegram xabari ketadi.
42. NDS konversiyasi BE SERVICE qatlamida amalga oshadi (DTO'da `price_with_vat` hisoblab chiqiladi); NDS stavkasi `tax_rates` jadvalida saqlanadi (faqat egasi/moliya direktori o'zgartira oladi); FE ham, PDF ham BE'dan tayyor `price_with_vat` qiymatini oladi — FE da hisob-kitob yo'q.
43. Margin maydoni BE tomonida `@Roles(['director','sales_manager_senior'])` guard bilan himoyalanadi; menejer rol bilan kirsa DB query'da `margin_pct` SELECT LIST'dan chiqarib tashlanadi (BE `SdOrderProjection::forRole(role)` metodi); response interceptor yoki DTO filter emas — query darajasida chiqariladi.
44. `отгрузка + N kun` hisob EVENT orqali (`OrderShippedEvent`) ishga tushadi; N kun mijozning `sd_customers.payment_delay_days` maydonidan olinadi (shartnomaga bog'liq); shartnoma muddat o'zgarsа FAQAT KEYINGI отгрузkalardan hisob yangilanadi — oldin hisoblangan qoldiqlar o'zgarmaydi.
45. Etiketka miqdori `руlon` birligida buyurtmada ko'rsatiladi; PP'da `dona` birligiga aylantirish `unit_conversion_rules` jadvalidan (`rulon → dona` koeffitsiyent mahsulot turiga qarab) avto-hisoblanadi; rulon WMS'da `material_type='roll'` belgisi bilan alohida o'lchov birligida saqlanadi.
46. Muddati o'tgan prosrochka bo'yicha yangi buyurtmani Daromadlar bo'limi boshlig'i tekshiradi va qaror qiladi (tasdiqlaydi yoki rad etadi); bu ruxsat Daromadlar bo'limi boshlig'i kartasining `razryad` darajasidan keladi (karta-model RBAC); SLA = 24 soat ichida javob, javob yo'q bo'lsa Moliya direktoriga eskalatsiya.
47. PP AI navbatni uch mezon bo'yicha ustuvorlashtiradi: (1) `promised_date` yaqinligi, (2) mijoz ABC toifasi (A ustun), (3) mashina yuklamasi optimizatsiyasi; ustuvorlik natijasi SD menejerga buyurtma kartasida `queue_position` va `estimated_start` sifatida ko'rinadi.
48. Muzlatilgan zona (≈3 kun) shoshilinch buyurtma uchun faqat DIREKTOR yoki EGASI tasdiqlash bilan ochiladi; SD'da bu holat avtomatik `urgent_order_surcharge` (shoshilinch ustamasi, %) qo'shiladi narxga — foiz master-data jadvalida, egasi belgilaydi.
49. AI haftalik pattern tahlil qilib (masalan `sifat` sababi 3+ hafta ketma-ket oshsa) Director dashboardga + QC boshlig'iga avto-signal yuboradi; bu signal QC modulida CAPA (korrektiv harakat) vazifasini AVTOMATIK ochadi (E3 printsipi: AI rejalashtiradi, lekin `E1`: CAPA vazifasi ochiladi — bajarish inson tomonidan).
50. Distributed transaction outbox pattern bilan himoyalanadi: har bosqich (GL yozuv/WMS chiqim/MES yopish/HR bonus) `domain_events` jadvaliga yoziladi; agar bitta bosqich muvaffaqiyatsiz bo'lsa, kompensatsiya event'i ishga tushadi (masalan GL yozilgan lekin MES yopilmasa — `MesCloseRetryEvent` navbatga tushadi); idempotent handler'lar takror ishlov berish xavfsiz.
