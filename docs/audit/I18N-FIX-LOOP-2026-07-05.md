# To'liq i18n tuzatish loop'i — uz-Latin / Rus / uz-Kirill

**Status:** QUEUED — starts only after all currently active workflows are 100% complete
(VISION-3340 completion loop, Residual Fix Loop §8.10, IoT Kiosk-Screen §8.11, and anything
else already in progress). This does not interrupt or reprioritize any currently-running
work — it only adds an item to the end of the queue.

**Source:** `docs/audit/I18N-FULL-AUDIT-2026-07-04.md` — findings A1 through D13, plus the
"Top 10 highest-leverage gaps" and "Structural verdict" sections of that report. Do not
re-derive findings from scratch; use that report as the baseline and re-verify each item's
current state at PLAN time in case anything has shifted during the queue wait.

**Execution model:** sequential, plan-first — one item at a time: PLAN (no edits) → owner
confirmation → EXECUTE → verify → commit → report in Uzbek → stop. This is NOT the continuous
autonomous-loop model used for the VISION-3340 completion run; treat every item here like the
Residual Fix Loop (§8.10) and IoT Kiosk-Screen phase (§8.11) — one at a time, always waiting
for confirmation before editing code. Group items in the same file into one commit where the
audit already groups them; never touch the same file twice across two separate items.

**Compatibility requirement:** every fix must be built as an extension of the EXISTING i18n
stack found in the audit — do not introduce a second, competing i18n library. Specifically:
- **Frontend:** the existing custom key-based system in
  `artifacts/erp-dashboard/src/lib/i18n/` (`loader.ts`, `context.tsx`,
  `getTranslation(lang,module,key)`) is the canonical FE mechanism. Any file currently using
  the buggy 2-arg `t(uz,ru)` helper must be migrated TO the key-based system, not given a
  patched-up 2-arg helper with a third branch bolted on.
- **Backend:** `nestjs-i18n` (already wired in `apps/api/src/app.module.ts`) is the canonical
  BE mechanism. New localized messages must use `i18n.t()` through this existing setup, not a
  new ad-hoc translation function.
- **Database:** follow the audit's finding that the current convention is a base column (uz) +
  `_ru` sibling column. Decide (at PLAN time, and confirm with the owner before executing
  schema changes) whether Cyrillic gets a third `_cyrl` sibling column per existing convention,
  or whether this is the point to introduce a proper translations table — do not silently pick
  one without flagging the tradeoff to the owner first, since this is an architecture decision
  that affects every future bilingual column added to the schema.

---

## Item ordering — by leverage (highest first, per the audit's own ranking)

Group into tiers; within each tier, one item = one PLAN→EXECUTE→commit cycle. Do not skip
ahead to a later tier before the current one is done unless the owner explicitly redirects.

### Tier 1 — foundation fixes (do first, everything else is cheaper after)

**F1 — Centralize number/date/currency formatting.**

`lib/format.ts:48,59,70,80,109`, `lib/sd-helpers.ts:7`, `components/director/MetricCard.tsx:57`,
and ~15 `pos-monitor/pages/*` files hardcode `"uz-UZ"` locale regardless of the selected UI
language.

```
PLAN: Plan how to make every date/number/currency formatting call in the files above take the
currently-selected language and map it to the correct locale string (uz-UZ / ru-RU / and the
correct locale for uz-Cyrl display), instead of hardcoding "uz-UZ". Confirm what locale
identifier should be used for Uzbek Cyrillic specifically (this may not have a standard locale
code — flag if so). List every call site you'd change. Do not edit anything yet.
```

> Bu yerdan keyingi Tier 1 qolgan bandlari va Tier 2/3 (agar bo'lsa) egasi tomonidan
> keyinroq to'ldiriladi/aniqlashtiriladi — hozircha faqat F1 to'liq band sifatida
> qo'shilgan qism (navbat boshlanishidan oldin, PLAN vaqtida `I18N-FULL-AUDIT-2026-07-04.md`
> ning to'liq A1-D13 ro'yxati va "Top 10 highest-leverage gaps" bo'limi qayta o'qib chiqilib,
> qolgan bandlar tier bo'yicha shu faylga qo'shiladi).

---

*Ushbu fayl faqat NAVBATGA QO'YISH hujjati — hozircha KOD YOZILMAGAN. Ijro faqat yuqorida
sanab o'tilgan barcha joriy/navbatdagi ishlar 100% tugagandan va egasi shu loop'ni
boshlashga aniq ruxsat berganidan keyin boshlanadi.*
