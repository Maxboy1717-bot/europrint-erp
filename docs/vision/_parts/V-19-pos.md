## [V/VERIFY] POS (19) — cross-ref hal qilindi

Kod-tomon izlash: `apps/api/src/modules/pos/**`, `lib/db/src/schema/pos-schema*.ts`, `artifacts/erp-dashboard/src/pos-monitor/**`. Barcha 38 "cross-ref kerak" satri jonli kod bilan hal qilindi.

| # | Savol (qisqa) | Oldingi | Hal qilingan status | Dalil (fayl:satr / jadval / grep) |
|---|---|---|---|---|
| 3 | Texkarta norma≠skan → QATIY BLOK + override | cross-ref kerak | Qisman | Blok REAL+wired: `pos-techcard-gate.service.ts:58` checkLines → `pos-movement.service.ts:281-288` BadRequestException; `recheckOnOrderChange` (:130) + endpoint `movements.controller.ts:171` `/recheck-techcard`; WMS `OutboundEnforcementService` reuse. GAP: boshliq-override yo'li topilmadi (grep override_approved_by → yo'q) |
| 4 | Offline DRAFT + UUID idempotency + CONFLICT | cross-ref kerak | Ha | `pos-sync.service.ts:39-76` push: clientUuid dedup→ConflictException, `markConflict`+`conflictReason`, syncStatus 'CONFLICT'; `idempotencyKey` `pos-movement.service.ts:78-98,341-348`; jadval `pos_offline_queue` (sync_status CHK PENDING/SYNCED/FAILED/CONFLICT, pos-schema-v2.ts:510-526) |
| 6 | FEFO(expiry) yoki FIFO; muddati o'tgan TO'LIQ BLOK | cross-ref kerak | Ha | `pos-fifo.service.ts:46-72` getCandidates: `hasExpiry`→FEFO(`expiry_date ASC`)/FIFO(`received_date ASC`), WHERE `status='ACTIVE'` (EXPIRED partiya kandidatdan CHIQARILADI=blok); `markExpiredBatches` (:108, cron `pos-fifo-recalculate.job.ts:17` `0 2 * * *`) `status='EXPIRED'`. Jadval `pos_batches` (data-check rowcount) |
| 7 | Smena akti PDF + PIN e-imzo, imzosiz keyingi smena blok | cross-ref kerak | Qisman | 2-IMZO GATE REAL: `pos-shift-handover.service.ts:68-91` sign() (from/to ikkalasi imzolamaguncha 'closed' emas), endpoint `shift-handover.controller.ts:77` `/:id/sign`; `photo_evidence_url` repo:49. GAP: PIN-imzo (grep pin→yo'q), PDF-generatsiya + keyingi-smena-blok topilmadi |
| 8 | Foto-dalil MinIO/S3, evidence_urls JSONB, QC inline | cross-ref kerak | Qisman | Faqat `photo_evidence_url` (bitta URL) `pos-shift-handover.repository.ts:49,91,115` (handover + pallet). GAP: `pos_movements.evidence_urls` JSONB ustuni YO'Q (pos-schema-v2.ts:74-143 da yo'q); MinIO/S3 + QC-inline uzatish topilmadi |
| 9 | Bin location freeform + dropdown, sig'im OGOHLANTIRISH | cross-ref kerak | Qisman | Bin saqlanadi: `posMovementLines.binId` (pos-schema-v2.ts:161), `pos-movement.service.ts:499`, inventory `binLocation` (pos-inventory-count-query.service.ts:60,116). GAP: sig'im/capacity ogohlantirish YO'Q (grep capacity → 0 hit pos ichida), `wms_bin_locations` bog' topilmadi |
| 10 | Yangi kartochka so'rovi MM'ga, tmp_ DRAFT | cross-ref kerak | Qisman | `material_card_suggestions` jadvali (pos-schema-v2.ts:611, unknownBarcode/aiConfidence/createdMaterialCardId) + `pos-barcode-ext.service.ts:55` reviewAiSuggestion + endpoint `barcode.controller.ts:101,110`. GAP: `tmp_` prefiks DRAFT + `mm_material_requests` yo'naltirish + MM-rad→CANCELLED oqimi tasdiqlanmadi |
| 12 | Poddon/tara RETURNABLE_ASSET, N-M balans, overdue→Finance | cross-ref kerak | Qisman | `pos-shift-handover.service.ts:154` recordPallet + `179` getPalletBalance, endpoint `shift-handover.controller.ts:50,105` `/pallets`+`/pallets/balance`; jadval `pos_returnable_pallets` (raw SQL repo:115, Drizzle sxemada yo'q → data-check). GAP: RETURNABLE_ASSET material-turi + overdue→Finance jarima tasdig'i topilmadi |
| 13 | Lab namunasi LAB_SAMPLE, QC xarajat GL, salbiy→storno | cross-ref kerak | Qisman | `LAB_SAMPLE_OUT` enum (pos-schema-v2.ts:33) + kontekst `labSampleReason/labTestRef` (:200-201), persist `pos-movement.service.ts:425`; `INTERNAL_RETURN` mavjud. GAP: LAB_SAMPLE uchun QC-xarajat GL yozuvi YO'Q (auto-gl-posting.service.ts:57-84 da case yo'q → default warn), storno topilmadi |
| 15 | Shubhali: flagged_by_ai (avto) + penalty_confirmed (boshliq) alohida | cross-ref kerak | Qisman | `pos-anomaly.service.ts:64` evaluateMovement → `pos_anomaly_flags` (movement_id,rule_code UNIQUE idempotent) + boshliq notif (`ANOMALY_ALERT_ROLES`:39) + socket; blok YO'Q (E1 mos). GAP: alohida `penalty_confirmed` (boshliq-tasdiq) ustuni tasdiqlanmadi |
| 18 | SD/PP order o'zgarsa → planshet modal "qaytarish tavsiya" | cross-ref kerak | Yo'q | grep `sd.order.updated\|order.updated\|orderUpdated` @ apps/api/src/modules/pos → 0 hit; listener yo'q. (Faqat texkarta-recheck `recheckOnOrderChange` bor — u SD-eventga ulanmagan) |
| 19 | Mijoz materiali ownership_type=CLIENT, GL skip | cross-ref kerak | Qisman | `CUSTOMER_MATERIAL` enum (pos-schema-v2.ts:35) + kontekst `customerId/customerName/isCustomerOwned` (:208-210); GL default-skip (auto-gl-posting.service.ts:82 case yo'q). GAP: `warehouse_stock.ownership_type=CLIENT` ustuni + `CLIENT_RETURN` turi YO'Q |
| 20 | Makulatura SCRAP_IN → SCRAP ombor, sotuv EXTERNAL_OUT narx=0 | cross-ref kerak | Qisman | `WASTE_IN` enum + kontekst `wasteSource` (pos-schema-v2.ts:32,212), persist `pos-movement.service.ts:425`. GAP: alohida SCRAP warehouse_type + makulatura-sotuv daromadi + GL mapping (auto-gl-posting da WASTE_IN case yo'q) topilmadi |
| 21 | Qabul akti 3 qator (yaxshi/buzuq-karantin/qoldiq) + debit-nota | cross-ref kerak | Qisman | `PARTIAL_RECEIPT` enum + kontekst `orderedQty/acceptedQty/rejectedQty/partialReason` (pos-schema-v2.ts:203-206); karantin `quarantine-workflow.service.ts` + `pos_damage_qc_links.quarantineWarehouseId` (:561). GAP: debit-nota/`mm_credit_notes` bog' YO'Q (grep credit_note→0) |
| 22 | Sikl-sanash guruhlari, natija pos_inventory_counts, tunda | cross-ref kerak | Qisman | `pos_inventory_counts` + ustunlar `countType/scheduledFor/autoGenerated/cycleAbcSegment` (pos-schema-v2.ts:463,478-480), `pos-inventory-count.service.ts:137` scheduledFor. GAP: tungi avto-generatsiya cron topilmadi; MASTER-STATUS 5.5 (for-loop no-tx) hamon ochiq |
| 23 | Avto-tasdiq limiti ±1%, oshgani Finance PENDING_REVIEW | cross-ref kerak | data-check kerak | `pos-variance-config.service.ts:77` getThreshold + `131` decideForLine (AUTO_APPROVE/ESCALATE, fail-CLOSED) + endpoint `inventory-count.controller.ts:61,69,124`. Jadval `pos_variance_config` — rowcount TEKSHIR (config bo'sh→hammasi eskalatsiya). Izoh: eskalatsiya menejerga, "Finance PENDING_REVIEW" emas |
| 24 | Boshlang'ich qoldiq freeze yo'q, opening balance GL direktor tasdig'i | cross-ref kerak | Yo'q | grep `opening_balance\|openingBalance\|opening` @ pos → 0 hit; opening-balance GL yozuvi/direktor-tasdig'i oqimi topilmadi |
| 25 | Prostoy vaqti pos_downtime_requests, MES FK | cross-ref kerak | Yo'q | grep `downtime\|pos_downtime` @ pos + schema → 0 hit; downtime jadval/oqim YO'Q |
| 26 | Kredit-limit real-time, oshsa EXTERNAL_OUT blok | cross-ref kerak | Yo'q | grep `credit_limit\|creditLimit` @ pos → 0 hit; EXTERNAL_OUT'da kredit-limit tekshiruvi/blok topilmadi (chiqim-blok faqat bron/oversell: pos-movement.service.ts:183-251) |
| 27 | Moddiy javobgarlik agregat, terminated→outstanding | cross-ref kerak | Qisman | `employee_inventory_ledger` + `employee_liability_cases` jadvallari (pos-schema-v2.ts:281,343) + `pos-employee-balance.service.ts` agregat + reports `liabilities` (reports.controller.ts:96). GAP: `terminated`→outstanding signal YO'Q (grep terminated @ pos → 0). #45 bilan bir mavzu |
| 29 | Storno GL teskari yozuv, original_movement_id, ikki storno 409 | cross-ref kerak | Yo'q | grep `storno\|reversal\|reverse\|reversed` @ pos → mazmunli hit yo'q (faqat array `.reverse()`); teskari-GL + dubl-409 guard topilmadi. (Faqat `INTERNAL_RETURN`/`INVENTORY_ADJUST` bor) |
| 30 | Norma pp_routing_operations, oshsa qizil + sabab (blok) | cross-ref kerak | Qisman | `material-norms.service.ts` (qo'lda norma CRUD + AI-norma tarixiy iste'moldan, `POS_AI_NORM_MIN_SAMPLE_SIZE`) + endpoint `material-norms.controller.ts`. GAP: `pp_routing_operations`dan norma + oshsa-qizil-blok + `pos_overage_reasons` katalog tasdiqlanmadi (grep overage→0) |
| 32 | Rulon og'irlik+sertifikat pos_movement_rolls, QC lab FK | cross-ref kerak | Yo'q | grep `pos_movement_rolls\|roll_weight\|rollWeight\|movementRolls` @ pos + schema → 0 hit; rulon jadvali YO'Q |
| 33 | STIR/TIN supplier_tin majburiy (regex 9 raqam) | cross-ref kerak | Yo'q | grep `supplier_tin\|supplierTin\|\.tin` @ pos → 0 hit; `pos_movements` da faqat `supplierId/supplierName` (pos-schema-v2.ts:83,133), TIN ustuni/regex YO'Q |
| 34 | currency + exchange_rate (MB kursi), FIFO valyuta konversiya | cross-ref kerak | Qisman | `pos_movements.currency/exchangeRate/totalAmountBase` (pos-schema-v2.ts:89-92) + `resolveExchangeRate` gate (pos-movement.service.ts:405-413, foreign-valyuta rate yo'q→GATE, `exchange_rates` jadvali) + qator `unitPriceBase/totalPriceBase` (:170-171). GAP: FIFO partiya-darajali valyuta konversiya to'liq emas (batch-currency yo'q, #48) |
| 36 | AI taklif mm_purchase_requests (AI_AUTO), kunlik unique | cross-ref kerak | Yo'q | Past-qoldiq faqat NOTIFICATION yaratadi (`pos-low-stock.job.ts:34-58`), PR emas; `procurement-request.service.ts` = QO'LDA tasdiq-zanjiri. AI_AUTO `mm_purchase_requests` + kunlik-unique race-guard topilmadi (grep mm_purchase→0) |
| 37 | Cron 22:00 muddat ogohlantirishi, kunlik unique index | cross-ref kerak | Qisman | Muddat-cron REAL: `pos-fifo-recalculate.job.ts:17` (`0 2 * * *`) markExpiredBatches + `pos.stock.expiry_alert` event → notif (`pos-secondary-events.handler.ts:97-100`). GAP: aynan 22:00 kunlik + `pos_expiry_notifications` unique-index (bir kun-bir xabar dedup) YO'Q |
| 38 | RBAC ombor menejer hammasi/xodim o'z bo'limi, MovementOwnershipGuard 403 | cross-ref kerak | Qisman | `PosDepartmentGuard` (o'z-bo'lim tekshiruv, ForbiddenException) + `PosWarehouseAccessGuard` (`warehouse_access_grants`+`department_warehouse_map`) `guards/pos-department.guard.ts:24-70`; exemptRoles rol-asosli. GAP: karta-permission emas ROL/position-asosli (SB0190 mos); "MovementOwnershipGuard" nomi topilmadi |
| 40 | Telegram Mini App JWT SSO (bot token emas), faqat online | cross-ref kerak | Qisman | `/pos/mini-app/auth` (mini-app.controller.ts:81,92) → `pos-telegram.service.ts:98` validateInitData (initData HMAC-SHA256) → `posTelegramSessions` session-token; `pos-auth.service.ts:54` JwtService.sign. GAP: sof JWT-SSO emas (session-token model); FE `/pos/mini-app` route CLAUDE.md F4 bo'yicha o'chirilgan |
| 41 | pos_movements yillik partitioning, 3y+ arxiv | cross-ref kerak | Yo'q | grep `PARTITION` @ pos + schema → 0 hit; `posMovements` oddiy serial PK + indekslar (pos-schema-v2.ts:144-151), partitioning/arxiv YO'Q (qurilmagan) |
| 42 | Zod price/weight positive (BE+FE), 0-narx → PRICE_CORRECTION | cross-ref kerak | Qisman | BE Zod: `movement.dto.ts:26` quantity `.positive()`, unitPrice `.min(0)`, exchangeRate `.min(0)`; 0-narx GL-0 izoh (:91-101). GAP: `PRICE_CORRECTION` avto-tuzatish oqimi YO'Q (grep→0); FE qatlam alohida tekshirilmadi |
| 43 | Bir xil warehouse_type tezkor ko'chirish, GL yozuv yo'q (netto nol) | cross-ref kerak | Qisman | `INTERNAL_TRANSFER` kanonik `entries`ga YOZILMAYDI: `auto-gl-posting.service.ts:150` filter `debitAccount!==creditAccount` (1010↔1010 skip). GAP: subledger `pos_gl_postings`ga hamon wash-qator yoziladi (:76-78 calculateEntries INTERNAL_TRANSFER case) — to'liq GL-siz emas |
| 44 | Offline IndexedDB background sync, SHA256 idempotency, 3 retry | cross-ref kerak | Ha | FE: `pos-monitor/components/PosOfflineBanner.tsx:17,26` IndexedDB `pos_monitor_offline` + `crypto.randomUUID()` (:51) + `hooks/useOfflineSync`; BE: `pos-sync.service.ts` clientUuid dedup + `pos_offline_queue.retryCount` (pos-schema-v2.ts:520). Izoh: idempotency=UUID (SHA256 emas) |
| 45 | terminated event → pos_outstanding_items, HR+ombor Telegram | cross-ref kerak | Yo'q | grep `terminated` @ pos → 0 hit; `hr.employee.terminated` listener + `pos_outstanding_items` jadvali YO'Q. #27 bilan bir mavzu (agregat bor, terminated-signal yo'q) |
| 47 | Bo'yoq IoT "tugadi" iot_sensor_events, iot_job_ref unique | cross-ref kerak | Yo'q | grep `iot_job_ref\|iotJobRef` @ pos + schema → 0 hit; IoT-signal→POS-chiqim + dubl-guard YO'Q |
| 48 | Multi-currency FIFO: batch_currency + batch_exchange_rate | cross-ref kerak | Qisman | Qator-darajali valyuta bor: `posMovementLines.currency/exchangeRate/unitPriceBase/totalPriceBase` (pos-schema-v2.ts:168-171). GAP: partiya-darajali `batch_currency/batch_exchange_rate` (material_batches) YO'Q (grep→0). #34 bilan bog'liq |
| 49 | Bitta qurilmada MES brak + POS chiqim, mes_session_id FK | cross-ref kerak | Yo'q | grep `mes_session_id\|mesSessionId` @ pos → 0 hit; `posMovements`da MES-session FK YO'Q (production_sessions kanonik, lekin POS-bog' ustuni yaratilmagan) |
| 50 | Director dashboard pos_director_summary MV, 5min cron | cross-ref kerak | Yo'q | grep `pos_director_summary\|director_summary` @ pos + schema → 0 hit; materialized view + drill-down YO'Q. (reports.controller da alohida GET'lar bor, MV emas) |

### Xulosa
- **Ha (3):** #4 (offline idempotency+CONFLICT), #6 (FEFO/FIFO+expired-blok), #44 (FE IndexedDB offline sync).
- **data-check kerak (1):** #23 (`pos_variance_config` rowcount).
- **Qisman (21):** #3,7,8,9,10,12,13,15,19,20,21,22,27,30,34,37,38,40,42,43,48 — kod/jadval/ustun mavjud, lekin vizyon-detali (override, PIN/PDF, evidence_urls JSONB, capacity, tmp_→MM, RETURNABLE_ASSET, QC-GL, penalty_confirmed, ownership_type, SCRAP-sotuv, debit-nota, tungi-cron, terminated-signal, overage-blok, batch-FX, 22:00-dedup, karta-RBAC, JWT-SSO, PRICE_CORRECTION, transfer-subledger-wash, batch_currency) chala.
- **Yo'q (13):** #18,24,25,26,29,32,33,36,41,45,47,49,50 — grep-tasdiqlangan, kod/jadval umuman yo'q.
