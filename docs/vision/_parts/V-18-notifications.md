## [V/VERIFY] Notifications (18) — cross-ref hal qilindi

> Kod-tomon trace (read-only). Kanonik jadval = `notifications` (`lib/db/src/schema/core/core-users.ts:39`).
> Real Telegram dispatcher = `bot-gateway` (9 modul-bot, 1 webhook), NTF-modul + HR telegram-bots + queue/telegram.processor (BullMQ).
> Yangi config-jadvallar: `notification_schedules`, `notification_routing_rules` (migration + CRUD controller + cron/listener wired).

| # | Savol (qisqa) | Oldingi | Hal qilingan status | Dalil (fayl:satr / jadval / grep) |
|---|---|---|---|---|
| 1 | Bitta dispatcher-bot + webhook routing | cross-ref kerak | **Ha** | `bot-gateway.controller.ts:69` `POST /bot/:bot/webhook` → 9 modul-bot (crm/mes/hr/logistics/fin/qc/director/ombor/pos) routing; `telegram-auth.guard.ts:50-68` webhook secret token constant-time (`TELEGRAM_SECRET_TOKEN_<BOT>` / fallback), bitta `TELEGRAM_BOT_TOKEN`. Izoh: env nomi `TELEGRAM_SECRET_TOKEN` (vizyondagi `TELEGRAM_WEBHOOK_SECRET` emas), 1 bot emas 9-bot-1-gateway |
| 2 | Bitta `ntf_notifications` jadval (module_code filter) | cross-ref kerak | **Qisman** | Yagona `notifications` jadval bor (`core-users.ts:39`) — type/priority/reference/metadata/immutable-emas. GAP: nom `notifications` (`ntf_notifications` emas), `module_code` ustuni YO'Q (filtr `type`/`reference_type` orqali) |
| 4 | `priority='CRITICAL'` quiet-hours bypass | cross-ref kerak | **Qisman** | `priority` ustuni bor (`notifications`, `notification_schedules` enum low\|normal\|high\|urgent). GAP: quiet-hours YO'Q (grep `quiet_hour`→0), 'CRITICAL' bypass mantiqi yo'q, enum 'urgent' ishlatadi |
| 5 | Inline ACK bosilmadi → 2× resend → eskalatsiya | cross-ref kerak | **Yo'q** | Notifications modulida inline ACK/callback yo'q (grep `callback_query` NTF-da→0); faqat web `PATCH /:id/read` (`notifications.controller.ts:130`). retry×2→escalate yo'q |
| 6 | Noto'g'ri rol `/company_state` rad; ulanmagan → deep-link | cross-ref kerak | **Qisman** | `/company_state` bor (`remaining/company-state.*`, `director/owner-summary`); ulanmagan javob `bot-gateway.controller.ts:96-103` ("HR ga murojaat qiling"); rol `req.botEmployee.role`. GAP: ulanmaganga deep-link EMAS, oddiy matn |
| 7 | Deep-link/OTP `t.me/bot?start=TOKEN` 24h; telegram_id UNIQUE | cross-ref kerak | **Yo'q** | grep `deep_link`/`OTP`/`start=`/`linkToken` HR telegram-bots→0. `employees.telegram_chat_id` bo'yicha lookup bor (`telegram-auth.guard.ts:85-91`), lekin onboarding token/24h flow yo'q |
| 8 | Digest_window 30s debounce batching | cross-ref kerak | **Yo'q** | grep `digest_window`/`debounce` NTF-da→0. Batching yo'q |
| 9 | `notification_schedules` cron master-data; real-time update | cross-ref kerak | **Ha** | Jadval `notification-schedules-2026-06-30.sql:6`; CRUD `notification-schedules.controller.ts` (list/create/update/delete); iste'mol `notification-schedule.cron.ts:38` `@Cron('0 * * * *')` next_run_at o'qiydi. Izoh: real-time = DB-poll (restart-siz), jonli `updateJob` emas |
| 10 | DELETE trigger + immutable flag | cross-ref kerak | **Yo'q** | `fix-notifications-schema.sql` — DELETE trigger/immutable YO'Q; schedules controller DELETE ruxsat beradi. grep `RAISE EXCEPTION`/`BEFORE DELETE` notifications→0 |
| 11 | `TechCardErrorDetectedEvent` → 15min→ACK→60min taymer | cross-ref kerak | **Yo'q** | grep `TechCardErrorDetectedEvent`→0. Ketma-ket taymer listener yo'q |
| 13 | "Ko'rildi" callback_query → immutable audit-log | cross-ref kerak | **Yo'q** | `notifications.read_at` ustuni bor lekin callback_query ACK audit-log yo'q (grep callback_query NTF→0) |
| 14 | Verbal task Kanban `source='verbal'` + 24h taymer | cross-ref kerak | **Yo'q** | grep `verbal` NTF/telegram-bots→0. verbal_confirmed_at eskalatsiya yo'q |
| 15 | `ntf_doc_views` + doc_version signal | cross-ref kerak | **Yo'q** | grep `ntf_doc_views`→0 (butun repo). Jadval yo'q |
| 16 | "Bog'liq bo'limlar" `workflow_rules`; 10+ throttle | cross-ref kerak | **Qisman** | `workflow_rules` jadval BOR (`workflow-rules-2026-06-20.sql`, director-owned CRUD). GAP: NTF uni fan-out uchun iste'mol qilmaydi; BullMQ throttle bu signalga ulanmagan |
| 17 | Bot inline keyboard primary; web secondary; BullMQ | cross-ref kerak | **Qisman** | Telegram delivery (`telegram-bot.adapter.ts`, BullMQ `queue/processors/telegram.processor.ts`) + web `notifications` ikkalasi bor. GAP: inline keyboard NTF-da yo'q, tun/kun kanal-ustuvorlik mantiqi yo'q |
| 18 | Xabar turini qo'lda tanlash; NLP pre-select | cross-ref kerak | **Yo'q** | NTF-da turi-tanlash UI/oqim yo'q (grep topilmadi) |
| 20 | Brak tabiati QC texnolog belgilaydi; recategorize | cross-ref kerak | **Yo'q** | NTF-tomon trigger yo'q; QC-domen toifalash (bu modul emas), NTF-da dalil topilmadi |
| 21 | `sales_orders.status` → `OrderStatusChangedEvent` listener | cross-ref kerak | **Qisman** | Event BOR (`sd/domain/events/order-status-changed.event.ts`) + listenerlar (`logistics/.../order-status-changed-delivery.listener.ts`, PP). GAP: NTF-modulda status listener YO'Q (faqat `OrderCreatedNotificationListener`) |
| 22 | `warehouse_stock` min/max cron 5daq; 5+ digest | cross-ref kerak | **Qisman** | `stock-alert.cron.ts:44` (lot-expiry, `@Cron('0 8 * * *')` + routing_rules) + `pos-low-stock.job.ts`; min/max_threshold ustunlar bor. GAP: 5-daq cron emas (kunlik), 2min digest_window batching yo'q |
| 23 | IoT `EquipmentFaultEvent` → darhol; offline → `ntf_outbox` | cross-ref kerak | **Qisman** | IoT fault→alert bor (`iot/domain/events/sos-alert-raised.event.ts`, `anomaly-detected.handler.ts`, `mes-sos-escalation`); umumiy outbox bor (`modules/shared/outbox/`). GAP: `EquipmentFaultEvent` (grep→0) va `ntf_outbox` (grep→0) aynan yo'q |
| 24 | `shift_handovers` real jadval; 15daq oldin xabar | cross-ref kerak | **Qisman** | `shift_handovers` real (grep: `d1-mes-shift-handovers-notes.sql`, `pos-shift-handover.*`, `mes_shift_handovers`=VIEW). GAP: smena 15daq-oldin scheduled notification tasdiqlanmadi |
| 25 | `org_nodes.telegram_chat_id`; 403 → shaxsiy fallback | cross-ref kerak | **Qisman** | `telegram_chat_id` BOR lekin `employees` da (`telegram-auth.guard.ts:86`), `org_nodes` da EMAS. GAP: guruh 403 → shaxsiy fallback + admin signal handler yo'q |
| 27 | Telegram xabarda emoji ruxsat | cross-ref kerak | **Ha** | `telegram-bot.adapter.ts:92-105` urgency emoji 🔵🟡🔴 + ⏰ Telegram HTML xabarda; bot-gateway javoblari ⚠️/❌/✅ (`bot-gateway.controller.ts:100,122,136`) |
| 28 | Shablon i18n aware; snapshot at enqueue; placeholder Zod | cross-ref kerak | **Qisman** | Bilingual ustunlar: `notifications.title_uz/ru,message_uz/ru`; `notification_schedules.title_uz/ru,body_uz/ru`. GAP: snapshot-at-enqueue shablon-dvigatel yo'q, placeholder Zod-validate yo'q, uz-cyr yo'q (main doc i18n C10 IN-PROGRESS bilan mos) |
| 29 | Reports 503 → digest PDF'siz; 50MB → link | cross-ref kerak | **Yo'q** | PDF fallback/50MB→link mantiqi topilmadi (grep→0) |
| 30 | Past-3 shaxsiy, top-3 guruh (leaderboard) | cross-ref kerak | **Yo'q** | NTF-da leaderboard past-3/top-3 yo'naltirish topilmadi (gamification-domen, NTF-tomon yo'q) |
| 31 | PP `PlanChangedMidOrderEvent` → "Sabab majburiy?" | cross-ref kerak | **Yo'q** | grep `PlanChangedMidOrderEvent`→0. Sabab-modal signal yo'q |
| 32 | `workflow_rules` chegara-taqiq → avto "chegara buzildi" | cross-ref kerak | **Qisman** | `workflow_rules` jadval BOR (director). GAP: NTF-ga avto chegara-buzilish signali ulanmagan |
| 33 | i.o. avto-bildirishnoma; HR `LeaveApprovedEvent` integrate | cross-ref kerak | **Qisman** | `LeaveApprovedEvent` BOR (`hr/domain/events/leave-approved.event.ts`, `leave-request.aggregate.ts`). GAP: i.o. belgilash / NTF listener tasdiqlanmadi |
| 34 | Kechasi 23:00 cron agregat; GROUP BY (N+1 yo'q) | cross-ref kerak | **Qisman** | Kunlik agregat-cronlar bor (`director/.../owner-summary-daily.cron.ts`, `company-state-snapshot.cron.ts`; NTF-schedule cron soatlik). GAP: aniq 23:00 + `GROUP BY org_node_id` tasdiqlanmadi |
| 35 | Har xabar individual ACK; 3+ ketma-ket | cross-ref kerak | **Yo'q** | Inline per-message ACK yo'q; faqat web `PATCH /:id/read` + `mark-all-read`. 3+ ketma-ket prompt yo'q |
| 36 | Smena `mes_shift_schedules` dinamik; per-shift job + `ShiftEndedEvent` | cross-ref kerak | **Yo'q** | grep `ShiftEndedEvent`→0; per-shift scheduled notification job topilmadi (mes shift schedules bor lekin NTF-job yo'q) |
| 37 | LMS `LmsModuleCompletedEvent` (100%) → avto ACK | cross-ref kerak | **Yo'q** | grep `LmsModuleCompletedEvent`→0. NTF-da faqat `LmsCertExpiredNotificationListener` (boshqa event) |
| 38 | Alert debounce 5daq 3+ → birlashtirish | cross-ref kerak | **Yo'q** | grep `debounce` NTF→0. BullMQ delayed-reset debounce yo'q |
| 39 | Favqulodda bypass inline button + majburiy sabab + manager nusxa | cross-ref kerak | **Yo'q** | Inline-button bypass + majburiy sabab oqimi topilmadi |
| 40 | `design_files.approved_by IS NULL` → approval request | cross-ref kerak | **Yo'q** | NTF-tomon approved_by-NULL trigger topilmadi (grep→0) |
| 41 | NTF faqat signal; blok Kanban (`KanbanBlockRequestedEvent`) | cross-ref kerak | **Yo'q** | grep `KanbanBlockRequestedEvent`→0 |
| 42 | SD `TtValidationService`; NTF 24h → "TT to'ldirilmagan" | cross-ref kerak | **Yo'q** | NTF-da 24h TT-timeout trigger topilmadi (grep→0) |
| 43 | Tasdiqlovchi ta'tilda → `LeaveApprovedEvent` → i.o. karta (dup #33) | cross-ref kerak | **Qisman** | #33 bilan bir xil: `LeaveApprovedEvent` BOR; i.o./rahbar yo'naltirish listener tasdiqlanmadi |
| 44 | "Bir xil xato" QC `defect_type_code` → Kanban `source='ntf-repeat-error'` | cross-ref kerak | **Yo'q** | grep `ntf-repeat-error`→0. Takroriy-xato → task yo'q |
| 45 | Tungi yakka qaror immutable (F5); soft-cancel record | cross-ref kerak | **Yo'q** | soft-cancel/immutable qaror-qaydi topilmadi |
| 46 | Telegram "senga o'tkazaman" rasmiy emas; ERP transfer form | cross-ref kerak | **Yo'q** | Transfer-form havola oqimi topilmadi |
| 47 | Parallel eventlar alohida queue; to'xtatish ustunlik | cross-ref kerak | **Qisman** | BullMQ navbat infra bor (`queue/processors/telegram.processor.ts` concurrency:2, `QUEUE_NAMES`). GAP: per-event izolyatsiya + cancel-logic (yig'ilish vs to'xtatish) yo'q |
| 48 | Event payload `{version,data}`; noma'lum → dead-letter | cross-ref kerak | **Qisman** | BullMQ retry/backoff DLQ bor (`telegram.processor.ts:34` backoffDelay/attemptsMade); EventBridge bor. GAP: `{version,data}` konvert + version-check yo'q |
| 49 | `answerCallbackQuery` darhol; real BullMQ background; retry×3 | cross-ref kerak | **Qisman** | BullMQ background + retry/backoff bor (`telegram.processor.ts:33-49`). GAP: `answerCallbackQuery` instant-ack aynan tasdiqlanmadi (cc-bot/pos-telegram da callback bor, NTF-da yo'q) |
| 50 | Har bot health `ntf_bot_health`; 30s ping cron | cross-ref kerak | **Qisman** | `TelegramSvc.getStatus()` health probe bor (`telegram/telegram.service.ts:161`, real bot-status). GAP: `ntf_bot_health` jadval (grep→0) + 30s ping cron + restart yo'q |
