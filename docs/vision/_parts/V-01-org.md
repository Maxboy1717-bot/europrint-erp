## [V/VERIFY] Org-Kartalar (01) — cross-ref hal qilindi

| # | Savol (qisqa) | Oldingi | Hal qilingan status | Dalil (fayl:satr / jadval / grep) |
|---|---|---|---|---|
| 27 | primary_department_id + ikki-otdeleniye Coordination marshrut asosiy kartadan | cross-ref kerak | Yo'q | grep `primary_department_id\|primaryDepartmentId` butun repo bo'ylab FAQAT doc fayllarga urdi (FULL-VISION, ORGSXEMA, vision-1000-answers) — apps/api/src, lib/db/src/schema, artifacts/erp-dashboard da 0 kod hit. Ustun/marshrut yo'q. |
| 33 | Razryad imtihoni to'liqlik% ≥70% eligibility-gate (blok emas, ogohlantirish+override) | cross-ref kerak | Qisman | Bo'laklar bor, gate ulanmagan: completeness% hisoblanadi `card-folder.service.ts:46` (filled/6*100); exam-config ustunlari mavjud `org-razryad-exam-config-2026-06-19.sql:16-18` (exam_pass_threshold/max_retakes) + seed 60/75 (NOT 70) `razryad-exam-threshold-seed-2026-07-03.sql:17-23`. LEKIN completeness≥70%→imtihon-eligibility bog'lovchi kod YO'Q — grep `completeness\|eligib\|canTakeExam` (org-structure) faqat folder-hisob + RBAC card-gate qaytardi, imtihonga ulanish yo'q. exam_pass_threshold = imtihon o'tish bali (folder-to'liqlik gate emas). |
| 40 | Yangi karta shtat-reja bog'lanish + limit oshsa ogohlantirish (POST /cards check) | cross-ref kerak | Yo'q | grep `shtat\|staffing_plan\|штат\|headcount limit\|staff_plan` (apps/api/src) = 0 fayl. `CardCreateSchema` (card.controller.ts:24-43) da shtat-reja/limit maydoni yo'q; `create()` (card.controller.ts:304-307 → card.service.ts:50) faqat repo.create(dto) — hech qanday limit-check yo'q. |
| 48 | Karta arxiv FK: RBAC/LMS/Coord→SET NULL, Payroll→RESTRICT, is_archived indikator | cross-ref kerak | Qisman | Arxiv+tiklash lifecycle BOR (doc "restore qurilmagan" da'vosini tuzatadi): `card.service.ts:60` softDelete, `card.service.ts:219` restoreCard (archived→active). FK strategiya qisman vizyonga mos: employee_cards(payroll link).card_id → org_functions ON DELETE RESTRICT `org-phase6-employee-cards-2026-06-08.sql:17`; users.card_id / qc_inspections / production_sessions → org_departments ON DELETE SET NULL `migrations-drift.ts:3842,1138-1139`, `migrations-schema.ts:344`. Yetishmaydi: maxsus is_archived ustun (o'rniga current_state='archived'/is_active flag); arxiv = soft-delete (flag), shu sabab FK-on-delete arxiv yo'li EMAS; LMS/Coordination-specific SET NULL tasdiqlanmadi. |
